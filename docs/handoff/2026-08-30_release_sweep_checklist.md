# Handoff: リリース前 実機掃討チェックリスト (台帳の検証待ち 24 行の翻訳)

**書いた状況**: 2026-08-30。門の実装と検証がほぼ畳まれ、残りは「実機を色々触ってまずいところを探す」段になった (まはー)。in_flight 台帳の検証待ち行を、掃討セッションで消し込めるよう**操作の流れ順**に並べ直した一枚。各項目は「何をすれば・何が見えれば緑」だけ書く。**「実質済み」「v0.4 の向こう側」の印は私 (メティス) の見立て** — まはーが同意したらその場で台帳から消す (帳簿の更新は私がやる)。

---

## 0. 再起動の瞬間 (次にバックエンドを再起動したとき、ログに出るもの — 私も掃ける)

- [x] **Playbook 全置き換えの本番初走行**: 2026-08-30 22:37 の再起動で確認 — 25 行を `~/.saiverse/backups/playbooks/playbooks_pre_v030_20260830_223706.json` へ退避 → 全削除 + 退役 permission 4 行掃除 → 同期が 25 本再取込 (imported=25 errors=0)。二都市目のパスは「テーブル空」の分岐に正しく落ちた (隔離検証と同型)。会話も動作 (まはー実機)。
- [x] **オートノミー整理④の巻き取り+prune 本走行** (台帳: v0.3.0 ④): 完了を確認 — 巻き取り (dev4 handler) は過去の再起動で走行済み (今夜の両都市が dev5 から開始 = バージョン鎖が dev4 完了を保証)。最終状態を DB 読み取りで検算: 退役 Playbook 行 0 / スケジュールの META_PLAYBOOK・PLAYBOOK_PARAMS に退役名 0 / 旧 SELECTED_META_PLAYBOOK 列は消滅済み。個別 prune は全置換マイグレーションに上書きされ、退役名は構造的に残れない。
- [x] **head 通知の既読基準** (台帳: last_notified 握り潰し根治): 届くこと自体はまはー実機で確認 (2026-08-30)。**通知の内容 (文面) の調整は、まはーが全体を見てから別途議論**。

## 1. 普段の会話だけで消える行

- [x] **runtime_llm 分割のスモーク** (台帳: 巨大 node 分割): ①通常会話 ✅ ②スペル入り会話 ✅ (2026-08-31 09:00 エリスの定時発言、ログで streaming 経路を確認) ③TOOL ノード ✅ (2026-08-31 10:41 aifi の generate_image、sub-line 分岐→TOOL→MEMORIZE の全段をログで確認。全置換後の再取込 Playbook で走った点も含めて緑) ④sync 経路 = **実機見送り (2026-08-31 まはー裁定)** — 実運用で到達する道が無く ([包み紙 issue](../issues/event_callback_wrapper_defeats_none_checks.md))、pytest (配信口なし実行) が最も厚く踏んでいる経路のため単体回帰を歯止めとする — ユーザー設定は存在せず、env `SAIVERSE_LLM_STREAMING` (既定 ON) だけが公式スイッチ。ただし**発言先の部屋をブラウザが開いていない定時発言は自然に sync に落ちる**設計 (配信口が無いため)。直近 2 セッションの本番ログでは sync 発火 0 回 (常に UI が開いていた)。検証法はまはー選択: (a) 全タブを閉じて定時発言を 1 回踏ませてログ確認 / (b) env OFF で再起動して一言会話。なお pytest は event_callback 無しで走るので単体の回帰は sync 経路が主に受けている。
- [x] **llama-server busy 判定** (台帳: 処理中サーバー射殺の根治): 2026-08-31 実機合格 — gemma4 E4B (Vulkan) を自動起動管理下で走らせ、3 分 37 秒の生成が idle タイマーを跨いで完走 (旧バグなら射殺)、完走後の idle 停止も `idle for 620s (timeout=600s), stopping` で正常発火。副産物 = 設定スナップショットの鮮度 issue ([llama_server_config_snapshot_staleness](../issues/llama_server_config_snapshot_staleness.md)) と再送ボタン欠落 issue ([llm_error_reply_offers_no_retry](../issues/llm_error_reply_offers_no_retry.md)) を起票。残 = §5 の裁定 (8項 + idle_timeout:0 を戻すか)。
- [x] **W10 スペル許可まわり**: 2026-08-31 実機合格 — 「毎回確認」設定の create_building を自律系 Pulse で使わせ、ダイアログは出ずに `requires user permission but running in auto mode. Skipped.` で完走 (まはー実機)。定時 Playbook の起動自体は 09:00/12:00 の定時発言で確認済み。
- [x] **知覚配送の重複なし** (台帳: 知覚消費点の Beat 頭化): 2026-08-31 機械検査で合格 — 全ペルソナの perception_batches / perception_buffer (直近 3 日) を読み取り専用で突き合わせ、**一つの知覚が二つのバッチへ入った例はゼロ** (buffer 1 行 → バッチ 1 個の対応が全行で成立)。同一文面の再出現は (1) 実際に繰り返し起きた出来事 (入退室・スペル可否の変動、それぞれ別の台帳配送 ID を持つ) と (2) 入室時の想起が同じ過去会話を引き直したもの — どちらも設計どおり。※観察メモ: エリスでは入室のたびの想起が 4 回連続で同じ朝の会話を引いており、重複ではないが提示の変化に乏しい。v0.4 の想起チューニングの材料。

## 2. Memory 窓 (Chronicle タブ) — 今夜 (8/29) の検証と地続き

- [x] **head に直近の記憶が戻っている** (台帳: 🔴 提示 gap): 2026-08-31 機械検査で合格 — 本物の提示関数を読み取り専用で全ペルソナに走らせ、**「提示に覆われない期間」ゼロ** (issue の不変条件そのもの)。エリスの実物: Chronicle 30 項が 2024-12〜08-19 を被覆し、08-19 以降は生ログ 130 通で地続き。プレビュー正常のまはー確認と併せて緑。※観察: 血縁が提示に立たない「孤児」(範囲は上位が被覆、編纂側の既知負債) が sophie で 71 件 (修正時 1 件) — undersized chunks 系の管轄でリリース非遮断。
- [x] **読み戻し (§15)**: ✅ 2026-08-31 23:06 本番初発火 — テスタロッサとの会話で `window refill: 28,232→94,643 字 (verified)、80 通引き戻し` をログで確認、context-status の実測とも一致。issue は archive 行き。aifi の二例目は豆粒後始末 (隣人吸収 issue) の再編纂後。プレビュー面は緑 (下端の厚い生ログ / 二重表示なし = まはー確認)。機構は本番未発火だが、全員調査 (2026-08-31) で実態判明 — **13 人中 10 人が目標量 100,000 字を下回っている**。発火しない理由は三種: ①発火は user 会話の開始時のみ (設計 §15-4) で、未達組の大半は機能導入 (7/30) 以降ユーザー会話が無い ②navi は読み戻せる編纂履歴がほぼ無い (正常な skip) ③aifi は直近の編纂の段が開けない形で梯子が設計どおり停止 — **再編纂 (§6 の単発作業) が済むまで読み戻し不能**。計画が立つのは air と**テスタロッサ**の 2 人 (preview で refill_applied=true)。air は暴走記憶の掃除が先 (まはー判断) のため、**検証はテスタロッサと一言会話 → ログの `[metabolism] window refill` をメティスが検算**。※aifi の「UI の記憶整理で再編纂」という旧手順は §13 の範囲規則改修より前の記述で現行では成立しない (2026-08-31 実機で「整理できる履歴がまだありません」— 目標量未満なので畳むものが無い、が正しい応答)。未達 10 人中 8 人で計画が立たない構造は v0.4 の観察課題として issue 化。
- [x] **チャットオプション「データ送信量の管理」**: 水位バーと文字数の整合 = まはー確認 (2026-08-31)。トグル消滅後の通常動作 = ここ数日の全会話が証拠。※「モデル編集で表示が追従」だけ明示確認が残る — 最近モデルを切り替えた際にバーが追従していた記憶があれば頷きで消す。
- [x] **編纂入口の一本化 (§13)**: 2026-08-31 まはー確認 — 普段の会話で確認ダイアログは出ない。
- [x] **Memopedia ロック漏れ**: 確認済み — 8/29 18:13 の生成で `[extraction-backlog] 付箋なし — 拾い直しは不要 (persona=eris_city_a)` を実ログで確認 (回収点が毎回呼ばれている証拠)。失敗を人工的に作る側の検証は省略してよいか、まはーの一言で台帳から消す。

**実質済みの見立て (同意で消せる)**:
- **退場デッドロック根治** と **退場の境目原子性** — どちらの「次」も「通常の Metabolism/退場に退行がない」で、8/29 の §1 (一押しで採取〜畳み完走) と §2 (被覆 10,685 字の正常なあらすじ) がそのまま証拠になっている。
- **体験の構造 (W14)** — 検証対象だった出力側付記は 8/29 に撤去され、後継 (知覚バッチの材料化) を同夜実機合格済み。工程 1〜3 に個別で見たいものが無ければ消せる。
- **実行台帳** — 「スルース経路だけ見る」は 8/25 の窓凍結検証と 8/29 の生成完走が通している。

## 3. 管理 UI・単発の操作で消える行

- [x] **ID 文字種契約のペルソナ適用**: ✅ 2026-08-31 実機合格 — 「テラス」で `persona_1_city_a`、二体目のモーダルに `persona_2` プレフィル (まはー実機)。issue は archive 行き。
- [x] **ComfyUI アドオン**: 2026-08-31 実機合格 (まはー)。初回試行は .env の COMFYUI_BASE_URL 末尾スラッシュ → `//prompt` 405 で失敗し、アドオン側の base URL 正規化で根治 (saiverse-comfyui-addon にコミット、push はまはー合図待ち)。再起動後の再試行で生成成功。
- [ ] **OpenRouter 掲載**: OpenRouter 経由のモデルで一度会話 → 後日 openrouter.ai/apps の roleplay / general-chat に SAIVerse が現れるか。

## 4. v0.3 では走りようがない見立ての行 (同意したら台帳の次アクションを v0.4 へ書き換え)

- **概念再編⑥の検証項目** (就寝判断の棚・朝の報告など) — 大半が自律駆動の止め具 (AUTONOMOUS_DRIVING_SHIPPED=False) の向こう側。v0.3 で見えるのは机移行ログ程度で、それはとうに済んでいるはず。
- **編纂が同名ページを増やす輪** — 再開を判断する対象の Memopedia 編纂 (curation) 自体が v0.3 では走らない (§9-E の検証で確認済み)。v0.4 の運転配線と一緒に。

## 5. 裁定・判断もの (手を動かさず決めるだけ)

- [x] **llama-server「受け入れた限界」8 項の裁定** — 2026-08-31 決着: 7 項受理・4 項 (別ホスト同ポート) は [issue 化](../issues/llama_server_registry_ignores_host.md)。idle_timeout:0 は「まはーの環境の値、触らない」で確定、builtin テンプレの焼き込みは除去。**llama-server 案件は完了、台帳から降ろした。**
- [x] **Elyth Remote MCP** — 完了 (2026-08-31 まはー裁定: 4 巡目は回さない)。台帳から降ろし済み。
- [ ] **quick_spell の台帳行** — 記述が Wave 0 実装完了 (門 §2-8) より古い可能性。残っているのはクオンのデータ修復 GO と §7-4 帳簿系ツール宣言の裁定だけのはず — 行を現況に直すか消すかの確認。
- (v0.4 送りの確認: LLM 使用量記帳の三案 / 刺激の永続 ID — どちらも設計裁定で、リリースは塞がない)

## 6. リリース工程そのもの (掃討とは別立て)

- [x] **段 3**: update.bat / self_update.py の経路検証 — 2026-08-30 深夜に完了。修正 2 件 (snapshot.py の sys.path / requirements.txt の ASCII 化) と issue 1 件 (v0.2.29 の update.bat は pull 直後に死ぬ) を出して、旧 bat → 新 engine → 起動移行 → health 200 まで実機合格。記録は [2026-08-29_v0229_upgrade_test.md](2026-08-29_v0229_upgrade_test.md) の段 3 節。
- [ ] **タグを切るときの必須作業**: dev6 → 0.3.0 の空エッジ追加 (忘れると全ユーザーの起動が version chain エラーで止まる) / VERSION / リリースノート — **「v0.2.x からは update.bat を 2 回実行」の明記が必須** ([issue](../issues/v0229_update_bat_truncates_after_git_pull.md)) / develop → main の PR / `gen_reference_docs --check`。
- [ ] **ZIP インストールの Git 自動導入**: クリーン Windows (git 未導入) での実機テスト — リリース ZIP ができた時に一緒に。
- [ ] **単発作業 (タイミングまはー)**: aifi の豆粒後始末 — §16 補修は 8/31 実機済みだが、極小 run の捏造欠陥 ([隣人吸収 issue](../issues/arasuji_tiny_run_absorption.md)) を直してから豆粒削除→補修再実行。
- (発生待ち: 記憶補填の実地発火 — ログの目印 `memory backfill on beat death succeeded` を私が掃き続ける)

---

## 使い方

上から順でなくていい。触って緑だったら教えてくれれば、台帳と issue の消し込みは私がその場でやる。「実質済み」「v0.4 の向こう側」の節は、まはーが頷くだけで台帳から降ろす。
