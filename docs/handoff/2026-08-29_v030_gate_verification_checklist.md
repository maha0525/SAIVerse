# Handoff: v0.3 の門 — 実機検証チェックリスト (2026-08-29 の変更ぶん)

**書いた状況**: 2026-08-29。門の棚卸しから始まった一日で、§2-9 (編纂材料の一本化 + 付記撤去)、U 判定の材料字数化、§2-18/19+J (生成の段階の信号) の三束をコミットした。この一枚は、まはーが次にエリスを触るときに一晩で見て回れる検証の束。

**準備**: バックエンドを再起動する (コードが変わったため必須。Playbook の再取り込みは今日の変更には不要)。

---

## 1. 持ち越しの再検証 (8/25 の修理)

Memory 窓 → Chronicle → 「生成」。**一押しで採取から畳みまで通って完了メッセージが出る**こと。「見送りました」は原則もう出ない。

⚠️ **今日の U 変更で確認窓の文言が変わっている**: 畳める材料 (機構文の圧縮後で数えた字数) が U=10,000 に達するまで「あと約 N 文字の会話がたまれば畳めます」と出て、ボタンが押せない。これは**正しい挙動** — エリスの現状は生の超過はあっても材料が薄い可能性があり、その場合は少し会話してから。

## 2. 編纂材料の一本化 (§2-9) と U 級

会話がある程度たまってから「生成」を実行し、新しくできた一次あらすじを見る:

- [x] スペルの痕跡: 2026-08-29 確認 — 当日の編纂でスペル 2 本 (6,215 字 / 7,093 字 → 圧縮一行) と知覚 1 本 (345 字 → 原文) が材料に入り、本文にも Elyth の言及が織り込まれた。
- [x] 付記ブロックなし: 2026-08-29 確認。
- [x] 材料 1 万字級の被覆: 2026-08-29 確認 — coverage_chars=10,685 (検算一致: 会話生文 10,531 + 圧縮行 154)。生 24,500 字 → あらすじ 251 字。
- [x] 移行ノイズ: 2026-08-29 確認 — 当日できたあらすじは 1 本のみ、小粒の量産なし。

エリスの旧あらすじ 2 件に入った付記ブロックの掃除は、まはーの手が空いたときに (簡単に編集できると言ってた件)。

## 3. 生成の段階の信号 (§2-18/19 + J)

- [x] **通信切断からの復旧**: 2026-08-29 20:50 にソフィーで実機確認済み。**タブを閉じた場合は案内は出ない** — 問い合わせをするページごと死ぬため (当初この行に「タブを閉じる」と書いたのはメティスの誤り)。開き直せば履歴 + 5 秒ポーリングがサーバーの真実を映し、生成は読み手が居なくても走り切って保存される (status=saved をログで確認)。「発言は届いています」の自動問い合わせが出るのは**ページが生きたままストリームだけ切れた**場合 (例: 生成中に Next 中継を再起動)。この経路の実機確認は任意 — 問い合わせ口そのものは実データへの GET で正答を確認済み (found + has_reply)。
- [x] **再送の門番**: 2026-08-29 実機確認済み (まはー承認の直接 API テスト)。応答済みの発言への retry は生成前の検査で `409 already_replied`「この発言の後に既にペルソナの発言があります。」— 副作用ゼロ。ブラウザ操作で 409 に自然到達するのは難しい: 裏タブでもポーリングは止まらず (ブラウザが約 1 分に 1 回へ間引くだけ)、後述の印の鮮度が先にボタンを降ろすため。門番はレースの受け皿として残る。
- [x] **再送の印の鮮度** (2026-08-29 夜に表示側を追加実装): 同夜まはーが実機確認 — 一度出ていた再送ボタンが、後の応答で自分から消えた。
- [x] **続きの生成の印**: 2026-08-29 夜まはーが実機確認 — 中断された発言に「続きの生成」が出て、押した後はボタンが戻らなかった (読み手切断時の保存独立は §3-1 の status=saved で確認済み)。

## 4. 継続観察 (私の番)

- 記憶補填 (Beat 死亡時) の実地発火 — backend.log の「memory backfill on beat death succeeded」を私が掃く (2026-08-27 の件の残り)。

---

## 全部通ったら

門 §2 の 18・19 と §2-9 の行が消え、**残るは §4 の観察走行の束と、台帳の検証待ち行の消化だけ**になる。§9 は本日全行仕分け済み (空)。
