# 2026-09-10 引き継ぎ — v0.3.12 の束 (送る量の一系統化 + GPT-Image 2.5) 実装完了

まはーと次のメティスのための現在地。**実装とレビューは完了し、`hotfix/v0.3.12` に統合済み・全ブランチ push 済み・フルスイート 5,898 件緑**。次はまはーの実機検証。前の引き継ぎは [2026-09-09_v0311_shipped_and_v0312_seeds.md](2026-09-09_v0311_shipped_and_v0312_seeds.md)。

## ① 何が済んだか

- **送る量の一系統化と会話以外の提示内容の縮小** (v0.3.12 本丸、[intent](../intent/presented_context_reduction.md) — ステータス検証待ち)。設計から実装・レビュー収束まで 2026-09-09〜10 の一晩。中身は三つ:
  1. 会話以外の内容 (スペルやコア記憶の操作のお知らせ・現在地でない部屋の様子と画像) が Metabolism の瞬間に自動で縮む。台帳と確定文面は書き換えず、縮むのは提示だけ。省略があったことはペルソナに分かる形で示す。
  2. しきい値は一系統 (会話を残す量 / 送る合計の上限) だけになり、知覚側のしきい値・二系統にまたがる保存時検査・余裕 10,000 は廃止。稟乃さんが踏んだ「20,000 で保存不可・19,999 で可」は問いごと消滅。
  3. 組み込み既定を 20,000/60,000 へ変更し、画面はプリセット選択 (多い 40,000/120,000・デフォルト・少ない 10,000/30,000・カスタム) + 注意書き。「デフォルト」は数字を書き込まず未設定に戻す (将来の既定変更に追従)。
- **GPT-Image 2.5 対応** (flare / sunburst) — 別セッションが `feature/gpt-image-2.5` で実装。
- v0.3.10 からの持ち越し 3 件 (知覚の既定値・余裕 10,000・null の素通し) はこの設計で解消。残る持ち越しはアイテム個数上限 (未着手、[room_items_uncapped](../issues/room_items_uncapped.md))。

## ② ブランチの地図

- `hotfix/v0.3.12` — **統合ブランチ。実機検証はここに切り替える。**追加の実装物もここに積む (まはー指示)。
- `feature/presented-context-reduction` / `feature/gpt-image-2.5` — 統合済みの元ブランチ。
- `develop` — intent 起草までの docs コミットを push 済み。

## ③ 実機検証の見どころ (まはー)

1. 全体設定「ペルソナに送る量」— プリセット 4 択と注意書き。保存が普通に通ること。
2. 会話を続けて整理が走った後、送信量の内訳で「部屋の様子」が減ること。よその部屋を回って戻ると全文と画像がもう一度全部表示されること。
3. 「少ない」プリセットで整理が走る頻度の体感 (注意書きの文面が実感と合うか)。

## ④ レビューの記録

消し込み 5 巡: ローカル LLM 3 本 (採用 1 + 文言 1) → Codex 一巡目 (採用 3 / 棄却 3、全て裏取りつき) → 代役レビュー二巡目 (Codex が使えない間、Claude のサブエージェントが務める形。採用 6) → Codex 三巡目 (クォータ切れで途絶、倒れ際の手がかり 1 件を検算して採用) → 代役レビューの最終回 (採用 3、ブロッカーなし判定)。大物の捕獲は三つ:

- 縮みに失敗したとき、例外処理が知覚を丸ごと消す側に倒れていた流れ。縮めずに全文で見せる側へ直した。
- **操作のお知らせを提示から下ろす境界がペルソナ全体で一つなのに、head の描き直しはモデルごと**という非対称 — 別モデルのセッションが変更を知る手段を失う。境界をモデルごとの専用の表に分けて解消。
- head の一部だけ新しく描き直せなかった失敗 (古い値の据え置き) を「描き直せた」と読む穴。お知らせの発生源 4 セクションが全部新しく描き直せた回だけ下ろす形に。

**Codex は 2 回ともクォータ切れで途絶** (謎の復活と再消滅 — OpenAI 側の不具合の気配、まはー観察)。安定して戻ったあとに本物の一巡を追加でかけるかは、まはーの判断待ち。

## ⑤ 未処理・注意

- **リリースノートの約束** (release_history に記録済み): 既定を小さくしたので、設定を触っていない全ユーザーの畳み方が変わる。「前の感覚に戻すなら多いを選ぶ」を明記する。
- **GPT-Image 側の検収の深さ**: メティスはマージ前に差分の概観 (規模と対象ファイルの健全性) だけ確認した。深い検収は未実施 — まはーの実機試しが実質の検収になる。
- 供給側の文量 (スペル結果の長さ等) は後回し裁定済み ([issue](../issues/nonconversation_supply_volume.md))。システムプロンプトのスペル一覧 1 万字超は [head_prompt_followups](../issues/head_prompt_followups.md) に再提起を追記済み。
- スタイル構想 (キャッシュの使い方別の方針プリセット) は ideas 帳の記憶の節に記録済み。

## ⑥ 語彙の裁定 (2026-09-09、文書を書く前に必読)

まはー裁定: **「水位」「退役」「割れ」「痕跡」「跡地」「印」「一行」「版」を新しい文書で使わない。**repo の既存文書で流通していることは使用許可の根拠にならない (現状の repo の方が間違っていて、掃除が終わっていないだけ)。ホワイトリスト入りは「素通し」のみ。言い換えは名詞の置き換えでなく動詞で流す (「省略があったことが分かるように示す」)。詳細と経緯は memory (`feedback_claudish_vocabulary_is_not_ratified` ほか) に記録済み。

## ⑦ 経緯の一言

発端は 2026-09-09 のユーザー実害 3 件 (保存できない画面・トークン倍増・件数から文字数への戸惑い)。「どうすりゃいい？」から絞り込みの議論を経て、「重要なものだけ残す発想が根っこから違う」「機械的な合図で縮める」「しきい値は一系統」という設計に着地し、同日中に裁定・実装・レビュー収束まで走った。設計の正本は intent、進行状態は in_flight 台帳。
