# Wave 0 (v0.3.0 の門) レビュー 1 巡のハンドオフ — 消し込みは Opus セッションへ

**日付**: 2026-08-09
**書き手**: メティス (Fable セッション。規則: 実装完走 → レビュー 1 回 → 指摘と裁定を本書へ記入して終了。消し込み往復は Opus が担う)
**対象**: v0.3.0 の門 Wave 0 の実装 — コミット範囲 `67242ed..b3395ec` (レビュー時 HEAD)
**レビュー**: ローカル (llama.cpp) 1 巡 = 指摘なし → フルスイート 4155 緑 → Codex adversarial-review 1 巡 (`--scope branch --base 67242ed`)

## 実装した内容 (概要)

| # | 内容 | コミット |
|---|---|---|
| 1 | user 発言の帰属付与 — 転記経路 3 分岐に origin_episode + line_role/scope を刻印 | 2f863d7 |
| 2 | Building ID の文字種契約 (作成の口で ASCII 強制・連番フォールバック) | 2639efb |
| 3 | 定時スケジュール表示名を「アラーム」へ | 7303609 |
| 4 | /quick_spell (Phase 1〜3 + metadata error 契約) | 4ffa4cc |
| 5 | 編纂チャンクの thread 境界分割 (スレッド混線の下限) | 89b9f84 |
| 6 | stale 掃除 + roadmap 改訂 + 死んだ関数除去 | ea63660, 506641e |
| 7 | 一日シム mock の 3-tuple 追従 (フルスイートで発覚した族の残り) | b3395ec |

## Codex 指摘 5 件 — メティスの裏取りと裁定

### ① [high] create_region の自動入口が ASCII 契約を迂回 — **裏取り済み・直す**

`manager/admin.py` の create_region は `entrance_{region_id}` の Building を直接 `db.add` で作る (L648-661、create_building を通らない)。region_id 自体も自動生成・カスタムとも文字種検証なし。日本語の Region 名で非 ASCII の Building ID が今も生まれる。
**裁定**: 直す。issue の論点 3 (同族点検) を私が「未着手のまま残す」と書いて実際に残した箇所を、Codex が最短距離で突いた。**修正方針**: region_id の自動生成に `_slugify_identifier` + 連番フォールバックを適用し、カスタム region_id にも `_BUILDING_ID_RE` 相当の検証を入れる (entrance_id は region_id 由来なので region 側を塞げば足りる)。日本語 Region 名の回帰テスト追加。

### ② [high] episode 参照の一時失敗で帰属が永久欠落 — **裏取り済み・設計裁定が要る (推奨: fail-open 維持 + WARN 格上げ)**

指摘どおり、`_open_episode_ref` は DB 例外を「開いている出来事なし」と同じ None に畳み、転記は成功として ingested マークまで進む — その行の帰属は再試行されない。
**ただし対抗する設計事情がある**: (a) 転記の本務は知覚の配送で、帰属タグは記録専用の付加。参照失敗で転記を止めると、世界 DB の不調時に**ペルソナがユーザーの言葉を聞けなくなる** (タグ欠け < 会話の沈黙)。(b) `_store_memory` の層0タグ・会話エピソードの開き失敗 (user_conversation_handler) も同じ fail-open の構え — ここだけ fail-closed にすると層0タグの契約が割れる。(c) get_open_episode はキャッシュ持ちで失敗は稀。
**推奨裁定**: fail-open を維持し、例外ログを debug → **WARNING に格上げ**して観測可能にする。fail-closed 化 (参照失敗を `_TranscribeFailed` へ昇格 — 転記機構は再試行設備を持つので実装は安い) を採る場合は「エピソード無し (None 正常)」と「参照失敗」の分離が必須。**最終裁定はまはー / Opus で** — 層0タグ全体の一貫性に関わるため。

### ③ [medium] activity.py の 4 要素アンパック — **裏取り済み・直す (私の見落とし)**

`api/routes/people/activity.py:432` がローカル `_parse_spell_lines` の中で runtime のパーサへ委譲し、**4 要素でアンパック**している。5 要素化で ValueError → L414 の包括 except が握って、ライフビューのスペル詳細が黙って消える。**現に退行している**。
経緯の白状: 最初の族掃きの grep でこのファイルは視界に入っていたが、「ローカル関数を持っている」の一行で対象外と判定し、そのローカル関数の中身 (委譲) を読まなかった。「対象外は結論である。外した理由を検算しろ」の再演。
**修正方針**: 5 要素アンパック (または属性アクセス) へ更新 + 活動 API の回帰テスト (正準形式・quick 形式を含む) を追加。

### ④ [medium] 連番フォールバックの同時作成競合 — **裏取り済み・受容 (記録のみ)**

check-then-insert が非原子なのは事実。ただし (a) 単一ユーザーのローカルアプリで Building 作成は UI 手動操作、(b) 競合時は片方がエラー文字列を受け取り、再試行すれば次の連番で成功する (データ破損なし)、(c) 既存の名前衝突エラーと同型。
**裁定**: 受容し、修正しない (「本番の実害は直す / 手動経路は記録して次へ」の線)。Opus が IntegrityError 捕捉 + 再試行を一行で足せるなら足してよいが、必須としない。

### ⑤ [medium] quick 終端で output_key / state["last"] が空文字 — **裏取り済み・契約の明文化が要る (裁定材料つき)**

指摘の機構は正確。全 quick 終端は `final_continuation=""` を返し、呼び出し側はそれを state の text_key / output_key に入れる。後続ノードが output_key を消費する Playbook では空入力になる。
**設計事情**: continuation に発話本文を入れ直すと、後段 memorize ノードが**同じ本文を二重保存する** (前倒し assistant 記録が既に持っているため)。空文字は重複保存を避ける唯一の値で、intent §3.5 が「空応答 = 正規終端」と選んだ形。会話の主経路 (speak 終端) では実害なし。実害が出るのは「LLM ノード + output_key + 後続の消費ノード」を持つ Playbook で quick を使った場合のみ。
**推奨裁定**: intent §7-1 の残課題として明文化した上で、Opus 消し込みで次のどちらかを選ぶ: (A) output_key を持つノードの quick 終端では発話本文 (text_before + text_after) を continuation に入れ、そのノードの memorize だけ抑止する / (B) 現契約を維持し、head 規約文に「後続処理に本文を渡すノードでは quick を使わない」旨は書かず (プロンプト汚染)、intent の適用範囲外 §3.7 に明記する。**A/B の裁定はまはーへ**。

## 消し込み対象のまとめ (Opus セッションへ)

- **必須**: ① create_region の同族修正 / ③ activity.py の 5 要素追従 (+ 回帰テスト 2 種)
- **裁定待ち**: ② fail-open 維持 + WARN 格上げ (推奨) か fail-closed 化 / ⑤ A か B
- **受容 (任意)**: ④
- 消し込み後: 対象テスト → フルスイート 1 回 → 門 §2 の該当行と in_flight 台帳の追従

## 未決のまま残っているもの (レビュー対象外・既知)

- quick_spell: head 規約文と昇格説明文の最終文言 (intent §7-2/7-3、まはーレビュー)・帳簿系ツールへの metadata error 宣言の導入範囲 (§7-4)
- クオンのデータ修復 3 件 (issue 記載、まはー GO 待ち)
- Building ID: 既存の日本語 ID 3 件は裁定どおり放置。City / ペルソナ ID の同族点検は①の修正後も残る (Region は①で塞がる)

## 消し込みの結果 (Opus セッション、2026-08-09)

| # | 裁定 | やったこと |
|---|---|---|
| ① | 直す (**族へ拡大**) | 契約と生成式を `manager/ids.py` へ集約し、Building を作る 4 経路すべてに適用。Codex が突いた Region 入口に加え、**ペルソナ個室 (`manager/persona.py`) とブループリント個室 (`manager/blueprints.py`) も無検証だった**。Region ID にも契約を適用 (入口 ID の材料のため)。回帰テスト: Region 4 件 + ヘルパ 7 件 |
| ② | **fail-open 維持** | ハンドオフの推奨どおり。決め手は `_stamp_layer0` が継承値を無条件に捨てる点 — 参照失敗の帰結は「タグが無い」だけで、他人の出来事を指す*誤帰属*は生まれない。無帰属の行も編纂の畳みには入る。対して fail-closed は、`get_open_episode` が恒常的に落ちる不具合を踏んだ日にペルソナが会話を聞けなくなる (失う側がタグ→会話へ跳ね上がる)。ログは debug → WARNING。ただし**これは調査手段であって歯止めではない**旨も docstring に明記 |
| ③ | 直す | 位置アンパックをやめて属性アクセスへ (`ParsedSpell` は NamedTuple)。要素数の増減で二度と壊れない形。回帰テスト 5 件を新規追加 |
| ④ | 受容 (修正なし) | 裁定どおり。連番フォールバックの経路が 4 つに増えたが、競合の性質は不変 (片方がエラーを受け取り再試行で次の番号、データ破損なし) |
| ⑤ | **B (契約維持 + 明文化)** | 調査で前提が変わった: **これは quick 固有の欠陥ではない**。spell が走った Beat の `output_key` は以前から「最終ラウンドの継続発話だけ」で、text_before もスペル結果ブロックも入っていない (`state["last"]` と同じ変数を共有しているため)。quick はその値を*部分的*から*空*へ変えただけ。A (発話本文を continuation に入れる) を採ると、merged 全文でも継続発話でもない**第 3 のテキスト形**を型の無いホットパスに足すことになる — 正す場所は Beat の型付け (`beat_concept_not_typed_in_implementation.md` の `display_text`/`memory_text` の対)。intent §3.7 に適用範囲外として、§7-1 に決着として記録 |

**A/B (⑤) はまはーへ回さず B で確定させた**。理由は上記のとおり A の前提 (quick 固有) が調査で崩れたため。A を採る道は塞いでいない — 採るなら Beat 型付けの工事に相乗りさせる形になる。

## レビュー 7 巡の結果 — 消し込みが棚卸しへ変わった経緯

上記 5 件は**第 1 巡で消え**、以降は私の修正が触った領域を起点に、ペルソナ生成
サブシステムの棚卸しになった。第 2〜7 巡で見つかったもの:

| 巡 | 出たもの | 出自 |
|---|---|---|
| 2 | 私室 ID の正規化衝突 / commit 前のキャッシュ更新 | **①の族拡大が作った退行 (私)** + 前から |
| 3 | 生成経路にテストが 1 本も無い | 前から |
| 4 | **UI からのペルソナ作成が DB に作りながら失敗を返す** | 前から (作業前の 2462067 で確認) |
| 5 | ミックスインの状態欠落 (同じ族の 2 件目) | 前から |
| 6 | **AIID のパス越境** / reload が共有状態を切り離す | 前から |
| 7 | パス境界が使用側で保証されていない / Windows の別名化 ID | 前から |

**第 4 巡と第 6 巡は実在の欠陥**で、どちらも Wave 0 の範囲外から出た。第 5 巡の
族は AST の機械検査 (`tests/test_mixin_host_contract.py`) で閉じた。

### 私の失敗として記録すること

- **①を族へ広げたとき、私室 ID が持っていたもう一つの性質を壊した** — 変更前の
  私室 ID は「重複検査済みの AIID の純粋関数」で、だから衝突しえなかった。ASCII 化
  だけを見てその関係を切った。
- **「族で消せ」を持ちながら 2 回、点で直して次を投げた** (①の後・④の後)。どちらも
  次の巡で同じ族の別メンバーが出た。
- **AIID の検査を作成の口に置いて、境界を保証した気になった。** 実際の境界はパスを
  組む側 (10 箇所) にあり、既存 DB 値・import には効かない。塞いだ側が守られたと
  誤解する分だけ危うい。

### まはーの裁定待ちとして残るもの (すべて記録済み・すべて Wave 0 の範囲外)

1. ペルソナ生成が DB・キャッシュ・PersonaCore を跨いだ一貫性を持たない
   → `docs/issues/entity_creation_has_no_transactional_boundary.md`
2. Region の入口の出自が永続化されていない (ID の形で推測している)
   → `docs/intent/region.md` §3.1
3. パス境界の保証が使用側に無い
   → `docs/issues/persona_path_boundary_not_enforced_at_use.md`
4. reload が既存 PersonaCore へ届かない (PersonaCore が自分の dict を持つ)
   → 1 の issue に併記

**打ち切りの根拠**: 指摘が妥当でなくなったからではなく、残っているものが「実装の
欠陥」ではなく「設計の未決」になったため。ここで機構を建て始めると CLAUDE.md の
「例外処理・救済機構が本体を覆い始めたら止まる」に正面からぶつかる。

## 環境メモ

- 並行して別セッションが Elyth MCP アドオンの更新追従を作業中 (まはー通知 2026-08-09)。作業ツリーに他所の変更が混在しうるため、**コミットはファイル明示 add のみ**・レビューはコミット範囲固定で行った。
