# 2026-09-06 引き継ぎ: 部屋の様子の実機検証 — 主要な旅は本番で通過、残りは 4 件

**書き手**: メティス (2026-09-06 の実機検証セッション。Fable 5.1 → セーフガードで Opus へ切り替わり中断)。

## 次のセッションの最初の仕事

**→ 解決済み (同日中、この文書を書いた直後のセッション継続で)。** まはーの裁定は「並びの調整」より深い形になった: 役割・指示は識別情報ではないから重複させない → 調査で指示ラベルと束の `building:prompt` が同じフィールドの複製と確定 → ラベルを退役して束に一本化し、様子は回収が組成の末尾へ寄せる (出来事は到着順)。実装・検収・intent §11-3 改訂まで完了。詳細は [intent](../intent/room_state_packages.md) §11-2 / §11-3 と経緯の最終行。以下の「まはー待ちの一件」は当時の記録のまま残す。

## 現在地 (一枚)

- v0.3.9 の部屋の様子の再設計は、**まはーの実機検証で 8 件の欠陥が見つかり、全部直して本番で通った**。最終確認の実機出力: 8 往復の堆積が経路一行 (309 トークン) に畳まれ、Close→Open が「(開かれた)」+ リンク + 絵で出て、説明の再掲はゼロ。
- コミット 7 本を hotfix/v0.3.9 に push 済み (`0c91de12`〜`98947cbb`)。作業ツリーは clean。フルスイート **5,674 passed / 7 skipped**。
- [PR #282](https://github.com/maha0525/SAIVerse/pull/282) は draft のまま。説明は §11 と門の修正まで反映済み (一本化のぶんは未反映)。

## 今日直した 8 件 (すべて実機発見)

| # | 欠陥 | コミット |
|---|---|---|
| 1 | 往復のたびに部屋の全文が未消費に堆積 (5 万字級) | `907dc24c` |
| 2 | 「様子 → 通知 → 指示」の逆順 (配送機構の非対称) | `907dc24c` |
| 3 | 旧実装が積んだ壊れた差分の残留 | `907dc24c` |
| 4 | 再会想起の門が実形 (metadata.with) を見ておらず、ほぼ常に素通し | `7f0eb0ec` |
| 5 | 置き直しの門が回収で落ちる遺物を「運ばれる」と数え、移行後の初回 Pulse が部屋なし | `b1911343` |
| 6 | プレビューで提示済みの知覚が無印で history に埋没 | `7a1f9e00` |
| 7 | Close→Open が全文再掲 + 出来事が見えない | `a4a2254e` |
| 8 | 回収×開き直しの合流で全文が二枚並ぶ (保障 2 違反) | `98947cbb` |

8 の修正は設計の引き戻しになった: **描画は消費の組成の一回だけ、土台は常に提示の末尾** (§4 冒頭・§11-2 規則 2)。これで `ensure_room_state_base` が退役した。

## まはー待ちの一件 (次の作業)

**組成の並びを「出来事 → 指示 → 様子」に一般化するか。**

- 発端: エリスの入室で、部屋の差分 ([エリスの外見]) が入室通知より前に出た。因果が逆に読める。
- 私の案: 出来事 (経路一行・入室通知など) は到着順のまま、**指示と様子は状態なので常に組成の末尾へ**。一本化で様子の中身が「いま時点の集約」になったので、時系列の途中に挟まる方が意味に合わない。
- 未確定: これを**出来事が無い普通の入室でも同じ規則にするか** (= §11-3 の「通知 → 指示 → 様子」の裁定を、指示と様子を末尾に寄せる形へ更新するか)。私は一貫性のため常に同じ規則を推す。

## 残っている案件 (急ぎなし)

1. **③ 出来事の区切り** ([issue](../issues/archive/perception_event_boundaries_unclear.md)) — 上の並びの裁定が入ってから、残る区切り問題の実物を見て印の形を決める。→ 一出来事一ラベルで解決し、2026-09-07 に実機確認済み (issue は archive へ)。
2. **⑤ アイテム個数上限** ([issue](../issues/room_items_uncapped.md)) — 設計の問い 3 点を揃えてまはーの裁定へ。アイフィの部屋は 58 個・35,000 字。
3. **テリスの入室知覚欠落** ([issue](../issues/entry_perception_missing_for_fresh_persona.md)) — 未 ready の黙殺を配達の門で塞いだので、症状が変わっている可能性。再現から。
4. **入室通知の配達が数十秒遅れる** — 実測 38 秒。配達簿が人ごとの列で、他人の移動が積んだ分は次の回復 tick まで待つ。**issue 未起票** (次セッションで起票するか、④として直すかの判断が要る)。
5. [outbox_delivery_gate_siblings.md](../issues/outbox_delivery_gate_siblings.md) の同族 4 点 / [ingested_user_utterances_lack_speaker_id.md](../issues/ingested_user_utterances_lack_speaker_id.md) — どちらも実害待ち。
6. PR #282 のまはー確認 → マージ GO → タグ v0.3.9 → 発行 (発行作業はメティス、マージはまはー確認後)。

## このセッションで確立した規律 (memory 反映済み)

- **[検収は「出力を読む」まで](../../..)** (`feedback_acceptance_means_reading_the_output`): 実機で一瞬で分かる欠陥が 8 件も出た共通項は、私が一度も出力を読者として読んでいなかったこと。実装が上がったら隔離環境で実使用の場面の列 (往復・開閉など) を実パイプラインに通して文面を読む。設計の規則表には各行の出力例を添える。「冗長は出るが」式の受容は頻度を言えるまでしない。
- **順序の徹底** (同上 §4): 隔離環境で実形のデータを使い私の目で確認 → その後で本番環境の確認へ。本番に触るのは読み取り専用の検視のみ。
- **[まはーが言った事実を落とすな](../../..)** (`feedback_check_live_process_before_claiming_its_state`): 再起動の報告を記憶で上書きした事故。プロセスの状態は、まはーの発話 > 私の記憶。実測 (logs の先頭 vs コミット時刻) は控えの網。

## レビューの運用 (今日の実績)

- Codex (Luna/high) と ローカル (Qwopus、NEBULA) の**並走**を初めて実施。同じ差分に対し上位 2 指摘が同着で、独立した裏取りとして機能した。Qwopus は 2 分 20 秒・無料・誤指摘ゼロで、「不明」の欄に「何を渡せば結論が出るか」まで書いてきた。**重い巡は Codex、毎回の一次と相互裏取りは Qwopus** の二段が実戦で使える。
- 検収スクリプトの雛形: `scratchpad/read_script_room_journey.py` (テストの実形 fixture を借りて、入室 → 往復 → Close → Open → 変化なしの場面を実パイプラインに通し、ペルソナが読む文面を印字する — その出力を読者として読むのが検収の最終工程)。次も使えるので、必要なら `~/.claude/` 側へ移して保管する。
