# 反射判断 — 型付きの質問で確率を返す判断層と、答える側の差し替え

**ステータス: 検証待ち (2026-09-19 設計確定 — まはーの裁定は全件済み。第 1 段は
2026-09-20 に実装・実機合格・develop へマージ済み (PR #309)。第 2 段 (通常 LLM への
変換層・ペルソナ個別の上書き・choice / score) も 2026-09-20 に実装し、PR #310 と
追補の #314 が 2026-09-21 にマージされた。両段とも v0.3.14 に載って発行済みで、
既定は OFF。まはーの実機評価を待っている)**

## これは何か

**反射判断** (まはー命名、2026-09-19) は、「状況と、基準付きの型付き質問を渡すと、
文章ではなく確率・選択・数値だけが返る」判断の層。考えて文章を組み立てる判断
(通常の LLM 呼び出し) より速く・安く・形が保証される代わりに、自由記述はできない
— 反射で答える判断だけを担う。

Jev の実験 (docs/intent/auto_recall_jev_rerank.md) で最初の利用者 (自動想起の
選別) が動いた。この intent はそれを二方向に一般化する:

1. **答える側を差し替えられるようにする** — TypeSafe 公式の Jev、Jev の API と
   同じ作法で話せる互換の宛先 (OpenRouter・セルフホストなど)、手持ちの普通の
   軽量モデル (構造化出力で確率を返させる) のどれでもよい。
2. **想起以外でも使えるようにする** — 自由記述が要らない構造化判断 (ツール選択・
   自律判断など) の全般が、同じ質問の書き方・同じ失敗時の扱いで反射判断を呼べる。

まはーの依頼 (2026-09-19): 軽量モデルへの差し替え / 互換の宛先への差し替え /
構造化出力案件全般への一般化 / **互換勢で共用できるモデル設定ファイルの形が
作れるならそれを使う** / 質問の型は全部対応 / ペルソナ個別のモデル設定も可能に。

## 互換勢の調査 (2026-09-19 実測)

「Jev 互換」を名乗る提供元は増えているが、形は揃っていない。確認した 4 つ:

| 提供元 | 宛先の path | リクエストの中身 | 質問の型 | 認証 |
|---|---|---|---|---|
| TypeSafe 公式 | `/v1/systemone` | 正典 (state + questions) | noul / choice / score | TypeSafe のキー |
| [OpenRouter](https://openrouter.ai/typesafe/jev-1.13) | `/api/alpha/decisions` (alpha) | 正典とほぼ同一 | 同上 | OpenRouter のキー |
| [localjev](https://github.com/githubnext/localjev) (セルフホスト) | `/v1/systemone` | 正典と互換 | 3 型すべて | 任意 (Bearer、無しも可) |
| [simple-jev (Featherless)](https://simple-jev.featherless.ai/) | `/v1/classifier` | 中身は近似・応答の欄が異なる | choice のみ確認 | Featherless のキー ($0.03/1M 入力) |

ほかに [vLLM に System One 形式の判定モードを足す PR](https://github.com/vllm-project/vllm/pull/57250)
が open (DiffusionGemma 系。例示サーバーは `/v1/systemone` 互換)。

**結論: リクエストの中身 (state + 型付き質問の辞書) は事実上の共通方言。
違うのは宛先の path・認証・対応する型・応答の欄の名前で、これらは全部
「設定ファイルに書ける差」** — まはーの読みどおり、共用のモデル設定ファイルの
形に乗せられる。

## 設計

### 1. 答える側は「反射判断の役割に割り当てたモデル」で決まる — 専用の切り替え設定は作らない

SAIVerse には既にモデル設定ファイルの仕組みがある (3 層読み込み、provider_ref、
モデルの役割の一元表、使用量と費用は設定キーに帰属、キーは宛先とペアで宣言)。
反射判断はこの仕組みに**役割として乗る**:

- モデルの役割の一元表 (`saiverse/model_defaults.py` の `MODEL_ROLES`) に
  **「反射判断」の役割を足す** (7 個目)。世界の既定は env
  (`SAIVERSE_REFLEX_JUDGMENT_MODEL` 案)、**ペルソナ個別の上書きも可能にする**
  (まはー裁定 — `DEFAULT_MODEL` / `LIGHTWEIGHT_MODEL` と同じ形の列を追加)。
- 割り当てたモデルの設定ファイルのプロトコルが答える側を決める:
  - **jev 互換プロトコル** (新設する provider type、他の native 系と同じく
    組み込み限定) → System One の形をそのまま送る。宛先の path・応答の欄の
    方言・対応する型は provider/モデル設定ファイルの欄で宣言する。
  - **通常の LLM プロトコル** (既存の openai_compat / gemini_native など) →
    反射判断の層が質問をプロンプトへ変換し、構造化出力で答えさせる。
- **これで「答える側の切り替え」という専用の設定は不要になる** — どのモデル
  設定を役割に割り当てるかが、そのまま答えの出どころの選択になる。初稿に
  あった `SAIVERSE_JUDGMENT_BACKEND` / `TYPESAFE_API_URL` / `TYPESAFE_MODEL`
  の env 案は全部この形に吸収されて消える。

組み込みのモデル設定ファイルとして最初に同梱する候補: TypeSafe 公式
(`jev-latest`、キーは `TYPESAFE_API_KEY`、単価 $0.042/1M 入力)、OpenRouter 経由
(`typesafe/jev-latest`、キーは OpenRouter の既存キー)、localjev (ローカル宛先、
キー任意)。

### 2. この形が初稿の裁定ポイント 3 つをまとめて解消する

- **費用の帰属** (まはー裁定「A も B も LLM のモデルと同じように」): モデル設定
  ファイルに単価を書けば、既存の使用量の記帳がそのまま効く — 設定キー名義、
  画面の表示、答える側を替えても費用が同じ場所に見える。
- **キーと宛先のペア**: モデル設定ファイルは宛先 (base_url) とキーの env 名
  (api_key_env) を対で宣言する既存の形に乗る。「公式のキーが第三者の宛先へ
  流れる」事故は、既存の provider 照合の裁定 (信頼は宛先とキーの組で引き継ぐ)
  がそのまま守る。専用の判定ロジックを新設しない。**守らせるには呼ぶ必要がある** —
  答える側を解決するとき、通常の会話クライアント (`llm_clients/factory.py`) が接続を
  作る前に通すのと同じ `validate_model_config_connection` を通す (2026-09-20 の 6 巡目)。
- **設定ミスの見える化**: 役割の一元表に載るので、定義の無いモデル名を保存
  しない検査・画面の警告・プリセットが既存の仕組みのまま効く。その検査は
  「その役割で使えない宛先も保存しない」まで広げてある — jev 互換の宛先は型付きの
  質問に確率で答えるだけで、通常の会話クライアント (`llm_clients/factory.py`) は
  この protocol を知らない。標準・軽量・Memory Weave・画像/音声/動画の要約の
  各役割に jev 互換のモデルを保存できてしまうと、割り当てられたペルソナは話そうと
  した時点で失敗する — 会話の止まる設定を画面から作れないよう、保存の時点で断る
  (判定は `saiverse/model_defaults.py` の `role_model_save_rejection` 一本で、
  ペルソナ設定の保存・グローバル設定のモデルロールの保存・チャット画面のモデル
  一時上書きの三つの入口が同じものを引く)。**選択肢にも出さない** (2026-09-20 追加) —
  選んでから断られるより、最初から出てこないほうが正しい。モデルの一覧を返す API
  (`/api/info/models`・`/api/config/models`・`/api/tutorial/available-models`) が
  `is_reflex_only_model` の印を各モデルに載せ、会話に使う選択欄 (ペルソナ設定・
  世界エディタ・グローバル設定とチュートリアルの役割ごとの選択・チャットの一時上書き)
  がその印の付いたものを外す。印は保存の関所と同じ判定なので、「選べるのに保存だけ
  断られる」ずれは生まれない。一覧そのものからは落とさない — モデル管理画面
  (編集・削除) と反射判断の役割の選択欄には出し続ける必要がある。逆向き — 反射判断の役割への割り当て —
  は第 2 段からどちらの種別も合法なので、宛先の種類を理由に保存を断るものが無い。
  反射判断の役割で断るのは定義の無い名前だけ。**どちらの答える側としても解決できない
  設定** (jev 互換なのに宛先の宣言が無い・投げる型に答えない・キーと宛先の組が照合に
  通らない) は、保存を通したうえで画面の警告が知らせる — この検査は実行側の宛先解決と
  同じ関数を呼ぶので、第 2 段で変換層が入った時点で自動的に緩んだ (通常の LLM は通り、
  壊れた設定だけが引っかかり続ける)。

### 3. 質問の形 — System One の 3 型すべてを最初から作る (まはー裁定)

Noul (はい/いいえの確率)・Choice (選択肢と各確率)・Score (数値) の 3 型を
共通語とする (まはー「はい/いいえだけでは真価を発揮できない」)。qid (質問ごとの
ID) に `{type, instructions, criteria}` を添える形。

- jev 互換の答える側へはそのまま送る (方言の差は設定ファイルの宣言で吸収)。
- 通常の LLM へは型ごとの変換 (構造化出力のスキーマは配列で受けて辞書に戻す —
  Gemini は additionalProperties を使えず、qid が動的な辞書スキーマを組めない)。
- 答える側が対応しない型 (例: simple-jev は choice のみ) を含む質問は
  「使えなかった」— 部分回答を採用しない既存の厳格さのまま。

### 4. 変えないもの

- **失敗時の扱いは呼び出した機能の側が持つ**。反射判断の層は「使えなかった」を
  一種類の例外で返すだけで、そのターンをどう凌ぐか (自動想起なら従来方式へ
  戻る) は各機能の設計のまま。実行時の障害は WARNING ログ + フォールバック、
  設定ミスは画面で知らせる (§2)。
- 会話の返事を待たせる場所で使う前提の作り — 同時本数の上限 (プロセス全体で
  4 本、答える側で変えない)・実際の経過時間で打ち切る締切・キーの伏せ字・
  全 qid の揃わない応答の不成立 — は Jev の実験で実証済みの形 (旧
  `saiverse/typesafe_client.py`) を共通層 `saiverse/reflex_judgment.py` に引き継ぐ。
- 自動想起の選別の ON/OFF は、実験の env (`SAIVERSE_AUTO_RECALL_JEV`) を廃止し、
  **ペルソナごとの「自動想起を強化する」スイッチ** (まはー裁定 2026-09-19 —
  「環境変数での設定はしんどい」) に置き換える。DB の AI テーブルの列 + ペルソナ
  設定画面のトグル (`AUTONOMY_ENABLED` と同じ形)。既定 OFF。
  ON にしても、反射判断の役割にモデルが割り当てられていなければ判定は走らない
  (= 「キーが無ければ OFF」の既存の約束は「役割にモデルを割り当てる」という
  明示の行為に置き換わり、黙って費用が発生する経路は残らない)。

### 5. 観測と品質比較

- 判定ログの形は維持し、**どのモデル設定が答えたか**をログに載せる。
- 同じ場面を答える側だけ替えて再現する検証 (隔離環境または読み取り専用の合成
  再現。実 API の課金はその API の実費で、実行は従来どおりの承認の範囲) で、
  Jev と軽量モデルと互換勢の確率を並べて比較できる。

## §6 確定した裁定 (2026-09-19、まはー)

1. **名称**: 日本語「反射判断」、英字 `reflex_judgment` (コード・ログ・役割名)。
2. **スイッチ**: 実験の env を廃止し、ペルソナごとの「自動想起を強化する」
   スイッチへ (§4)。
3. **同梱する設定ファイル**: TypeSafe 公式 + OpenRouter + localjev の 3 つ。
   simple-jev は初期対応に含めない (型が揃わず、想起は noul を使うので初期の
   利用者が居ない。方言の宣言欄だけ設計に残す)。
4. **実装の段割り** (流れはメティス一任 → 2 段で確定):
   - **第 1 段**: 反射判断の層 (typesafe_client の器を引き継ぐ共通層) +
     jev 互換 provider type + モデルの役割「反射判断」(世界既定) +
     ペルソナごとの「自動想起を強化する」スイッチ (DB 列 + 設定画面のトグル) +
     自動想起の載せ替え (現行の実験と等価な判定を、カタログ + スイッチの形で
     再現)。env `SAIVERSE_AUTO_RECALL_JEV` と関連 env はここで廃止。
   - **第 2 段**: 通常 LLM への変換層 (軽量モデルで判定) + ペルソナ個別の
     モデル上書き + choice / score の型。
   - 段の間に実機確認を挟む (崩れたときの切り分けのため)。

## 経緯

- 2026-09-19: Jev 実験の実機評価と並行して、まはーが一般化を要望。初稿を起草し、
  言葉づかい・整合性の検査を実施 (env ベースの初稿の主な取り込み: 費用が黙って
  発生しない約束・モデル指定の役割一元表への登録・公式キーを第三者の宛先へ
  流さない・例外名の別名を残さない・費用の帰属を 3 種すべてで考える)。
- 2026-09-19: まはーの裁定 5 件 — 名称は「反射判断」/ 費用は互換勢もモデルと
  同じ扱いに / 互換勢で共用できるモデル設定ファイルの形が作れるならそれを使う /
  質問の型は全部対応 / ペルソナ個別設定も可能に。互換勢 4 つを調査して
  「中身は共通方言、差は設定ファイルに書ける範囲」を確認し、env 切り替え案を
  カタログ (モデル設定ファイル + 役割) 方式へ全面改訂。初稿の裁定ポイント 7 件の
  うち 4 件 (費用・キー・型・共通語の射程) はこの改訂で解消。
- 2026-09-19: 残りの裁定も確定 (§6) — 英字表記 OK / スイッチはペルソナごとの
  「自動想起を強化する」へ (env はしんどい、の裁定) / 同梱 3 つ OK / 段割りは
  一任 → 2 段。ステータスを実装待ちに。
- 2026-09-20: 第 1 段を実装。共通層は `saiverse/reflex_judgment.py`
  (`saiverse/typesafe_client.py` は置き換えて廃止、旧経路は残していない)。
  provider の protocol `jev_compat` を新設し、宛先の path・応答の欄の名前・
  記帳する使用量の欄・対応する型を provider 設定の `reflex_judgment` 欄で宣言する
  形にした (提供元ごとの分岐はコードに置いていない)。モデルの役割
  `reflex_judgment_model` (env `SAIVERSE_REFLEX_JUDGMENT_MODEL`) を一元表へ追加し、
  DB に `AI.AUTO_RECALL_ENHANCED` を足してペルソナ設定画面にトグルを出した。
  自動想起は env 5 本を廃止して「スイッチ + 役割の割り当て」判定へ載せ替え、
  同梱のモデル設定 3 つ (TypeSafe 公式 / OpenRouter / localjev) を入れた。
  設計が黙っていた点でこちらが決めたこと: 方言の宣言の欄名と項目 / OpenRouter は
  宛先の根が違うので専用 provider (`openrouter_systemone`) を分けたこと / 使用量は
  既存の `UsageTracker.record_usage` へ区分 `reflex_judgment` で載せること /
  第 1 段では noul だけ範囲まで検算し、choice・score は器を通すところまでにしたこと。
- 2026-09-20: 敵対レビューの指摘 4 件を反映。(1) 締切で見切った呼び出しでも、応答が
  後から届けば**使用量だけ**は記帳するようにした (答えは読まない)。API 側の課金は
  もう発生しているので、落とすと「支払ったのに台帳に無い」費用ができる。記帳するのは
  「メインが見切った印が立っている場合のワーカー」だけで、メインが記帳するのは成功で
  返った経路だけなので二重記帳は構造上起きない。(2) 辞書でない質問は
  `ValueError` (呼び出し側のバグ) として返す — 外部 API の障害には化かさない。
  (3) モデル設定の `reflex_judgment` 欄は provider の宣言との**キー単位の合成**に
  した。丸ごとの置き換えのままだと、モデル側に空辞書や部分的な宣言が書かれただけで
  provider の宛先の path が消え、OpenRouter 系で間違った URL を呼ぶ穴が残っていた。
  (4) トークン数として成り立たない使用量 (負数・NaN・inf) はその欄だけ捨てる。
  会計の欄が壊れているだけの応答を「判定できなかった」にはしない。
- 2026-09-20: 敵対レビュー 3 巡目の指摘 2 件を反映。(1) 正常応答の使用量の記帳を、
  答えの検証より**前**へ動かした。前の巡で「メインが記帳するのは成功で返った経路
  だけ」にしていたため、HTTP 200 で有効な使用量が載っていても答えの側の不備
  (qid の欠落・型違い・範囲外) で例外になると記帳に届かず、課金済みの費用が台帳から
  落ちていた。締切で見切った呼び出しが答えを見ずに使用量だけ記帳する形とも揃う。
  非 200 と JSON として読めない応答は従来どおり記帳しない。(2) 画面の警告の検査を、
  実行側の宛先解決 (`resolve_backend`) を呼ぶ形へ集約した。protocol だけを自前で
  見直していたため、protocol は `jev_compat` でも宛先の URL や答えられる型が
  宣言されていない不備が画面に出なかった。同じ関数に寄せたので、今後 `resolve_backend`
  に検査が増えても画面が自動で追従する。警告の文面も「jev 互換ではない」から
  「反射判断の宛先として使えない」へ広げた。
- 2026-09-20: 敵対レビュー 6 巡目の指摘 4 件を反映。(1) **§2 が「既存の provider
  照合が守る」と書いていた約束を、実際に呼んで成立させた。** 通常の会話クライアントは
  接続を作る前に `validate_model_config_connection` を必ず通るのに、反射判断の宛先解決
  だけが素通りしていた — 拡張データや user_data の定義が `api_key_env` に他所のキーの
  env 名を書き、宛先を自分の好きな所に向けるだけで、そのキーが第三者へ送られる穴だった。
  照合の失敗は `ReflexJudgmentUnavailable` へ正規化する (この層の例外契約のまま)。
  (2) 判定が使えなかったターンは、**候補集めからやり直して従来の形へ戻す**ようにした。
  強化が効いているターンは候補の母集団を広げているので、判定だけ落ちて広げたまま cosine
  で選別すると、従来経路なら母集団にすら入らない記憶が注入される — 「従来方式へ戻った」
  というログと実際の挙動が食い違っていた。(3) 「使えるか」の判定に**答えられる質問の型**
  を入れた (`resolve_backend(required_type=...)`)。第 1 段でこの層に仕事を頼むのは自動
  想起の選別だけで、その仕事は noul しか投げないので、自動想起も画面の警告も noul 対応
  までを「噛み合っている」の条件にする。choice にしか答えない宛先を「使える」と答えると、
  (2) の劣化が毎ターン起き続ける状態になる。(4) 画面の警告で、検査そのものが例外を出した
  値を黙って捨てるのをやめ、「確かめられなかった = 定義なし」として警告に出すようにした
  (保存と一時上書きの関所と同じ向き)。
- 2026-09-20: 敵対レビュー 7 巡目の指摘 2 件を反映し、8 巡目で approve — 収束。
  (1) 候補の集め直しの失敗を「候補ゼロ」と区別する形にした。集め直しの障害の空を
  成功として扱うと、粘着台帳が「候補の無かったターン」として古び、障害が続いた
  だけで粘着記憶が消える — 集め直しに失敗した回だけは成功済みの広い候補のまま
  cosine 選別に掛ける (集め直し導入前の受容済みの形へ、そのターンだけ戻る)。
  (2) 実呼び出し直前の宛先解決にも「答えられる型」の条件を渡し、使えるかの判定と
  同じ述語にした。7 巡目の「解決結果を判定から実呼び出しまで一本で持ち回せ」は
  採らず — 実呼び出し直前の解決が照合を全部やり直すので、判定との間に設定が
  読み直されても、検査を通らない宛先へ飛ぶ経路は生じない。レビュー全体の収支:
  8 巡で本物 13 件を反映、誤指摘 3 件 (二重記帳の競合 / supported_types の
  unhashable / 画面警告の TypeError 漏れ) は証明・実測で却下。
- 2026-09-20: 第 1 段がまはーの実機確認に合格し、develop へマージ (PR #309)。
  続けて第 2 段 (§6-4) を実装。(1) **通常 LLM への変換層** — `resolve_backend` が
  jev 互換以外のモデル設定も答える側として解決し (種別は解決結果の 2 値だけで持つ、
  提供元名の分岐なし)、`evaluate` の器 (同時 4 本・絶対締切・全 qid・記帳の錠の
  取り決め) を共有したまま、往復の中身だけを差し替える。質問は 1 通のプロンプトへ
  畳み、答えは構造化出力の**配列**で受けて qid の辞書へ戻す (Gemini が
  additionalProperties を受けないため — §3 の設計どおり)。使用量はクライアントの
  `consume_usage()` から受け取り、同じ入口へモデル設定キー名義・区分
  `reflex_judgment` で記帳 (キャッシュの欄も写す)。通常 LLM の `generate` は途中で
  切れないので、締切で見切ったワーカーは走り切ってから使用量だけを記帳し、その間
  同時実行の枠を 1 本占有し続ける (壊れ方は費用ゼロ側)。キー有無の判定は
  `is_model_available` (代替キー名・旧形式の既定キー名・キー不要のローカル勢を知る
  既存の述語) に寄せた。(2) **ペルソナ個別の上書き** — DB 列
  `AI.REFLEX_JUDGMENT_MODEL` (NULL = 世界の既定)。保存はペルソナ設定の関所
  (`_checked`) を通り、実行時は自動想起の呼び出し側が読んで判定と実呼び出しの両方へ
  渡す。起動時の画面警告のペルソナ単位の検査にもこの列を足した (標準・軽量・
  Memory Weave と同じ扱い)。(3) **choice / score** — 変換層は 3 型すべてに対応。
  `options` を宣言した choice の答えは選択肢の中のどれかであることまで検算する
  (jev 互換の道にも同じ規則)。設計が黙っていた点でこちらが決めたこと: 変換層の
  プロンプトは system ロールを使わず user 1 通 (既存の構造化出力呼び出しと同形) /
  temperature は指定しない / 選択肢の enum はすべての choice 質問が選択肢を宣言して
  いるときだけスキーマに載せる。実装は Opus サブエージェント 2 体 (変換層 / 上書きの
  配線) + 検収の直し 1 体で、検収で 3 件を差し戻した (壊れた宛先への画面警告の削除を
  復元 / キー有無の手組み判定を既存述語へ / ペルソナ列の起動時警告の欠け)。
- 2026-09-20: 第 2 段のレビュー。ローカル LLM 1 巡 — 本物 1 件 (代替キー名で認証した
  通常 LLM の例外文言で、伏せ字が実際のキーの値を知らない) を修正: 伏せる値の一覧を
  キー有無の判定と同じ表 (`required_api_key_env_names`) から作る。あわせて、強化
  OFF のペルソナでもペルソナ個別の上書き列を毎ターン読んでいた無駄を削った (ON の
  ときだけ読む)。「遅い通常 LLM が同時実行の枠を応答まで占有する」指摘は設計済みの
  受容 (費用ゼロ側の壊れ方、コメントに明記済み) として採らず。敵対レビュー 1 巡目 —
  本物 2 件を修正: (1) ワールドエディタの保存が、画面の扱わないモデル欄 (Memory
  Weave・画像/音声/動画の要約・反射判断) のペルソナ個別設定を NULL へ戻すデータ損失。
  入口が現在値を詰めて渡す形で族ごと修正
  (docs/issues/archive/world_editor_save_wipes_persona_model_overrides.md)。
  (2) LLM クライアントの工場が話せない protocol の設定が「通常の LLM として解決
  できた」顔で保存と画面警告を通り抜け、実行時に毎ターン黙って失敗する穴。解決の
  照合を工場の規則そのもの (`llm_clients.factory` の `resolve_protocol` +
  `SUPPORTED_PROTOCOLS`、この変更で公開) に寄せた — 旧形式 (provider 名だけ) の
  設定は工場と同じ写像で通る。
- 2026-09-20: 敵対レビュー 2 巡目の指摘 3 件を反映。(1) 前巡のワールドエディタの
  修正 (入口が現在値を詰めて渡す) は、読みと書きの間に別の画面の保存が挟まると
  その保存を古い値で巻き戻す — 受け側 (`update_ai`) がモデル欄の既定を「送られて
  こなかった = 触らない」の印 (Building の更新で実績のある `UNSET`) にし、印の解決を
  錠の中で行う形へ差し替えた。ペルソナ設定の PATCH の「現在値で詰め直す」も同じ穴
  だったので、印を渡す形に揃えた。(2)(3) は同根 — provider 欄なしで protocol だけ
  書いた手書きの設定では、キー有無の判定も伏せ字も「クライアントが実際に読む既定の
  キー名 (OPENAI_API_KEY 等)」を知らなかった。キー候補の表
  (`saiverse/model_configs.py` の `_get_required_env_vars`) が明示の protocol でも
  既定キー名を引けるようにした — 判定 (`is_model_available`) と伏せ字
  (`_mask_targets` → `required_api_key_env_names`) は同じ表を見ているので、一箇所の
  修正で両方が直る。
- 2026-09-20: 敵対レビュー 3 巡目の指摘 1 件を反映。ペルソナ設定のモーダルが、開いた
  時点のモデル欄 7 つを毎回全部送っていた — 開いている間に別の画面で変えたモデル設定を、
  保存が古い値で巻き戻せる (受け側の「送られてこなかった欄は触らない」が、全欄送信では
  効かない)。モーダルは「開いてから変えた欄」だけを送る形にした (変わっていない欄は
  リクエストから落ちる)。空文字 =「個別設定を外す」は従来どおり明示で送る。版番号での
  楽観的排他 (古い画面の保存を拒否する仕組み) は採らない — 一人で使うローカルアプリの
  脅威模型に対して過剰で、説明・文言・エラー経路の複雑さが把握可能性の制約を超えるため。
- 2026-09-20: 敵対レビュー 4 巡目の指摘 1 件を反映。ペルソナ設定モーダルの設定の
  読み込みは同じペルソナでも二度走る (開いたときと、モデル一覧が届いたとき) のに、
  古い応答を捨てる判定がペルソナの切り替えしか見ていなかった — 遅れて返った応答が
  編集中のフォームとモデル欄の控えを上書きしうる (この変更以前からの画面の性質だが、
  3 巡目で足した控えも同じ上書きを受けるため、ここで直した)。読み込みに世代番号を
  付け、最後に始めた読み込み以外の応答はフォームへ反映しない。
- 2026-09-20: 敵対レビュー 5 巡目の指摘 1 件を反映。読み込みの後半 (スペル一覧の
  取得のあと) にも、古い世代の応答を捨てる判定の無い反映点が残っていた — 取得と
  反映を分け、反映の直前に世代の判定を一度通す形にした (ロード成功の印まで含めて、
  古い世代はそこから先を実行しない)。
- 2026-09-20: 敵対レビュー 6 巡目で approve — 第 2 段のレビュー収束。収支: ローカル
  LLM 1 巡 (本物 2 件を修正、受容 1 件 — 遅い通常 LLM の枠の占有は設計済みの費用ゼロ側)
  + Codex 6 巡 (本物 8 件を修正)。誤指摘は 0。処方を差し替えたのは 2 箇所 — 競合時の
  保存の保護は「入口の詰め直し」でなく「受け側の UNSET の印」(2 巡目)、古い画面の保存
  への対処は「版番号の楽観的排他」でなく「変えた欄だけの差分送信」(3 巡目、把握可能性
  の制約)。3〜5 巡目の画面の直し 3 件は、この変更以前からのペルソナ設定モーダルの
  性質 (全欄送信・二重読み込みの上書き) に対するもので、レビューが実害の形を具体化した
  のを受けてこの束で閉じた。
- 2026-09-21: 変換層が、クライアントへ**「この用途は一撃の判定なので思考は最小でよい」**
  と伝えるようにした (`llm_clients/base.py` の `prefer_minimal_reasoning`、既定は
  no-op。`_LLMCall.perform` がクライアント生成の直後、`generate` の前に呼ぶ)。発端は
  締切を 2.5 秒から 5 秒へ延ばしたのと同じ実測 — Gemini 3.5 Flash-Lite が判定 1 回に
  3.5〜4 秒かかり、`llm_io.log` に思考の痕跡が出ていた。一撃の分類に長考は要らず、
  待ち時間と費用だけが増える。つまみの実名 (thinking_level / thinking_budget /
  reasoning_effort) は各クライアントの中に置き、呼び出し側には提供元別の分岐を作らない。
  契約は 2 つ: **明示された思考の設定 (モデル設定ファイルの `parameters` の既定も含む)
  があれば尊重して何もしない**、**最小にできると確信が持てないモデルでは何もしない**
  (思考を切れない世代に「切る」を送ると呼び出しごと失敗する — 遅いまま動く方が、速く
  壊れるより良い)。実装は Gemini 3 系が `thinking_level="low"` (`"minimal"` は 3.7 以降で
  廃止され、送るとエラーになる)、Gemini 2.5 の Flash / Flash-Lite が `thinking_budget=0`
  (それぞれのモデル設定が "off" として宣言している値)、2.5 Pro とそれ以外の世代・
  openai 互換・anthropic・ollama・xai・codex は no-op。
  **効果測定の前提**: 現在役割に割り当てられている `gemini-3.5-flash-lite-paid` は、
  モデル設定が `thinking_level` の既定に `"minimal"` を宣言していて、factory が生成直後に
  それを適用している (実機の `llm_io.log` でも
  `ThinkingConfig(include_thoughts=True, thinking_level=MINIMAL)` を送っていることを確認
  済み)。つまりこのモデルは既に思考の下限におり、この変更では 3.5〜4 秒は縮まない。
  効くのは、思考の既定を宣言していないモデル設定を反射判断に割り当てたときだけ。
- 2026-09-21: **フォールバックが起きたことを、使う人が気づける形にした**。それまでは
  判定が時間内に返らなくても WARNING がログに 1 行出るだけで、画面には何も出なかった —
  毎ターン費用だけが出て効果が出ていない状態が、ログを読む人以外には見えなかった。
  まはー裁定で 3 点セットにする: **気づく** (起きたターンの返事のそばに画面の注記を
  1 行)、**状況が分かる** (直近 20 回のうち 3 回以上ならモデル設定の警告欄に集計)、
  **直せる** (待ち時間をグローバル設定から変えられる)。
  - **待ち時間はグローバルに一つだけ** (まはー裁定)。モデルごとには置かない — これは
    「どのくらい待ったらフォールバックを発動させるか」のパラメータで、モデル依存の
    需要はほぼ無い。env は `SAIVERSE_REFLEX_TIMEOUT_SECONDS` (既定 5 秒、1〜60 に丸める)
    で、読む側 (`sea/auto_recall.py` の `get_reflex_timeout`) が呼び出しのたびに読むので
    再起動なしに次のターンから効く。設定が無い・壊れている回は 5 秒で動き続ける
    (会話を止めない)。
  - **フォールバックはチャットの注記で知らせる** (まはー裁定)。設定画面だけでは
    気づけない — 使う人はそこを見ていない。注記は**ペルソナの発言ではなく画面の注記**で、
    会話履歴・建物の記録・ペルソナの記憶のどれにも書かない。運び方は自動想起の
    `auto_recall` イベントと同じ面 — `_maybe_inject_auto_recall` が `event_callback` へ
    `{"type": "reflex_fallback", "reason": "deadline"}` を 1 回流し、NDJSON のストリームを
    通って画面の吹き出しに貼られるだけ (発言の metadata へ載せる経路は持たない)。
    出す先が無いターン (`event_callback` を持たない自律 Pulse) は静かに何もしない。
  - **知らせるのは「時間内に答えなかった」ときだけ**。キー欠落・接続失敗・不正応答は
    待ち時間を延ばしても速いモデルに替えても直らないので、直し方の書いてある注記で
    案内すると誤った案内になる。**例外の型は 1 つのまま** (呼び出し側が 1 種類を捕まえて
    畳めるという契約を壊さない) で、種別は `ReflexJudgmentUnavailable.kind` の属性で運ぶ —
    締切超過の raise だけが `"deadline"`。
  - 集計の記録はプロセス内の直近 20 件だけ (`saiverse/reflex_judgment.py` の
    `recent_outcomes`)。再起動で消えるのは仕様 — これは設定を直すための観測であって、
    世界の状態ではない (永続化すると「どのペルソナの・いつの」まで背負うことになる)。
  - **数えるのは 30 分以内の記録だけ** (`OUTCOME_WINDOW_SECONDS`)。件数だけで数えると、
    モデルを速いものへ替えて直した人が、そのペルソナとしばらく喋らないだけで
    「最近 3 回中 3 回…」の警告を見続ける。効果の観測は、古い証拠で警告を出し続けない。
  - 警告の分母 (直近◯回) には時間切れ以外の失敗も入るので、**その回数も文面に書く**。
    書かないと、キーが無くて毎ターン落ちている家にまで「待ち時間を延ばせ」と読める
    文面だけが出る。`recent_outcomes` が `(総数, 時間切れ, それ以外の失敗)` を返すのは
    そのため。
- 2026-09-21: 可視化の束の敵対レビュー 2 巡目 2 件を反映。(1) 貼り違いのガードが
  「食い違ったら新しい吹き出しを作る」に倒れていたのを「作らずに捨てる」へ —
  本文の流し込みは「最後の生成中の吹き出し」しか見ないので、空の受け皿を末尾に
  置くと別ペルソナの本文がそこへ吸い込まれる。同時進行という稀なターンだけ注記と
  想起の表示を諦める (集計と設定画面の警告には残る)。チャット画面のイベントと
  吹き出しの対応付けは「最後の生成中」方式が土台で、真の同時進行に耐えるには
  対応付けの作り直しが要る — 既知の限界として記す (想起の折りたたみ・作業の途中
  経過の表示も同じ土台)。(2) 待ち時間の保存中に打ち直した値が、先の保存の応答で
  黙って元に戻る競合 — 編集の世代番号で、応答が古ければ入力欄に触らない形にした。
- 2026-09-21: 可視化の敵対レビュー 4〜6 巡目 (下書きの破棄で注記が消える → 空の
  吹き出しの孤児 → 流し込みを使わない設定での孤児) を経て、注記の届け方を根から
  単純化した。**注記は生成中の吹き出しには一切貼らず、帳簿 (ペルソナ ID の集合、
  イベント処理の同期経路でだけ読み書き) に付けて、そのペルソナの確定の発言 (say)
  が届いたときにそこへ貼る。** 生成中の吹き出しは寿命が複雑 (Beat の切れ目で捨て
  られる・流し込みを使わない設定では最初から無い・確定は別便で届く) で、そこへ
  貼る限り退避や空の吹き出しの例外処理が際限なく要る — 例外処理が本体を覆い
  始めたので、供給源 (貼り先の選び方) を消した。表示のタイミングは「返事の途中」
  から「返事の確定時」になる。帳簿は部屋の判定を通ったイベントだけが触り、新しい
  応答ターンの入口 3 つ (送信・続き・やり直し) で捨てる。
- 2026-09-21: 可視化の敵対レビュー 7 巡目 (帳簿の鍵がペルソナだけで、部屋をまたぐ
  持ち越しが理屈上作れる) を反映 — 鍵を「部屋 + ペルソナ」の組にして、8 巡目で
  approve。可視化の束の収支: Codex 8 巡で本物 12 件を修正・受容 2 件 (空の吹き出し
  の縁は途中の設計変更で消滅・フロントの回帰テスト基盤は無いので実機検証で代える)・
  裁定却下 1 件 (同室並行の土台は issue 切り出し)。ローカル (Flash-Next) 1 巡で
  本物 4 件 (集計の古び・壊れた env 値の詰み・警告文の分母・blur の黙った保存)。
- 2026-09-21 (実機で注記が出ない): まはーの実機テストで、締切超過は起きているのに
  チャットの注記が一度も出なかった。原因は上の「確定の発言 (say) が届いたときに
  貼る」の前提が偽だったこと — **流し込み (ストリーミング) の返事は say イベントを
  出さず、streaming_complete で確定する**。say が来るのは流し込みを使わない道・
  Beat の差し替え・スペル後の締めだけで、既定の設定の普通の返事では帳簿が誰にも
  読まれず注記が永遠に出なかった。修正は streaming_complete の処理にも say 側と
  同じ消費 + 貼り付けを足す (鍵・部屋の判定・同期の規則は同一)。検証は隔離環境で
  実施 — わざと 20 秒黙る宛先を反射判断に割り当て、注記がブラウザの実画面に出る
  ことまで確認した (サーバー側は NDJSON に reflex_fallback が乗ることを別途実測)。
  教訓: 「確定の発言」のような抽象は、実際のイベント列 (ブラウザのコンソールに
  並ぶ type の列) を一度も見ずに実装してはいけない — 8 巡の敵対レビューも、say が
  来ない道があることには気づかなかった。
- 2026-09-21 (上の修正への敵対レビュー 1 巡): 指摘 2 件。(1) 同室で二人が同時に
  生成中だと、末尾の吹き出し頼みの貼り付けが隣のペルソナに注記を付けうる — 「同じ
  話者か」の判定を貼り付けの条件に足した (不一致は貼らずに諦める。本文側の流入は
  chat_stream_event_correlation_by_last_bubble.md の土台の限界)。(2) 貼り先の
  吹き出しが無い完了 (中身ゼロの中断など) では帳簿を消費して注記が消える — 受容。
  注記は「確定の返事に添える」ものなので、添える相手の返事が無いターンに表示先は
  そもそも無く、集計と設定画面の警告には残る (可視化の設計時の受容「稀なターンは
  表示を諦める」と同じ族)。
- 2026-09-21 (収束確認の巡): 「帳簿の鍵に Pulse ID が無く、同一ペルソナの別ターンの
  完了が注記を横取りしうる」の 1 件 — 却下。前提の「同一ペルソナの 2 ターンが同時に
  走る」は、実行側の台帳 (sea/pulse_controller.py の _current、同一ペルソナで同時に
  1 本のみ・塞がっていれば新規を弃てる) が防いでいる。1 ターン内の複数 Beat は同じ
  ターンなので、最初の確定へ貼るのは設計どおり。Pulse ID での相関付けは、注記を
  含む全マージ経路の作り直しとして issue 側に切り出し済み
  (docs/issues/chat_stream_event_correlation_by_last_bubble.md、v0.4 前が適時)。
