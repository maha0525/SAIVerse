# Handoff 2026-05-26: Pulse 可視化と派生改修

**親**: [README.md](README.md)
**位置づけ**: Phase 5 UC-2 (割り込みと復帰) の検証準備として可視化基盤を作り、それで自律 Track の動作を実際に覗いたら問題が次々見つかって派生改修が連鎖したセッションの記録。次セッションのハンドオフ兼ねる。
**状態**: 全実装一巡完了。ただし **大半が実機検証待ち (要サーバー再起動)**。

---

## 経緯

UC-2 の最初の達成目標 (= 自律 Track running 中の割り込み→応答→復帰) を実機検証するため、(1) タイマーを手動制御するデバッグコントローラーと (2) Pulse 単位の可視化を先に整備した。それで cb67c333 という実際の自律 Pulse を覗いたところ、記録・時系列・文脈保持に複数の問題が見つかり、派生改修が連鎖した。

UC-2 本体 (系統 i 往復の実機検証 → 系統 ii = SocialTrackHandler 運用化) は**まだ着手していない**。本セッションは「検証の前に道具を作り、その道具で見つけた地雷を片付けた」段階。

---

## 成果一覧

### 本流: 可視化・デバッグ基盤

| 内容 | 実装 | Intent/issue |
|------|------|------|
| デバッグコントローラー (タイマー無視の手動発火 + 完全手動モード) | `api/routes/people/debug.py`, `frontend/.../DebugPanel.tsx`, `pulse_scheduler.py` is_running, `saiverse_manager._debug_manual_mode_personas` | `debug_controller.md` |
| Pulse タイムライン (messages を pulse_id でグルーピング、Track 内連番 #N、入力プロンプト表示) | `api/routes/people/pulse_timeline.py`, `frontend/.../memory/PulseTimelineViewer.tsx` (MemoryModal にタブ追加) | — |

### 派生改修 (可視化で発見)

| # | 内容 | 実装 | issue |
|---|------|------|------|
| 1 | spell args パーサが小文字 `true`/`false`/`null` を正規化 (tokenize で文字列内保護) | `sea/runtime_llm.py:_normalize_json_literals` + `tests/test_spell_args_parsing.py` | — |
| 2 | spell の HTML (`<user_only>`/`spellResult`) が SAIMemory に漏れるバグを 3 経路対称化で修正 | `sea/runtime_llm.py` (streaming/sync/tool mode) | `spell_html_leak_into_saimemory.md` |
| 3 | judgment が子ライン出力より後に記録される時系列逆転を修正 (spell 実行前に記録) | `sea/runtime_llm.py:_run_spell_loop` | `spell_judgment_recorded_after_subline.md` |
| 4 | memopedia_note を spell 化 (`spell=True` 欠落が不発の原因) + playbook を archive | `builtin_data/tools/memopedia_note.py` | — |
| 5 | round 上限 (`SAIVERSE_SPELL_MAX_ROUNDS=10`) 到達時の挙動再設計 (低優先で保留) | — | `spell_round_limit_redesign.md` |
| 6 | SubLineScheduler が ACTIVITY_STATE を見ない件 (保留) | — | `subline_scheduler_ignores_activity_state.md` |
| 7 | volatile 結論: context には漏れていない (実害なし)、DB 肥大と削除未実装が残るのみ | (調査のみ) | — |
| 8 | **paired_action_text の context 復元** (下記、本セッションで最重要の認知モデル修正) | `sai_memory/.../storage.py`, `saiverse_memory/adapter.py` | `spell_judgment_recorded_after_subline.md` 周辺 |

---

## #8 paired_action_text の context 復元 (認知モデルの本質修正)

**発見した破綻**: 各 LLM ノードの action (指示テキスト) は `paired_action_text` カラムに応答とペアで記録されていた (Phase 1 P0-6) が、**context 構築の SELECT に含まれず、次 Pulse で読まれていなかった**。= ペルソナが「過去に何を指示されたか」を Pulse 跨ぎで思い出せない文脈喪失。#2 の Pulse で「サブラインとしての最初のパルス」と誤認したのはこれが原因 (#1 の起動 action が context に無く、毎回「最初」だと思っていた)。

**重要な切り分け**: Pulse 内 (spell loop / run_playbook の子ライン) では action は messages 変数で持ち回されるので揮発しない (ログで確認: track_autonomous の action が web_research 子ラインの全ラウンドに残っていた)。**揮発は Pulse 跨ぎのみ**。

**修正**: scope に連動させた。
- `_LINE_METADATA_COLUMNS` に `paired_action_text` 追加 (SELECT に含める) — `storage.py`
- `_payload_from_message_locked` で **scope=committed の応答のみ** payload に乗せる (volatile は揮発) — `adapter.py`
- `_expand_paired_action_payloads` で「action → 応答」順に展開 (committed の action を `<system>` 付き user メッセージとして応答の直前に挿入) — `adapter.py`、`recent_persona_messages` 系で適用

**綺麗にする案 (見送り)**: action を生成時に独立 user メッセージで記録すれば時系列で自然に並ぶが、SAIMemory (記憶本体) に指示が混ざって汚れる + レコード倍。paired_action_text は記憶の純度を守る設計なので、保存=ぶら下げ/復元=前置展開の非対称は受け入れる。

**当初の遠回り**: 最初 `_dump_llm_io` で送信 messages 全体を pulse_logs に二重記録しようとしたが、(a) paired_action_text に既にあった (b) messages 配列丸ごとは system prompt + 全履歴で重複過剰、と判明して revert した。

---

## 実機検証待ち (要サーバー再起動)

1. デバッグコントローラーの各操作 (手動発火 / タイマー停止 / 完全手動モード)
2. Pulse タイムライン表示 (Pulse カード / 連番 / 入力プロンプト)
3. HTML 混入修正 (新 Pulse から `<user_only>` が SAIMemory に入らないか)
4. 時系列逆転修正 (judgment が子ライン出力より前に並ぶか)
5. memopedia_note の spell 発火
6. **paired_action_text 復元: #2 の「最初のパルス」誤認が直るか / volatile (web_research 中間) の action は揮発するか**

**注意**: #8 で既存の committed メッセージにも paired_action_text があるため、**再起動初回は過去 action が一斉に context 復元されて履歴が変わり、プロンプトキャッシュが一度リセットされる** (一時的)。以降は action が一貫して入って安定する方向。

---

## 次セッションへ

- 上記の実機検証 (特に #8 が認知モデルの根幹なので最優先)
- **UC-2 本体**: 系統 i (対ユーザー割り込み→復帰) の実機検証 → 系統 ii (`SocialTrackHandler.on_persona_utterance` + `on_track_activated` の main_line 起動化、track_social.json 運用化)。詳細は `phases/phase_5_autonomy.md` と [README](README.md) 進捗表
- b の連番注入 (任意・small): `consecutive_pulse_count` を Pulse プロンプトに出して「最初のパルス」連呼を抑える。ただし #8 で過去 action が見えるようになったので優先度は下がった
- 保留 issue: `spell_round_limit_redesign.md`, `subline_scheduler_ignores_activity_state.md`, volatile の DB 肥大 (削除未実装)
