# Handoff 2026-05-10: メインライン応答の `origin_track_id` が NULL になる回帰バグ

**親**: [README.md](README.md)
**前回 handoff**: [handoff_2026-05-09.md](handoff_2026-05-09.md) — 自律稼働中の長期 idle 脱出機構
**関連 Intent**: [meta_judgment_structured.md](meta_judgment_structured.md) — メタ判断 v2 実装 (本セッションで完了、その実機検証中に本バグを発見)
**ステータス**: ✅ 対応完了 (commit `5d567a7`、2026-05-10)

このセッションでメタ判断 v2 (構造化出力ベース) の実装一巡 + 関連バグ 2 件修正 (commit `e95fa41`) を完了した直後、メタ判断時の Track 一覧で「最終: 3日前」と表示されるのが不自然 (まはー認識では昨日 5/9 17:59 が最終発話) という指摘から調査を開始し、**メインライン応答の `messages.origin_track_id` カラムに NULL が書き込まれる回帰バグ**を発見した。本ハンドオフはその追跡と修正のための引き継ぎ資料。

---

## 1. 観察された事実

### DB の状態 (air_city_a / 対 user1 会話 Track = `c6fa3f2d-ae1a-4d3e-b908-b9d92fbb7640`)

直接 `~/.saiverse/personas/air_city_a/memory.db` を SQL で見た結果:

```
-- 5/6 のメッセージ: origin_track_id が正しく埋まっている
2026-05-06 23:05:29  role=assistant  origin_track_id=c6fa3f2d..  line_role=main_line  scope=committed
2026-05-06 23:05:17  role=assistant  origin_track_id=c6fa3f2d..  line_role=main_line  scope=committed
2026-05-06 17:38:24  role=assistant  origin_track_id=c6fa3f2d..  line_role=main_line  scope=committed
2026-05-06 17:38:12  role=assistant  origin_track_id=c6fa3f2d..  line_role=main_line  scope=committed
2026-05-05 23:27:03  role=assistant  origin_track_id=c6fa3f2d..  line_role=main_line  scope=committed
...

-- 5/9 のメッセージ: origin_track_id が NULL になっている
2026-05-09 17:59:08  role=user       origin_track_id=NULL        line_role=NULL         scope=committed
  'エアも自分の声聴きたいよねぇ……今声質の自然言語化みたいなのの準備を…'
2026-05-09 17:59:52  role=assistant  origin_track_id=NULL        line_role=main_line    scope=committed
  'まはー、おかえりなさい！展示会が終わっても、まはーのエンジニア魂は…'
```

5/9 のエアの応答は `line_role=main_line, scope=committed` で**メインライン応答として正しく書き込まれている**が、**`origin_track_id` だけが NULL になっている**。

5/9 のメタ判断系 (line_role=meta_judgment) も全て `origin_track_id=NULL` だが、これは旧仕様の `_record_judgment_log` 経路では仕様的に NULL のまま (Track 横断のメタログ領域)。問題はメインライン応答のほう。

### 「3日前」表示は正しく動いている

- `MAX(created_at) WHERE origin_track_id=c6fa3f2d..` = `1778076329` (5/6 23:05:29、JST)
- 現在 5/10 14:54 から見て差 = **3.66 日** → 表示「3 日前」は正確
- タイムゾーンも `_format_relative` の計算もズレていない
- → 表示変換ではなく、データ側 (`origin_track_id` の埋め漏れ) が原因

調査スクリプト (実行済): `temp/check_track_messages.py` / `temp/check_track_messages2.py`

### 5/7 以降のいつかから NULL になり始めた

- `5/5 23:27` 以前: `origin_track_id` 埋まっている
- `5/6 23:05` まで: 埋まっている
- `5/7 〜 5/8` の範囲は `assistant` role でメインライン応答が無い (メタ判断系のみ)
- `5/9 17:59:52` 以降: メインライン応答が NULL

つまり**境界は 5/6 23:05 〜 5/9 17:59 の間**。この期間に commit されたコード変更が原因の可能性が高い。

---

## 2. 影響範囲

`origin_track_id` が NULL のままだと連鎖的に複数機能が壊れる:

1. **Track の最終メッセージ時間表示** (本件発覚の入口)
   - `SAIMemoryAdapter.get_track_last_message_times` (`saiverse_memory/adapter.py:1695`) は `WHERE origin_track_id IN (...)` で絞り込んで `MAX(created_at)` を取る。NULL のメッセージは拾われない。
   - `track_list` ツールの `last_message_at` / `last_message_relative` が古い値を返す。
   - メタ判断 v2 の状況テキスト「最終: N日前」が常に誤情報を提示 → ペルソナの判断を歪める。

2. **`wait_response_timeout` の base_time** (`saiverse/track_manager.py:_schedule_wait_response_timeout`)
   - provider が「最終メッセージ時刻」を返す経路で同じ MAX クエリを使っている (`SAIVerseManager` 周辺の provider 実装は要確認)。
   - base_time が古いままになり、本セッションで観測した「activate 直後に即タイムアウト → ループ」の遠因の一つにもなった。本セッションで base_time が過去なら `now()` フォールバックする修正は入れたので、ループ自体は止まる。が、根本は本件。

3. **Track Chronicle 生成範囲** (`sea/runtime.py:_generate_track_chronicle` 周辺)
   - 「Track ごとの未処理メッセージ」の集計が `origin_track_id` ベースのため、NULL 化したメッセージは Chronicle にもならない。
   - 本セッション末にコミットした `incomplete Lv1 のカバー範囲スキップ` 最適化 (commit `4a0a042`) も、このデータが正常な前提で動く。

4. **Track 別履歴フィルタ全般**
   - `messages` テーブルを `origin_track_id` で絞る他の経路 (履歴 UI / 検索 / Stelis 等) も影響を受ける可能性あり。

---

## 3. 原因のあたり

### 該当コード

`sea/runtime.py:_store_memory` (約 1200-1240 行付近) で `origin_track_id` を埋める:

```python
resolved_track_id = origin_track_id  # 引数からくる場合
if pulse_context is not None and (... resolved_track_id is None):
    meta = pulse_context.current_line_metadata()
    ...
    if resolved_track_id is None:
        resolved_track_id = meta.get("origin_track_id")

message: Dict[str, Any] = {"role": role or "assistant", "content": text}
...
if resolved_track_id is not None:
    message["origin_track_id"] = resolved_track_id  # ← ここが None なら set されない
```

つまり、**`PulseContext.current_line_metadata()` が `origin_track_id` を返してきていない / 引数経由でも来ていない**のが直接の原因。

### 仮説

**仮説 A: LineFrame に `origin_track_id` を登録する経路の漏れ**

`PulseContext._line_stack` / `LineFrame` に `origin_track_id` が乗るタイミングは:
- メインライン Pulse 起動時 (`run_meta_user` / `handle_user_input_stream` 経路)
- サブライン Spell (`/run_playbook`) 起動時
- Track 切り替えスペル発動後の deferred apply 直後

直近 commit のうち以下が容疑者:
- `9f03948 fix(sea): tool ノード結果が後続 LLM ノードに見えないバグを修正 + Phase 3 段階 4-D 完了`
  - context_profile / model_type / exclude_pulse_id / pulse_id タグ併行記録 などを一斉削除した大規模リファクタ。LineFrame の構築経路を整理したときに `origin_track_id` の伝播が一部で抜けた可能性。
- `cf946cd feat(persona): wait_response Track 自動 pause タイマー + Track 最終メッセージ時間の可視化`
  - Track 状態遷移経路を触っているので、activate 後の Pulse-root context 構築で LineFrame に origin_track_id をセットし忘れた可能性。
- `96a789c feat(persona): Track Chronicle 機構 + ユーザー会話 Track 親スレッド保持を実装`
  - ユーザー会話 Track 親スレッド保持 (`saiverse/user_conversation_preserver.py`) で履歴を別経路から流し込むように。この経路で書かれるメッセージに `origin_track_id` を渡し忘れている可能性。

**仮説 B: ユーザー発話の経路で origin_track_id が伝播しない**

5/9 の `role=user` のまはー発話も `origin_track_id=NULL` だった。`manager/runtime.py:handle_user_input_stream` でユーザー発話を SAIMemory に書き込む経路で、当該 Track ID を message dict に乗せていない可能性。

メインライン応答だけでなく**ユーザー発話側も同症状**である点は、書き込み経路全体の根の問題を示唆している。

---

## 4. 推奨される追跡手順

1. **境界 commit の特定**
   - `git log --oneline --since="2026-05-06" --until="2026-05-09" -- sea/ saiverse/ saiverse_memory/ persona/ manager/`
   - 上記 3 件以外にも該当コミットがあるか確認
   - `git bisect` を使うほどではないが、コミット粒度で原因を絞れる

2. **PulseContext / LineFrame の origin_track_id 登録経路の grep**
   ```bash
   grep -rn "origin_track_id" sea/ saiverse/ persona/ manager/ | grep -v "_test"
   ```
   - 「LineFrame 構築時に origin_track_id を引数で渡している箇所」がいくつあるか
   - 「current_line_metadata() の戻り値に origin_track_id を含める」経路があるか
   - 直近で削除/変更されていないか

3. **書き込み側のデバッグログ**
   - `sea/runtime.py:_store_memory` の入口で `pulse_context.current_line_metadata()` の結果と `resolved_track_id` を出すデバッグログを **無制限に**入れる (memory: 「ファイルログは無制限に出す」)。
   - `manager/runtime.py:handle_user_input_stream` のユーザー発話書き込み経路でも `origin_track_id` の値をログ。
   - 1 ターン回して、どの経路で None になっているか特定。

4. **再現テスト**
   - 簡単に: ペルソナを起動 → ユーザー発話 1 回 → DB の最新 message 行を見る
   - `origin_track_id` が NULL なら本件再現
   - 修正後: 同じ手順で値が入っているか確認

---

## 5. 短期しのぎ案 (根本修正に時間がかかる場合)

`SAIMemoryAdapter.get_track_last_message_times` のフォールバック:

```python
# origin_track_id が埋まっている前提のクエリ
SELECT origin_track_id, MAX(created_at) ...

# フォールバック: 永続 user_conversation Track などは「メインライン応答の最新時刻」も拾う
# (origin_track_id NULL でも line_role=main_line なら現行 user_conversation の応答とみなす)
```

ただし、この種のフォールバックは **NULL 状態を許容してしまう設計汚染**になるので推奨しない。**まず根本を直してから、フォールバックは入れない**のが良い。

---

## 6. 本件と並行で残っている案件

メタ判断 v2 (commit `e95fa41`) の Phase 4 残タスク:

- 汚染ログ (旧仕様の応答調独白) の DB 除去 — 7 層ストレージタブの新規削除 UI から実行可能
- 実機検証 2 回目 (各状況 B/C/D/E + 各 LLM プロバイダの動作確認)
- 旧 `02_mechanics.md` §「メタ判断の動作仕様」を新 Intent への redirect stub に変更

これらは本件と独立して進めて良いが、「Track 最終メッセージ時刻」がメタ判断の判断材料に乗るので、本件が直らないと実機検証時の Track 一覧表示は誤情報を出し続ける点に注意。

---

## 7. 関連ファイル一覧 (調査の起点)

- `sea/runtime.py:_store_memory` — メインライン応答の SAIMemory 書き込み (origin_track_id を埋める箇所)
- `sea/pulse_context.py` — PulseContext / LineFrame / current_line_metadata 実装
- `manager/runtime.py:handle_user_input_stream` — ユーザー発話の書き込み経路
- `saiverse_memory/adapter.py:_append_message` — SAIMemory への低レベル書き込み (`origin_track_id` カラム反映)
- `saiverse_memory/adapter.py:get_track_last_message_times` — Track 別最終時刻取得 (本件発覚の入口)
- `saiverse/user_conversation_preserver.py` — ユーザー会話 Track 親スレッド保持機構 (commit 96a789c で追加、容疑者の一つ)
- `saiverse/track_manager.py:activate` — Track 切り替え時の状態遷移 (running 化時に LineFrame 連動があれば原因の可能性)
- 調査スクリプト: `temp/check_track_messages.py`, `temp/check_track_messages2.py`

---

## 8. 対応完了記録 (2026-05-10、commit `5d567a7`)

### 真の原因

仮説 A (LineFrame に track_id が乗らない経路の漏れ) が当たっていた。具体的には:

1. ユーザー会話 Track が pending 状態で発話 → `on_user_utterance` で `set_alert` 経路に入る
2. メタ判断発火、しかし切り替えスペル不発 → Track は alert のまま running に遷移しない
3. `invoke_main_line()` → `run_meta_user` → `_resolve_pulse_root_line` で `track_manager.get_running(persona_id)` を呼ぶが、running が無いので **`(None, None)` を返す**
4. `runtime_graph.py:133` の `if parent_pulse_ctx is None and pulse_line_role:` が False になり push_line されない → スタック空 → 書き込み時 origin_track_id NULL

つまり「`get_running` が None を返したら諦める」という設計が、Track の status 遷移に対して脆弱だった。

### 修正方針 (案2: Handler から SEA runtime まで track_id を引き渡し)

Pulse の起点が Track のイベントである以上、起点側 (Handler) が Track id を保持して引き渡す方が自然。`get_running` だけに依存しない経路を追加した。

引数経路:

```
on_user_utterance (track 取得)
  ↓ track.track_id
invoke_main_line(track_id) → manager/runtime.py の closure
  ↓ origin_track_id=track_id
SAIVerseManager.run_sea_user(..., origin_track_id=...)
  ↓
PulseController.submit_user(..., origin_track_id=...)
  ↓ ExecutionRequest.origin_track_id
PulseController._do_execute → sea_runtime.run_meta_user(..., origin_track_id=...)
  ↓ override_track_id
_resolve_pulse_root_line: 引数優先 + get_running フォールバック
  ↓ (_root_role, _root_track_id)
push_line で LineFrame に track_id を載せる
  ↓
emit_speak / emit_say / emit_think / _store_memory が
persona._current_pulse_origin_track_id 経由で書き込み時に付与
```

### origin_track_id 付与の判断ルール (確定)

書き込み時に `origin_track_id` を付与するかは「**Track 内の出来事**か、**世界横断のメタログ**か」で判断する。

**Track 紐付け** (origin_track_id を付ける):
- メインライン応答 (`emit_speak` / `emit_say` / `emit_think`)
- ユーザー発話・他ペルソナ発話の `auto_ingest_building_messages` (user / assistant 分岐)
- Track 切替通知 (`UserConversationTrackHandler._inject_track_context`)
- Pulse 中断通知 (`PulseController._record_interruption`)
- LLM ノードからの `_store_memory` 経由 (PulseContext の LineFrame から自動取得)

**NULL のまま** (Track 横断のメタログ扱い):
- `DynamicStateManager.maybe_inject_event_messages` (世界状態変化通知)
- `DynamicStateManager.on_building_entered` の到着通知
- `auto_ingest_building_messages` の host 分岐 (退室・入室の occupancy event)
- メタ判断系 (`line_role=meta_judgment`、scope=discardable/committed)

判断軸: ペルソナがその Track の流れの中で認知する出来事か。yes なら紐付け、no なら NULL。

理由: Track に紐付けると Track Chronicle / last-message timestamp / history filter で拾われる。世界横断の通知 (誰の Pulse 中に届くかは偶然) を Track に紐付けると Chronicle のノイズになる。

### 主要修正ファイル

- `persona/history_manager.py`: `add_message` / `add_to_persona_only` / `add_to_building_only` に `origin_track_id` 引数追加
- `sea/runtime.py`: `run_meta_user` に `origin_track_id` 引数 + persona に Pulse-bound track_id を一時保持 + `_resolve_pulse_root_line` に `override_track_id` 引数 + injector 呼び出し前に `_root_track_id` を解決する順序に変更
- `sea/runtime_emitters.py`: `emit_speak` / `emit_say` / `emit_think` で `persona._current_pulse_origin_track_id` を読んで書き込み時に付与
- `sea/pulse_controller.py`: `ExecutionRequest.origin_track_id` 追加 + `submit_user` / `_record_interruption` / `_queue_for_resumption` で引き渡し
- `saiverse/saiverse_manager.py`: `run_sea_user` に `origin_track_id` 引数
- `saiverse/track_handlers/user_conversation_handler.py`: `MainLineInvoker` シグネチャ拡張 (`Callable[[Optional[str]], Any]`) + `on_user_utterance` で `invoke_main_line(track.track_id)` + `_inject_track_context` で track.track_id 渡し
- `manager/runtime.py`: `_invoke_main_line` クロージャを track_id 受け取り対応 (handle_user_input / handle_user_input_stream の両方)
- `builtin_data/tools/get_building_messages.py`: `auto_ingest_building_messages` に `origin_track_id` 引数 + `get_building_messages` 内で running track 自動解決 (host 分岐は意図的に NULL)
- `saiverse/dynamic_state.py`: docstring に「event_message は Track 横断のメタログなので origin_track_id NULL」を明記
- `tests/test_user_conversation_handler.py`: lambda シグネチャを `lambda *_a, **_kw: ...` に更新

### 動作検証

- `air_city_a/memory.db` で 5/10 15:50 のペア (修正後、当時 user_conversation Track が pending) が両方 `origin_track_id=c6fa3f2d..` で埋まっていることを確認 → メインバグ消失
- ruff: all checks passed
- pytest: handler / history / mixin tests 27 件 pass
- 関連テスト群通過、`test_meta_layer.py::test_state_message_includes_alert_marker` の 1 件失敗は本件と無関係 (メタ判断 v2 文字列テストの古さ、Phase 4 残タスク扱い)

### 残課題 (本件スコープ外)

- 5/9 17:59 以前の汚染データ (origin_track_id NULL の既存メッセージ) は手動掃除案件
- 7 層ストレージタブの削除 UI からメタ判断系汚染ログを除去 (Phase 4 残タスク §6 と一致)
- メタ判断 v2 (commit `e95fa41`) の実機検証 2 回目 (本件と独立)
- 旧 `02_mechanics.md` §「メタ判断の動作仕様」を [meta_judgment_structured.md](meta_judgment_structured.md) への redirect stub に変更
