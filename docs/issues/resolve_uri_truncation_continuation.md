# resolve_uri 切り詰めの継続読み (memopedia tree 等)

**状態**: 未解決 — 設計中 (まはーと仕様詰めの段階)。

`resolve_uri` の tree 読みが 8000 字で切られたとき、truncated の続きを読む手段がない。上限は `resolve_many` 共通経路にある。

## 論点 (4つ、提示済み)

1. 続きの単位 — サブツリー URI 推奨
2. 切り詰め案内の決定論埋め込み — 必須
3. depth 密度制御
4. 適用範囲 — まず tree 型から

## 経緯: resolve_uri 切り詰めの継続読み (2026-08-04 in_flight 台帳より移送)

> 台帳の器の再設計 (次アクション欄=前向きのみ) に伴い、それまで台帳セルに積もっていた経緯の全文をここへ移した。時系列の生の堆積であり、整理はしていない。

実機初日: tree 読みが 8000 字で切られ「truncated 後を読む手段がない」(まはー指摘)。
上限は resolve_many 共通経路。
論点4つ提示済み(続きの単位=サブツリーURI推奨 / 切り詰め案内の決定論埋め込み=必須 / depth 密度制御 / 適用範囲=まず tree 型)。
次: まはーと仕様詰め → 実装
