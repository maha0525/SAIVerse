# SAIVerse コードレビュー台帳

> **目的**: SAIVerse の各サブシステムを、ファイル単位ではなく不変条件と実行経路の単位で継続監査する。レビュー結果を一度きりの所感にせず、再現テストと修正状況まで追跡する。
> **工程はここではなく [監査対応 完了計画書](audit_remediation_plan.md) が持つ** — 「どこまで終わって次に何をやるか」は計画書、finding 単位の消し込み状態は本台帳。
> **運用体制 (2026-07-12 まはー)**: 一次監査の実施は Codex (GPT-5.6 Sol)、設計・実装指揮・指摘の裏取りと修正は Claude (Fable/メティス) の役割分担。レビュー指摘は head のコードで裏取りしてから台帳・修正に反映する。

## 状態

| 状態 | 意味 |
|---|---|
| 未監査 | 体系的なレビューをまだ行っていない |
| 一次監査中 | 境界・不変条件・主要経路を確認中 |
| 指摘あり | 再現可能な指摘があり、修正待ち |
| 回帰固定済み | 指摘を修正し、同型事故を防ぐテストがある |
| 実機確認済み | 回帰テストに加え、実際の運用経路でも確認済み |

## 共通の監査軸

1. **データ保全** — 生ログ・本文・履歴を欠落または無断変形させない。
2. **人格境界** — persona / thread / city / model / line の境界を越えて状態を混ぜない。
3. **自己著者性** — システム生成・ユーザー訂正・本人決定を区別し、本人名義を捏造しない。
4. **原子性と可逆性** — 複合更新は途中状態を残さず、失敗報告と実状態を一致させる。
5. **参照整合性** — 派生物から一次データへの参照を壊さず、削除・移行後も正直に扱う。
6. **観察可能性** — 重要な分岐・失敗・移行結果をログまたは履歴で追跡できる。

## 台帳

| 優先 | サブシステム | 状態 | 最終監査 | 結果 / 次アクション | 記録 |
|---|---|---|---|---|---|
| P0 | 記憶・人格境界 | **指摘あり（一次監査完了）** | 2026-07-17 | 合計 **P1×18 / P2×2**（Atlas編纂P1×3含む）。P1×10 / P2×2は修正・回帰固定済み（snapshot restore staging/rollback化は第二陣 / **native import P1×2+P2×1は柱4=復元/移植分離で消し込み 2026-07-17**: 復元は不一致を書き込み前拒否・移植は明示フラグ+原子写像+transplanted_from provenance・replace単一トランザクション・embedding非生成化。回帰=tests/test_native_import_separation.py 10件）、P1×2はまはー裁定で現状仕様として保留。**まはー裁定(2026-07-16): RemoteProxy転送/visitor heard_byのP1×2はmulti-city凍結スコープへ**。**Metabolism×3=統合工事§6-5で消し込み(2026-07-17)**: 共通排他なし=実行台帳の冪等claim(world DB UNIQUE=プロセス/コネクション横断) / 無効・失敗でもanchor前進=status退役ゲート(disabledの非圧縮退役は設計として引き受け) / TTL失効後の旧anchor touch=call-local化(metabolism_anchor_message_id全廃)。head snapshot model未分離も§6-3bで消し込み。**W4=実行台帳Phase 4でM2残片を消し込み・実機検証待ち(2026-07-21、体験の構造 工程(2)に統合)**: Chronicle生成をepisode整列チャンク(alignment/executor/bands)に世代交代 — M2-a(consolidationの親子別commit→次回同じ子から別親を二重生成、generator.py:553の自認バグ)=親INSERT+子mark_consolidatedの単一tx / M2-b(窓内メッセージまで編纂=§4-1違反)=evict boundary(新anchorより古い範囲のみ) / M2-c(W1のsession_digest行がChronicle材料に混入=digest再圧縮§4-4違反)=CHRONICLE_EXCLUDED_TAGSへ追加 / M2-d(source集合UNIQUE未・claim degrade時素通り)=tx内重複再検査+API生成ジョブへのM1 claim結線(旧:claimを通らない別コネクション入口) / M2-e(dismantle/gap-fill複合更新のtx無し)=経路ごと削除(穴は整列が自然に拾い壁は巻き込まない)。Track Chronicle生成廃止(§11-10)。圧縮七原則を回帰固定(alignment 20/executor 10/bands 18/metabolism 18計66件追加)。**Codexレビュー5巡20件消し込み(受諾20/却下0)**: ①tx内再検査がtx外+LLM挟み→INSERT直前のBEGIN IMMEDIATE原子化(SELECTは暗黙BEGINを張らない) / ②束ね子検査の同原子化(band-only並走防御の本体) / ③帯束ね失敗後plan空で再試行経路消失→dry予測(plan_band_overflow)がbacklogを検出しband-only実行 / ④確認ゲートが帯統合LLMを数えず→dry予測を加算 / ⑤壁打ち切りの目標未達列で小粒親量産→目標到達列のみ束ね / ⑥分裂episodeのrun毎二重転写→transcribed集合を計画全体へ / ⑦CLIのlimitがepisode分断→全量計画+truncate_plan / ⑧キャンセルをcompleted封印→mark_failed+claim_execution乗り換え(failedキー退避=同窓即時再試行) / ⑨estimateが既存帯無視→dry計画共有 / ⑩backfill部分欠損マーカー / (二巡)OperationalError握りがlockedも飲む→in_transactionプロパティ判別 / dry近似と実測の不一致→backfillを計画前へ / 安全弁を試行数カウントへ / (四巡)backfillのread-modify-writeが並走統合を消す→全体を単一IMMEDIATE tx化。走行メモ=[W4](../handoff/2026-07-21_w4_metabolism_ledger_handoff.md)。**W5=Building転記cursor (M8) を消し込み・実機検証待ち(2026-07-21)**: cursorを「連続消費した最大seq」に後行確定 (転記失敗で即停止・次ラウンド再試行)、`building_msg_ref` provenanceで「append成功→marker失敗」のlimbo (停止規律により常に高々1件=次ラウンド最初の転記候補) を冪等修復、`add_to_persona_only` に成否契約+`memory_first` (保存失敗時はin-memoryにも積まない=再試行の二重表示防止)。**副産物の実バグ**: auto_ingest経路の `_mark_ingested` がcontextvar経由でmanagerを引けず**DBのingested_byが一度も永続化されていなかった** (cursor先行と相殺して隠れていた) → manager明示渡しで根治。回帰=tests/test_building_ingest_m8.py 7件 (append失敗/mark失敗/DB lock/再起動で欠落も重複もなし)。これで記憶監査の非凍結findingは全消し込み | [一次監査](../handoff/2026-07-12_memory_persona_boundary_audit.md) / [Atlasレビュー](../handoff/2026-07-12_concept_consolidation_code_review.md) |
| P0 | migration / upgrade / backup | **第二陣修正・回帰固定済み** | 2026-07-16 | 第一陣に加え、mutation前pre-upgrade世代、検証済み独立retention、停止状態DB restore、upgrade chain fail-closed、world snapshot v2/staging rollback、process marker、全updater共通engine、同一argv/City/DB再起動healthまで固定。persona `memory.db`個別backupは独立維持 | [一次監査](../handoff/2026-07-13_migration_upgrade_backup_audit.md) / [第二陣Intent](../intent/audit_second_batch_hardening.md) |
| P0 | 自律行動・判断点・schedule | **指摘あり（W1+W2でP1×8回帰固定）** | 2026-07-20 | 現行HEAD `113567e` で **P1×13 / P2×1**（P1×1は`e3c78cb`で回帰固定済み）。全起動入口、life/budget/slot/Episode、5種finalize、manual、schedule CRUD/発火失敗までcoverageを完了。**W1=実行台帳Phase 1で判断点のP1×5を回帰固定・実機検証待ち(2026-07-19、コミット3f76619/7b2436c/e0ee4ff)**: A2(day_open/close同日重複→claim_executionの境界claim・冪等キーは`judgment.{kind}:{営業日|episode_ref}`) / A7(メタ判断runtime例外→空成功でイベント消失→`_submit_meta_lane`の例外再送出+台帳status(applied/completed)の証跡で成功判定・on_eventはprepared行がdurable queueで回復tick #2がrefire) / A8(finalize保存失敗→成功扱い・二重適用→SAIMemory直書き廃止しmark_applied+outbox化・入口のstatus検査で再finalizeの二重適用封鎖) / A9(post-session completed先行→`complete_with_artifact`で完了+artifact+履歴を単一commit・同一executionのartifact補修のみ許可) / A11(spell失敗をcommitted成功で記録→SpellOutcome化・成功spellのみ/spell正準形・失敗はperception.push(judgment_apply_failure)のシステム名義通知)。回帰=test_execution_ledger/test_autonomy_wiring/test_judgment_points計41件追加。**W2=実行台帳Phase 2で時間割/予算のP1×3を回帰固定・実機検証待ち(2026-07-20)**: A5(作業コマ予算精算の非原子性→コマ発火を`slot.fire`台帳実行で包み予約tx→ハンドラ→精算txの単一commit化・予算二重台帳を`lives[].used_rounds`正典へ一本化・精算失敗時も予約額保持) / A6(done保存失敗でEpisode永久open→精算txが全ロールバックしrunningのまま残り回復tickが`settle_stale_slot`でLLM再実行なしにsettle-close・startup/tick両方で汎用unknown sweepから`slot.fire`除外) / A1(day_open全置換の孤児化→検証先行の`replace_day_plan`に統合し検証成功後にのみcancel→保存→再push・保存拒否時は旧plan/旧予約とも不変でエコーも「既存を維持」に是正)。A5/A6はslots/予算/episode/台帳が全てworld DBのためoutbox不要の単一tx。**Codexレビュー修正(2026-07-20、eeb4ff2後)**: 5件の妥当な指摘を修正 — ①A1の原子性がDB保存/再予約失敗で未達(検証前ValueErrorしか塞げず)→旧plan/旧予約の復元を追加 / ②**ハンドラ中のreplace_remaining_slots(post_sessionが同期発火)で配列前詰め→精算が可変indexで別コマをdone**→コマに不変idを導入し冪等キー/精算/回復をidベース化(Fableの検収漏れの本丸) / ③ハンドラ例外+episode close失敗でunknown+永久open→回復が孤児episodeを閉じる / ④touch_desireが予約前→予約成立後へ移動 / ⑤負used_roundsの過剰返金→非負のみ受理。回帰9件追加。**第二レビュー(同修正への再指摘)で更に2件**: ⑥A1の復元が同じ_upsert成功に依存し継続DB障害で予約消失再発→**保存先行**に再設計(検証→保存→成功後cancel→再push、保存失敗なら旧予約は未cancelで無傷、cancel_scheduled_slotsにcount=追加) / ⑦不変idが予約txに未適用(発火時index無条件fired)→予約txにslot_idを通し同session内でid逆引き、対象消失時は`_SlotVanished`でmark_running前に中断(副作用ゼロ・台帳failed)。回帰3件追加。**第三レビュー(2026-07-20)で更に2件**: ⑧cancel失敗で旧時刻予約が残留するとindexベースkeyが新planと衝突しwatchdogが途絶を見逃し・旧時刻イベントが新planの別コマを前倒し誤発火→**EventScheduler予約keyを不変slot idベースへ移行**(残留予約はid不一致で無害に空振り、watchdogは新コマのkey不在を正しく検出。episode origin_refは回復互換のためindex形式のまま分離凍結) / ⑨二重claimはprepared再利用で同一execution_idを両者にrunnable返却→後発の`_SlotVanished`離脱が走行中の共有台帳を無条件mark_failedで壊し先発の精算がfailed→applied不正遷移で爆発→prepared→runningを条件付き一括UPDATE(`try_mark_running`)の早い者勝ちにし敗者は`_ClaimLost`で全ロールバック離脱、離脱時のfailed落としも`abandon_prepared`(preparedのときのみ)へ変更。回帰8件追加。**第四レビュー(2026-07-20)で更に2件**: ⑩id→index変換直後の再読込で照準喪失(`_fire_slot_by_id`がindexだけ渡し、間の組み替えで別コマ発火)→`_fire_slot`へslot_idを渡し発火に使う配列自身でid解決+全`_update_slot`書き込みに`expected_id`照合 / ⑪全置換で旧id持ち越し(入力が旧コマをidごと写すと予約key衝突が復活)→置換経路は`fresh_ids_from`でidの新世代採番を契約化(消化済み帳簿区間のみid維持)。回帰3件追加。**第五レビュー(2026-07-20)で更に1件**: ⑫`_update_slot`のid照合後の全配列無条件書き戻しが、読み書き間にcommitされた置換を消す(lost update=ペルソナの決定の静かな喪失)→slots_json書き込みを世代CAS化(`_mutate_slots_cas`=読・変異・保存同一tx+`WHERE slots_json=読んだpayload`条件付き更新+最新planで再試行)、予約/精算/回復settle txも条件付き更新+`_PlanGenerationConflict`再試行へ、`replace_remaining_slots`自身もCASループ化。回帰3件追加。**第六レビュー(2026-07-20)で更に1件**: ⑬予約/精算txがmeta_jsonを書き戻すのにCAS条件はslots_jsonのみ→読み書き間の`update_plan_meta`(明日メモ等)を古いmetaで消す→metaは書くときだけSETに含め読んだmetaもCAS条件へ(競合時は最新metaから予算再計算)、逆方向も`update_plan_meta`自体をmeta CAS+再試行化(全独立系meta書き手の共通口)。回帰3件追加。**第七レビュー(2026-07-20)で更に1件**: ⑭増分系(consume_budget/consume_life_pulse/record_judgment_pulse/consume_life_rounds)がCASの外側で古いmetaから完成値を計算→CASが読み直しても古い完成値で上書きし並走の積算が消える(2本並走で2でなく1)→`mutate_plan_meta`新設(読み→増分計算→保存を同一CAS試行の内側に、競合のたび最新metaで再計算)へ増分・引き継ぎ系を全て載せ替え(init_budget_ledger/save_lives/finalizeのevent_memo追記含む)。回帰3件追加。回帰=計約67件、本体スイート2749 passed。**W3=実行台帳Phase 3でscheduleのP1×2を回帰固定・実機検証待ち(2026-07-21)**: A13(dispatch失敗後もcompleted化・oneshot再試行不能→発火を`schedule.dispatch`台帳実行で包み、claim→`try_mark_running`→型付きoutcome(`ExecutionRequest`観測フィールド+`dispatch_schedule_fire`のdict戻り)→「schedule状態前進+mark_applied」のworld-DB単一commit。完了化は成功証跡後のみ、failed(副作用ゼロ)はbackoff 120秒×3、unknownは自動再実行禁止でclaimブロック。判断点のsubmitted=Falseはゲート系reason=settled_skip/例外系=failedに分類) / A12(予約同期失敗の握り潰し・稼働中reconciliation不在→`SYNC_GENERATION`世代列+行一生`INSTANCE_TOKEN`列(SQLite INTEGER PK再利用対策、additive migration+backfill)を追加し、冪等キー=`{id}:{instance}:{occurrence}`・予約closureが(行,occurrence,世代)を運び発火時に照合。回復tick #7に`_reconcile_schedules`(DB正典と予約・`_registered`(行,世代)の照合、60秒で自己回復、occurrenceブロック確認つき)を新設。API応答に`scheduler_synced`明示=`register_schedule`のtri-state化)。**Codexレビュー10巡27件消し込み(受諾24/裁定却下3、第七〜十陣の明細と打ち切り裁定は[W3走行メモ](../handoff/2026-07-20_w3_schedule_ledger_handoff.md)末尾の表)**: ①初回intervalのoccurrence key不安定(unknownブロックを別keyで迂回)→安定sentinel化 / ②非例外の登録失敗がsynced=True→tri-state / ③day_close境界副作用(活動終了通知等)の再試行重複→lives[0].endedマーカーで冪等化 / ④next_fire計算不能の残留予約未回収→reconcileでcancel / ⑤SCHEDULE_ID再利用で旧completed台帳が新scheduleを永久ブロック→INSTANCE_TOKEN+発火時行同一性照合 / ⑥unknown封印が設定変更でも解けない→sentinelを`first@g{世代}`化(設定変更=新しい論理occurrence) / ⑦境界の部分失敗がended封印される+発火結果へ未伝播→下請け3段の成否bool化(冪等段先行・非冪等な通知が最後)+境界失敗は判断前に`submitted=False`で打ち切りbackoffへ / ⑧periodicのprepared回収漏れ(claim→席取り間crashの当日分をreconciliationが翌日分照合で取りこぼす)→回復tickに`_collect_prepared_schedule_dispatch`新設(payload凍結の行・occurrence・世代でrefire、不一致はfailed終端、手動モードskip) / ⑨mark_life_ended失敗の無条件True→裁定付き部分受諾(False伝播は再試行ごとの境界重複を保証するため不採用 — 判断claimが境界より前にあり成功後の再突入はdedupされる。即時リトライ1回+ERROR表明、恒久解=境界通知のoutbox化をW5所掌に明記)。回帰=schedule系テスト約70件追加(test_schedule_manager_ledger/test_schedule_reconciliation/test_schedule_api_sync/test_schedule_dispatch_outcome新設ほか)、走行メモ=[W3](../handoff/2026-07-20_w3_schedule_ledger_handoff.md)。**W5=W3第六陣P2の恒久解 (ライフ境界通知のoutbox化) を実装・実機検証待ち(2026-07-21)**: 境界を kind `life.boundary_{start,end}` の台帳実行に — claim→冪等段(keep-alive cancel/TTL同期)→「started/endedマーカー+applied+通知outbox」を `mutate_plan_meta` の `in_session_extra` で world-DB **単一commit**→outbox_id冪等配送。「適用〜マーク間crashのat-least-once窓」と「マーク失敗の無条件True+即時リトライ」暫定を撤去 (マーカーが書けなければ台帳failed+Falseで正直に失敗、schedule backoffが再試行)。回帰=test_life_phase2.py 新規3件+W3期テスト2件を新契約に書換。**残**: A3(host/City時差→柱6。W8棚卸し2026-07-22で残存を裏取り — autonomy_wiringにtimezone処理なし、host=City同一TZでは潜伏。着手時期はまはー裁定待ち) / A10(完全手動モードgate→W9柱7)。残数の最終照合はW12仕上げで。以後は修正追跡 | [一次監査](../handoff/2026-07-14_autonomy_judgment_schedule_audit.md) / [走行メモ](../handoff/2026-07-19_w1_judgment_ledger_handoff.md) |
| P1 | SEA runtime / Session / head-tail | **指摘あり（P1×6+P2×1修正済み）** | 2026-07-21 | **P1×6 / P2×3** のうち **P1×4 / P2×1 は統合工事§6で修正・回帰固定済み (2026-07-17)**: S1 実model無視のanchor更新=§6-3a(usage.model記帳) / S2 Chronicle失敗後anchor前進=§6-5(status退役ゲート) / S3 配送前last_notified前進=§6-4(outbox確定後にB前進) / S4 Stelis復元漏れ=§6-6a(threadスタック+graph finally巻き戻し+クラッシュ孤児復旧) / S8 anchor並列RMW=§6-3a(session_anchor行upsert)。**W5=S5を完了化・実機検証待ち(2026-07-21)**: `flush_perception_buffer` の成功条件を「message id 取得」に — `_append_message` がDB/embedding例外を内部で握ってNoneを返す「静かな保存失敗」でpendingを全削除する経路を封鎖 (None→pending保持+False、次Pulse成功で一度だけ消費)。回帰2件。引き受ける残差 (走行メモに記録): append-commitとpending delete-commitは別commitのため「間のプロセス死」で1回だけ重複追記がありうる (消失ではない — 単一tx化はstorage層のcommit規律全体の改修になるため見送り)。**W6=S6を消し込み・実機検証待ち(2026-07-21)**: head の fail-closed 化 — required Section (common_prompt/persona_self/core_memory=人格の同一性、`required=True` 宣言) の capture/render/persist 失敗時は `HeadNotReadyError` で LLM 実行前に Pulse を中断 (会話=正直なエラー、判断点/コマ=台帳failed行)。capture失敗の痕跡は None でなく欠損 (`capture_failures` 記帳、既存値は stale-but-real で据え置き)、`ensure_snapshot` が None も欠損と認識し**欠損分だけ** `recapture_missing` で毎Pulse自己修復。`store.save` は成否bool化 (commit失敗/required serialize失敗/required実体値欠損は**書き込まず**False、旧版upsertは版条件付きUPDATE一文で拒否)、`ensure_persisted` が「durable版>=描画版」の単調性を保証するまで LLM 停止。core_memory の内部握り (DB読み失敗→空=コア記憶ゼロの捏造) も撤去。prepare_context は汎用例外も system_prompt 要求時は HeadNotReadyError に格上げ。**Codexレビュー5巡 (受諾9/裁定却下1)**: ①persist_failedフラグの版無視→persisted_version単調前進 / ②欠損再captureがcapture_all(=毎Pulse cache破壊+B全リセット)→recapture_missing / ③検証とrenderの間のTOCTOU→snapshot pin (検証・persist確認・render・compositionを同一オブジェクトで) / ④遅延旧版saveのDB巻き戻り→版ガード+DB版からの採番継続 / ⑤required serialize失敗の欠損行commit→書き込み前拒否 / ⑥recaptureの並行fill上書き→「今も欠損」のみmerge / ⑦採番と公開の別ロック区間(同版異内容)→採番を公開ロック内へ+persist記帳をsnapshot同一性(is)に結合+条件付きUPDATE / ⑧capture失敗でkey省略の行がserialize検査素通り→実体値欠損も保存拒否 / ⑨load_version一時失敗→恒久stale拒否→stale拒否時のDB版+1再採番で自己回復 / (却下1)「pin版自体の保存成否」要求→不変条件は版一致でなく単調性 (不要停止の主張は実装で不成立、裁定は走行メモ)。回帰=tests/test_head_fail_closed.py 27件、走行メモ=[W6](../handoff/2026-07-21_w6_head_fail_closed_handoff.md)。**W8=S7を消し込み・実機検証待ち(2026-07-22)**: messages の正典順序キーを `(created_at, rowid)` に確定 (created_at=意味時刻でインポート履歴尊重 / rowid=同秒内挿入順tie-breaker / NULL created_at=先頭側) — anchor境界 `get_messages_from_id` を「anchor行の(created_at, rowid)以上」のキーセットに (同一秒のevicted prefix再混入を根絶)、履歴・pagination・Chronicle (`get_messages_for_chronicle`)・周辺窓・範囲クリップ (rowid単独窓も正典キーセットへ)・表示系APIの全クエリを同一total orderに統一。W4のevict境界 `created_at <` は保守側(同秒は次回編纂に繰延)で据え置き。**Codexレビュー4巡(受諾3/スコープ外1、明細=[W8走行メモ](../handoff/2026-07-22_w8_time_order_handoff.md))**: ①native export/importの往復でスレッド横断の同秒交互順序が失われる→exportが元rowidを`seq`として運び、importを「器スキャフォールド全件→全thread横断の(created_at, seq)正典順INSERT」二段に再構成 / ②NULL created_at行(native importが許容)のanchor周辺検索が空になる退行→NULLの正典位置を「全実時刻より前」に定義し境界句ヘルパーで5関数統一+`_row_to_message`のint(None)クラッシュ修正 / ③部分import(thread_suffixes/単thread export)で保持スレッドとの同秒相対順が壊れる→seqの元rowidが全て空いていれば明示rowid挿入で完全復元・衝突時は追記に全件フォールバック。回帰=test_time_order_canonical_w8.py 18件(新設)+test_native_import_separation.py 4件追加。**残 P2×1**: S9 token trigger件数gate拒否(→柱8=W10) | [一次監査](../handoff/2026-07-15_sea_runtime_session_head_tail_audit.md) / [W8走行メモ](../handoff/2026-07-22_w8_time_order_handoff.md) |
| P1 | Spell / Tool / Playbook 権限 | **指摘あり（一次監査完了）** | 2026-07-16 | 現行HEAD `113567e` で **P1×5 / P2×2**。うち **P2×1**（`run_playbook`候補名漏洩）は第一陣、**P1×3**（run_playbook city権限 / Aspect権限のtool node迂回 / disabled addon）は第二陣の共通実行時認可gateで修正・回帰固定済み。P1×1（realtime spell）は部分修正（`SPELL_ENABLED`迂回・auto_mode固定が残存）。残: `_`予約namespace、入力contract | [一次監査](../handoff/2026-07-15_spell_tool_playbook_permission_audit.md) |
| P1 | Persona / City / Building 分離 | **非凍結finding全消し込み(W5+W7)・multi-city凍結(入口封鎖済み)・実機検証待ち** | 2026-07-21 | City移動profileへSAIVerse versionを必須化(完全一致要求)。**まはー裁定(2026-07-16): multi-city機能は凍結** — dispatch確定処理未実装/来訪profile署名/RemoteProxy転送のP1×2は修正でなく入口封鎖で対応。**封鎖実装済み(2026-07-16)**: inter-city/persona-proxy API=503+凍結メッセージ、VisitingAI/ThinkingRequest polling不起動、dispatch_persona/return_visiting_persona入口ガード、回帰=tests/test_multi_city_freeze.py。**W5=移動分裂(B1)を消し込み・実機検証待ち(2026-07-21)**: `move_entity` を `move.entity` 台帳実行に — 位置遷移+leave/enterイベント(`insert_building_message_in_session` 内核抽出で移動txに同居)+applied+後処理outbox(dynamic state/addon hooks/game lifecycleの3 target)を**単一commit**、tx失敗は全巻き戻し+False、**commit後はFalseを返さない** (「失敗を返したのにDBは移動済み」の分裂を根絶。後処理失敗はpending/deadの再配送状態)。死んだ`db_session`口を削除。回帰=tests/test_move_entity_ledger.py 8件。**属性更新のservice集約は明文契約(呼び出し側責務)と5呼び出し箇所に跨るためW7柱5のcanonical化と同時に実施** (走行メモに記録)。**Codexレビューで実装直後に発見・修正 (2026-07-21)**: 新設した後処理outboxハンドラ (move.post_dynamic_state/move.post_game_lifecycle) が誘発する再帰的`flush_pending_for_persona`が非再入`_delivery_lock`でデッドロックする欠陥をP1×2で指摘、`ExecutionLedger`に再入検知(threading.local)を追加し一括解消。回帰=`MoveDeadlockRegressionTest`ほか計4件追加。**W7=柱5で単一City内の非凍結findingを全消し込み・実機検証待ち(2026-07-21)**: P1-2(occupancy一意性)=部分一意index `uq_occupancy_active_ai`(起動時に重複修復[参照整合行優先]→CREATE、metadata外=全書換migration対策)+move_entityの**書き込み時仲裁**(close=条件付きUPDATE rowcount/新行=guarded INSERT[NOT EXISTS]/user=条件付きUPDATE — pysqliteはSELECTがautocommitで走るためread-then-write検査はCASにならない、が9巡の中心教訓)。P1-1残(W5委譲)=persona属性+cursor儀式+user stateの更新を`_sync_canonical_location`へservice集約(呼び出し側6箇所撤去、公開は配送前)。P1-3(chat境界)=/chat/send現在地専用化(409)+runtime多層防御(NDJSON契約のJSONエラー)+**発言永続化tx内の現在地検証**(`insert_building_message_with_location_guard`=無変化UPDATEでwriteロック先取り→検証→INSERT単一tx)+utter意味論の正直化(入室=台帳原子/発言=durable前提/再送=client_message_id冪等)+拒否時の添付Item cleanup(3分岐共通ヘルパ、delete_itemの"Error:"文字列契約検査)+撤去補記note(履歴のdangling防止、削除口は新設しない)。P1-6(Region入口)=parent変更の入口同一tx同期+入口の直接再所属拒否+入口所有一意index(レガシー共有はWARN可視化・自動修復なし)。P1-7=Building CITYIDのimmutable化(専用migrationは凍結解除時)+UIセレクタdisabled。P2-1=event_keyを台帳execution_id採番に(同一秒衝突根絶)。P2-2=startup checker分類化(重複修復/無効行close[参照先不明のみ、ロード失敗は保全]/派遣中・capacity警告/pre-start修復の監査引き継ぎ)。**Codexレビュー9巡18件消し込み(受諾18/却下0、明細=[W7走行メモ](../handoff/2026-07-21_w7_location_occupancy_handoff.md))** — read-then-write CAS×3(user/AI close/AI insert)/公開順(sync before delivery)/mirror stale/409の確定現在地/発言永続化の原子性/添付cleanup完全性(Item→履歴→ファイル裁定)/logging関数ローカル化UnboundLocalError/修復のcanonical選択が有効行を潰す複合ケースほか。回帰=test_location_occupancy_w7.py+test_chat_boundary_w7.py計47件(新設)+test_region_admin.py 9件追加。残=まはー実機検証 | [一次監査](../handoff/2026-07-15_persona_city_building_separation_audit.md) / [W7走行メモ](../handoff/2026-07-21_w7_location_occupancy_handoff.md) |
| P2 | API / frontend | **第二陣境界修正・回帰固定済み** | 2026-07-16 | 第一陣に加え、LAN owner認証/CORS/Origin、secret write-only応答、managed path、bounded/streaming upload、model/provider credential接続先束縛、user utterance先行永続化・idempotencyを共通境界へ実装 | [一次監査](../handoff/2026-07-15_api_frontend_audit.md) / [第二陣Intent](../intent/audit_second_batch_hardening.md) |
| P2 | 外部連携（LLM / Addon / MCP / Discord） | **第二陣境界修正・Discord保留** | 2026-07-16 | 実行時中央認可、stream commit point、結果不明MCP callの再送禁止、暗黙paid fallback撤去、provider接続先束縛、Addon HTTPS/full SHA/署名検証を固定。公式署名鍵のpublishは運用側必須作業。Discordはまはー判断どおり対象外 | [一次監査](../handoff/2026-07-15_external_integration_audit.md) / [第二陣Intent](../intent/audit_second_batch_hardening.md) |

## 運用

- 新機能は「実装完了 → コードレビュー → 回帰テスト固定 → 実機検証」の順に通す。
- finding は重大度、根拠行、最小再現、影響、修正後に必要なテストを必ず記録する。
- 修正済みでもテストが無ければ「回帰固定済み」にしない。
- 一次監査は全行読破を意味しない。監査文書の coverage に、見た範囲と未確認範囲を明記する。
