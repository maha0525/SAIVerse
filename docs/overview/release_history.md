# リリース履歴 (Release History)

> **これは何**: 発行済みリリースの記録 (経緯・レビュー・教訓つき) と、**次の版に入れる範囲の正典**。
> v0.3.0 発行後、発行記録が [`v030_release_gate.md`](v030_release_gate.md) (v0.3.0 専用の審査文書) の
> 経緯節に惰性で積まれていたのを、2026-09-05 に独立させた。
>
> **リリース判定の手順**: 「出せるか?」は必ず下の「次の版の範囲」との突き合わせから始める。
> 「次に回すもの」は一括りにせず、**一件ずつ実害の有無と版の約束を添えて**出す (2026-09-05 確立 —
> v0.3.8 で、前日に整理した約束 7 件のうち 1 件しか載らないまま発行した実害から)。
> **版の範囲が会話で確定した瞬間に、この doc へ版名つきで写す。** 会話と台帳は版の約束を運ばない。
>
> **大規模な版**: リリース基準の篩と審査が要る版は、v0.3.0 の門のように個別 doc を立てて
> ここからリンクする (2026-09-05 まはー裁定)。ホットフィックス級はこの doc の節で足りる。
>
> **発行の手続きの型**: hotfix/vX.Y.Z → PR → main (PR はまはーが内容確認してからマージ) →
> タグ vX.Y.Z (Actions が ZIP を添付、ノートは gh release edit で差し替え) → develop へ還流。

## 次の版の範囲

- 2026-09-10 (束の統合開始、まはー指示): **v0.3.12 の統合ブランチ = `hotfix/v0.3.12`**。実装済みで載っているのは (1) 送る量の一系統化と会話以外の提示内容の縮小 ([intent](../intent/presented_context_reduction.md) — 下の持ち越し (a) の 3 点を問いごと解消し、既定を 20,000/60,000 に変更 + プリセット 3 段。**リリースノートに「既定を小さくした。前の感覚に戻すなら多いを選ぶ」を明記する約束**) + (2) GPT-Image 2.5 対応 (flare / sunburst)。追加の実装物もこのブランチに積む。2026-09-11 に (3) 部屋 ID に「/」が入った部屋の付け替えを追加 (N さん報告の「過去ログが未取込」の警告が消えない件の根治、実装・レビュー済み — [issue](../issues/building_id_contains_path_separator.md))。(4) 冷えたウィンドウの見張りの素通しゲート (2026-09-10 実装、翌日本番ログで実機検証合格 — arasuji_levels §14-4)。(5) 提供終了した組み込みモデルの棚卸し (2026-09-10 まはー確定「削除対応はこのバージョンに入れる」。2026-09-11 に全数疎通チェックを終え、まはーが範囲を裁定)。削除 23 件 = 提供元が「無い」と返した 20 件 (OpenRouter 無料 4 / Gemini 2.5 Flash Lite Preview 09-2025 の無料枠用・有料枠用 2 / NVIDIA NIM 14) + 呼んだ名前と答える中身が違っていた Grok 4 0709・Grok 4.1 Fast Reasoning (xAI が Grok 4.3 に転送) + NIM の一覧にはあるが呼ぶと断られる Kimi K2.6。追加 5 件 = NIM の Kimi K3 / DeepSeek V4 Pro 0813 / DeepSeek V4 Flash 0731 / Muse Glimmer 30B (画像対応、考える深さの既定は low) と、OpenRouter 無料の Nex N2.5 Pro (無料枠で画像を読み、構造化出力にも対応するモデル)。Qwen3.8 Max は定義の名前を 0902 版へ差し替え。チュートリアルの自動設定のうち 3 つを生きているモデルへ付け替え (OpenRouter 無料 = 標準 Nemotron 3 Ultra、軽量・記憶の整理・画像要約 Nex N2.5 Pro / NIM = 標準 DeepSeek V4 Flash 0731、軽量・記憶の整理・画像要約 Muse Glimmer 30B / Grok = 全部 Grok 4.3)、プリセットが役割をこなせること (構造化出力を引き受けるモデルがいる・画像要約が画像を読める) を契約テストにし、「設定ファイルが見つかりません」の警告を標準モデル以外の役割 (軽量・記憶の整理・画像/音声/動画) にも広げ、起動時に一度作るのではなく画面が読むたびにいまの設定から作る形にする (選び直せば再起動なしで消える)。NIM の構造化出力がモデル定義の extra_body (thinking の指定) を落としていた既知の不具合も、同じ版で直す。**リリースノートに「削除したモデルを選んでいた場合は、ペルソナ設定と全体設定で選び直す。記憶の整理モデルは選び直すまで整理が止まったままになる」を明記する約束**。持ち越し (b) アイテム個数上限は 2026-09-07 裁定どおり別の feature ブランチ行きで、この版には入れない (2026-09-11 まはー確認)。リリース前の検証体制の強化 (まはーが Codex と洗い出し中) は版の束とは別枠で走る。
- 2026-09-09 (v0.3.11 発行後、まはー意向): v0.3.12 の候補 = (1) GPT-Image 2.5 対応 + (2) 下の 2026-09-07 の持ち越し候補、と定めた。
- 2026-09-07 (v0.3.10 発行後) の持ち越し候補 (v0.3.11 には入れなかった。次の版の候補): (a) 発行後検討と裁定された未裁定 3 点 — 知覚の既定値の調整 (上 60,000/下 20,000 の見直しと発火の相乗り案) / 保存時検査の余裕 10,000 の妥当性 / `perception_high` null のオプトアウトが検査を素通しする穴。(b) アイテム個数上限 — 片付け機能 (Bag の一般ユーザー向け作成手段) と Building ごとの個数設定とセットで**新 feature ブランチ**の裁定 (2026-09-07、[issues/room_items_uncapped.md](../issues/room_items_uncapped.md) に裁定と設計の問いを収録)。

## 発行済みの版の門の記録 (v0.3.9)

- 2026-09-05 (v0.3.9 の門 — 範囲の確定): 2026-09-04 昼に「0.3.8 に入れる 7 件」(水位 4 + 知覚供給側 3) がまはーの指示で束として整理されていたが、v0.3.8 に載ったのは 1 件 (低水位廃止) + 追加裁定 (スペル結果の勘定外し・内訳三分割) のみ — 読み戻し事故の割り込みで残りが飲まれ、リリース判定でも束との突き合わせが行われなかった (経緯と教訓は issues/archive/persona_recall_perception_unbounded.md と memory)。**残り 6 件を v0.3.9 の範囲として引き継ぐ**: (1) 想起に再会の門を配線 + 見出しの名前解決 (2) スペルの付与通知にスキーマ同梱 (3) 部屋の様子は再訪なら差分だけ積み、前回全文が付記で下りるとき最古の同部屋エントリへ移管 (4) 知覚の合計に二水位の上限 (上を超えたら下までまとめて下ろす、境界は一方向の一値) (5) 下ろした跡地に省略の印 (機構名義の一行) (6) 水位の保存時検査「整理を始める量 − 残す量 > 知覚の上限 + 余裕」。順序は供給側 (1)〜(3) が先 (のろけゆきさんの本番実害が出ている)、上限系 (4)〜(6) がその後。**以後、リリース判定は本 doc の版の束との突き合わせを必須手順とする** — 「次に回すもの」は一括りにせず、一件ずつ実害の有無と版の約束を添えて出す。

- 2026-09-06 (v0.3.9 出荷停止): リリース前確認で、部屋の様子の差分の「見当たらなくなったもの」にドキュメント本文の段落が列挙される欠陥をまはーが発見。原因は差分を描画済み文字列の解析で組む設計そのもので、**文字列解析をやめパッケージ単位へ再設計する裁定** (issues/archive/room_state_diff_built_on_string_parsing.md)。PR #282 は draft 化。7 件のうち差分以外の 6 件は生きており、再設計の決着後に束を組み直して発行する。

## 発行の記録 (時系列)

- v0.3.0 の発行までの経緯と審査は [`v030_release_gate.md`](v030_release_gate.md) にある
  (2026-09-01 発行、タグ v0.3.0)。
- 2026-09-01 (リリース後ホットフィックス第一陣、hotfix/v0.3.1): まはーの実機指摘バッチ — アラーム管理から Meta Playbook 選択欄を撤去 (track_user_conversation へ既定化 + 発火時の空値ガード)、「声をかける」「オブザーバー観測値取得」スペルを非表示化、コア記憶タブのモバイル崩れ修理と説明追加、経験・Pulse timeline タブの非表示化。Codex 三巡で消し込み (採用 5 / 却下 2 — 却下は「バインド一覧からの非表示スペル隠し = 有効な設定を隠す方が悪い」と「アップグレードでの空値一括補正 = 発火時ガードで冗長」)、全スイート緑でコミット済み (c2ea3e23〜c0887044)。
- 2026-09-01 深夜 (v0.3.1 リリース発行): ホットフィックス一式を PR #272 (hotfix→develop) / #273 (develop→main) で取り込み、タグ v0.3.1 で発行 (Actions が SAIVerse.zip 20.8MB を自動添付、リリースノートは gh release edit で差し替え)。内容 = 第一陣 (上記) + ユーザー報告二件の根治 (ENABLE_MEMORY_WEAVE_CONTEXT 全撤去 / 死んだ message 参照の sweep / ワールドエディタのページ分け) + 手動整理の背景ジョブ一本化と改名 + システムプロンプト掃除 (静的文言のコード追従つき) + Memopedia 索引デフォルト ON + Chronicle 帯予算のペルソナ設定化 + 診断レポート拡充 + Gemini 自動キャッシュ (実験的・既定 OFF) の初出荷。実機検証 = まはーが全項目を通し、sweep は本番エリスで「取り残し 4 件 → 0 件」、アップグレード鎖 0.3.0→0.3.1 は起動ログで全ペルソナの刻印を確認。残 = 帯ユーザー (konoe) の診断再提出待ち (重複 source_ids は未解決 issue)、Bag アイテム作成経路の判断。
- 2026-09-02 (v0.3.2 リリース発行 — 緊急修正): v0.3.1 に混入した「ペルソナ設定の保存が全ユーザーで失敗する」欠陥を根治して即日発行。原因 = AdminService.update_ai に足した chronicle_char_budget 引数を、API が経由する委譲側 SAIVerseManager.update_ai に足し忘れ (PATCH が常に TypeError → 500)。契約テストが AdminService 直呼びで委譲ホップを素通ししていたため緑のまま出荷された — 委譲と実体のシグネチャ一致 + 受け渡し漏れの機械検査テストを追加。隔離環境の実 API で 500 を再現 → 修正 → 保存・クリアの往復を確認し、まはーの本番でも保存成功を実機確認。hotfix/v0.3.2 → develop 直接マージ → PR #274 (develop→main) → タグ v0.3.2 (Actions が SAIVerse.zip 自動添付、ノートは gh release edit で差し替え)。発見の経緯 = konoe さんの続報 (設定保存エラーの報告)。教訓は memory (feedback_apply_the_discipline_to_the_sibling) に収録。
- 2026-09-02 (v0.3.3 リリース発行 — 利用者報告の修正束): 二つのセッションの成果を hotfix/v0.3.3 で束ねて発行。①konoe さん報告の残り半分「畳んでもコンテキストが減らない」の根治 — 水位判定・整理・表示が保存行だけを数え、送信直前に差し込まれる知覚ブロックが勘定から漏れていた (本番エリス実測: 勘定 149,856 字 vs 実送信 209,031 字で、既定水位 20 万字を実質超過なのに発火しない状態)。まはー裁定「勘定は実際に送る中身の合計」で全経路を統一 (issue は archive の context_accounting_excludes_injected_rows)。②macOS 利用者報告 3 件 (濁点を含む部屋名の過去ログ取り込み / OS 証明書ストアが空で HTTPS 全滅 / 記憶 DB の接続リーク) — Windows では退行なしのみ確認、直ったかは報告者待ち。③まはーの環境で起動不能を起こしたスナップショット (llama_cache 除外・制限時間 60 分)。④UI 2 件と Fable 5.1 の定義。出す直前に VERSION が 0.3.2 のまま・鎖の辺なしだったのを発見して修正 (起動時に鎖が途切れて全ペルソナが失敗するところだった)。hotfix → develop 直接マージ → PR #275 → タグ v0.3.3 (Actions が SAIVerse.zip 添付、ノートは gh release edit)。フルスイート 5127 緑。
- 2026-09-03 (v0.3.4 リリース発行 — 依存関係の lock 導入と SDK 更新): 契機は 9/1 04:18 に更新経路の pip install が NumPy を 2.5 へ上げ、voice-tts の numba が読めなくなって声が丸一日以上無音だった事故。まはー「そろそろやらなきゃダメかな、依存関係全体を洗いたい」→ intent (dependency_management.md) を起草して同日 GO。requirements.txt を意図 (下限と理由つきの上限、== 無し) に、requirements.lock (131 pin、universal) を検証済みの固定にして 7 経路を切り替え、アドオン導入には lock を constraints で渡す。SDK は Web の土台 → google-genai → anthropic → openai → 残り、の順に一族ごとに別コミットで現行へ (httpx → httpx2 の世代交代に追随)。Python 推奨は 3.13 へ (まはー裁定「開発機で見ている版を推奨に」)。レビューはローカル 1 巡 + Codex 3 巡: Intel Mac では lock が入らない (onnxruntime に wheel が無い) を Codex が発見 → 上限と全プラットフォーム検査 scripts/check_lock_platforms.py を新設、更新後の pip check 警告、lock の無い旧版への巻き戻し、など 11 件採用。lock だけの素の venv でフルスイート 5180 緑、本番 venv を同期して再起動 (声が戻った)、OpenAI / Anthropic / xAI の実 API を隔離環境から一回ずつ確認。feature/dependency-lockfile → develop → PR #276 → タグ v0.3.4 (Actions が SAIVerse.zip 添付、ノートは gh release edit)。教訓は memory (feedback_distribution_changes_need_all_platform_evidence: 配布物の変更で Windows の緑を他 OS の証拠にしない)。macOS / Linux の実インストールと、macOS での httpx2 経由の LLM 呼び出しは未検証 (報告者待ち)。
- 2026-09-03 (v0.3.5 リリース発行 — Chronicle 旧形式の引っ越しを再開可能に): macOS の報告者 (旧形式 1443 件) の「一人だけ Chronicle が『まだ生成されていません』/ Chronicle の後に Memopedia が開かない」の根本原因。返答「再起動して Chronicle に触らなければ Memopedia は開ける」と診断ファイルの `UNIQUE constraint failed: memopedia_pages.id` ×10 で確定 — 旧 arasuji_entries → memopedia_pages の移行が途中で止まった状態 (旧コードは行ごとに commit) を想定せず、次からは初手の id 重複で毎回倒れて旧テーブルが永遠に残り、(v0.3.2 まで) 閉じられない接続が Memopedia を待たせていた。修正 = 既にページになっている行を飛ばして残りを写し DROP まで進む、一括トランザクション、再開時の short_id は既存ページ側の最大値も見る、二接続の競合は rollback + 一度の再試行。私が最初レビューを飛ばしてコミットし、まはーの「これレビュー通ってる？」で回したら、ローカルと Codex の両方が short_id の重複を high で挙げた (memory の review-gate に実害として記録)。隔離環境でルシアの memory.db の複製に壊れた状態を再現し、Chronicle 一覧 200 → Memopedia 200 → 旧テーブル消失・31 件・short_id 一意を実走で確認。Gemini 3.8 Flash の定義 2 枚を相乗り。hotfix/v0.3.5 → develop → PR #277 → タグ v0.3.5 (Actions が ZIP 添付、ノートは gh release edit)。報告者の環境での確認待ち。
- 2026-09-03 (v0.3.6 リリース発行 — 未追跡ファイルで更新が始まらない欠陥): macOS の報告者の別件「アプリ内更新が 409、update.sh も Working tree has local changes で止まる」。原因 = 更新前の検査 (`assert_git_update_ready`) が `--untracked-files=all` で、Finder の `.DS_Store` やサポートで渡した診断スクリプトまで「手元の変更」に数えていた (監査第二陣 cc2712e8 で stash 経路を撤去したとき、「未追跡は早送りを邪魔しない」区別も一緒に捨てていた)。まはー裁定「未追跡は無害なので検査から外す。追跡ファイルの変更で止まったユーザーの出口 (強制更新) は別案件」→ issue `update_refuses_on_tracked_local_changes_without_exit.md` (P2、設計待ち)。修正 = 検査を追跡ファイルだけに、拒否時に該当パスを最大 20 件列挙 (`-z` + UTF-8 で日本語名も読める、リネームは両列)、フロントのトーストにサーバーの detail を表示、`.gitignore` に `.DS_Store` 全体追加。Codex 一巡目の high で「無視対象ファイルは git が既定で黙って上書きし、pull では止められない」(v0.3.5 以前から開いていた穴) → `git fetch` + `git merge --ff-only --no-overwrite-ignore` に変更、実験で拒否を確認。アップグレード辺の鎖 (0.0.0 → 現 VERSION) の検算テストを新設 (これまで手元スクリプトの目視)。二巡目は Codex が二本続けて応答停止 (原因不明、ステータスページは正常) したためまはー了承でローカル LLM に代替、指摘なし。hotfix/v0.3.6 → develop → PR #278 → タグ v0.3.6 (Actions が ZIP 添付、ノートは gh release edit)。フルスイート 5190 緑。報告者向けに v0.3.6 までの一回きりの回避策 (.DS_Store 削除 + 診断スクリプトの退避) の返信文を用意 (送るのはまはー)。報告者の環境での確認待ち。
- 2026-09-04 (v0.3.7 リリース発行 — 会話窓の最終防衛ラインと読み戻しの立て直し、ほか): なかみつさんの報告 (ペルソナ konoe さん、v0.3.3) 「水位を下げてあらすじ生成を回したら会話のコンテキストが消えた」の根本原因。診断スクリプトの出力で確定 — 最後の返答から 38 分後にあらすじタブの「生成」を押したとき、要約してよい上限 (`resolve_compile_ceiling`) が「温かい起点 = 生きている窓」と同一視していたため、全起点が冷えた状態では上限が無くなり、生きている窓 51 件を丸ごと一次あらすじにした。その後の冷えた窓への被覆印と冷えた起点の前進は設計どおりに働き、窓は直近一往復のあらすじ 1 枚に。読み戻しは「起点をまたぐ圧縮区間に近づかない」規則で一歩目で止まった。三つの原因 = 上限の温度依存 / 読み戻しの守りが「多すぎる」側しか恐れていなかった (最終検算が「残す量を超えたら全部やめる」= 目標達成で取り消す自己矛盾) / 「窓の会話は残す量を下回らない」契約がテストに無かった。まはー裁定 (09-04) の不変条件 = ペルソナは窓の会話が残す量を下回った状態で発話しない (材料があるかぎり)。修正 = 発話直前の最終防衛ライン `ensure_window_floor` (skip/ok/unmet、unmet はその Pulse の発話を見送る)、上限の候補に現在モデルの起点を温度不問で加える、またぐ区間を最初に丸ごと戻す、最終検算は high と比べて超えたら一段ずつ外す、起点・圧縮区間・履歴の読みを厳格化。同じ hotfix に「残す量の保護範囲を知覚ブロックが食い潰す」(v0.3.3 混入、f288f003)、Chronicle 生成の席取りが 1 時間で「消えた処理」扱いになる欠陥、束ねをチャンク確定ごとに挟む、空応答の再試行、旧形式 `.update_complete` でフル更新に入る欠陥、スレッド一覧の題名表示を相乗り。Codex adversarial review 九巡 (26 件、見送り 3 件は issue に既知として記録)、フルスイート 5378 緑。hotfix/v0.3.7 → develop → PR #279 → タグ v0.3.7 (Actions が ZIP 添付、ノートは gh release edit)。復旧手順 = v0.3.7 に上げてそのペルソナに一度話しかける (LLM は呼ばれない)。なかみつさんの環境での確認待ち。issue `window_floor_and_refill_redesign.md`。
- 2026-09-05 (v0.3.8 リリース発行 — 水位の二水位化と読み戻しの再設計): v0.3.7 の続き。中身 = 低水位の廃止と二水位化・既定値を残す量 40,000/上限 120,000 へ (残す量の主語が会話の行だけになったことを受けた見直し)・水位逆転の実行時クランプ・全体設定の既定層 (PR #280) / スペル結果など機構名義の行を残す量の勘定から外し、送信量の内訳を三分割 (会話・スペル結果・部屋の様子) / 読み戻しを「いちばん新しいあらすじから丸ごと開く」一本に再設計 (09-05 まはー裁定 9 点。予算・broken 停止・「忘れた過去」・残す量の天井・超過時の外し検算を撤去。実機でエリスの窓の開封を確認: 44,473→50,093 字、記録は issues/archive/refill_reads_by_budget_instead_of_arasuji_unit.md) / GPT-6 Astra のモデル定義 2 本 (API 版は 272K 超で全体が倍額になる境界どめ、Codex 版は割増が無く 1.05M。両経路の疎通確認済み) / 更新拒否メッセージに脱出手順 / setup.sh・update.sh の実行権限を Git に登録。レビュー = 読み戻しはローカル LLM 1 巡 + Codex 4 巡、水位はローカル 1 巡 + Codex 3 巡。フルスイート 5,418 緑。hotfix/v0.3.8 → PR #281 → main、タグ v0.3.8 (Actions が ZIP 添付、ノートは gh release edit)、develop へ fast-forward 還流。v0.2 系からの更新案内は「update.bat を 2 回」のまま据え置き (start.bat の自動仕上げに寄せる文面差し替えは、v0.2.29 複製での実機検証が通ってから)。
- 2026-09-05 (v0.3.9 の範囲へ 7 件目を追加): 実機検証ツアー中にまはーが発見した「部屋に戻ると head のアイテム一覧と知覚が全文で二重になる」(issues/archive/room_state_duplicates_head_inventory.md) を、まはー裁定「今直したい」で束に追加し、同日実装 (帰還時 4,099 字 → 48 字)。あわせて未裁定 3 点が発行前に残っていた: 知覚の既定 (上 60,000/下 40,000 — まはーは「省略の頻度が高すぎる。会話と知覚で割れ時点が二つある時点で倍」とし、既定値の調整と発火の相乗り案を検討中) / 保存時検査の余裕 10,000 / perception_high null のオプトアウトが検査を素通しする穴。→ 3 点とも発行後検討として持ち越し (上の「次の版の範囲」)。
- 2026-09-07 (v0.3.9 リリース発行 — 知覚の堆積対策と部屋の様子のパッケージ再設計): 引き継いだ 7 件 (供給側 3 + 上限系 3 + head 全文二重の根治) をすべて搭載して発行。「部屋の様子の差分」は出荷停止 (2026-09-06) を経てパッケージ設計として再実装 (intent room_state_packages.md — 束をキー照合で差分し、メディアはパッケージの持ち物、置き場は知覚一つ)。まはーの実機検証で欠陥 8 件 (往復の堆積 / 配送の逆順 / 遺物残留 / 再会の門の素通し / 置き直しの門の遺物数え / プレビューの埋没 / Close→Open の全文再掲 / 回収×開き直しの重複) を発見して全部根治し、追加裁定 3 件 (役割・指示の独立配送を退役して束の building:prompt に一本化 / 様子は組成の末尾へ = 出来事は到着順 / 出来事の区切りは一出来事一ラベル — world_state 合流の退役) も同日実装。テリスの入室知覚欠落は配達経路の修正群 (未 ready の黙殺を再試行に変えた配達の門ほか) で解消を実機確認 (原因の特定はせず記録)。検収の新規律を確立 — 隔離環境の実パイプラインに実使用の場面の列を通し、消費文面を読者として読んでから渡す (memory: feedback_acceptance_means_reading_the_output)。レビュー = Codex (Luna/high) 14 巡 (再設計まで、実在欠陥約 30 件) + §11 三巡 + Codex×ローカル (Qwopus/NEBULA) 並走 2 戦 (初戦は上位 2 指摘同着、二戦目は Codex high 1 件却下・medium 1 件を純関数契約テストとして採用)。フルスイート 5,680 緑。hotfix/v0.3.9 → PR #282 (まはー確認) → main、タグ v0.3.9 (Actions が SAIVerse.zip 21.3MB 添付、ノートは gh release edit)、develop へ ff 還流。発端 issue 4 本 (persona_recall_perception_unbounded / room_state_diff_built_on_string_parsing / room_state_duplicates_head_inventory / room_state_single_source_of_truth) を archive へ。残 = 入室通知の配達遅延 (38 秒、未起票) と配達の門の同族 4 点 (outbox_delivery_gate_siblings) は実害待ち。

- 2026-09-07 (v0.3.10 リリース発行 — 知覚の信頼性の是正一式): まはーの実機報告 (エリスの知覚に、もう居ない相手の「がいます」とスペルの得喪の対が残る) を発端に、同日中に根治から発行まで走った版。内容 = ①移動時に本人へ積むのは移動の事実だけ (状態の差分は Pulse 開始時の検知が「最後に知らせた状態 vs 今」で計算 — 往復は通知ゼロ) ②「がいます」廃止 + 部屋の様子に同席者の名乗り行 ③スペル・Memopedia・能力の通知を種別ごとに束ね ④再会の想起を Pulse 開始時の同席チェックへ (もう居ない相手の想起と積み重なりの根治、旧想起は回収が自動掃除) ⑤別ペルソナ宛て通知の配達遅延 (実測 38 秒) の根治 — 配達中に来た依頼の引き継ぎ (一般形、Beat 関所には返す時点の判定) ⑥コンテキストプレビューの真実化 (Pulse 頭の検知を読み取り専用で合成、未取得 per_persona スペルの偽剥奪の除外、黒画面の修理) ⑦v0.2 形式の記憶 DB の会話見送りの根治 (別セッション、PR #283)。レビュー = ローカル 3 回 + Codex 七巡 (採用 7 / 却下 9、全件 issues/archive/perception_state_pushed_at_event_time.md の経緯節)。実機検証 = まはーが全項目合格 (往復の静けさ / 退室の即時視認 / 想起の一回きり / 偽剥奪の消失)。文書レビューフローの初運用日でもあり、issue の起草検査 (agy + Opus) とリリースノートの公開前検査を通した。PR #284/#285 → タグ v0.3.10 (Actions が SAIVerse.zip 添付、ノートは gh release edit で差し替え)。フルスイート 5,746 緑。残 = v0.2 記憶 DB の報告者の回復確認。- 2026-09-09 (v0.3.11 リリース発行 — セルフアップデートの根治と、大きな未処理履歴を抱えた環境の救出): 範囲 = 2026-09-08 のまはー確定どおり三本 — (1) psutil 必須化とプロセス生存判定の fail-closed 化 (PR #286) / (2) Chronicle 被覆の穴 第一段 + 実機検証の追加修正 4 件 (実行台帳 completed の退避・畳み材料上限 U/2・まとめだけ残った補修入口・束ねだけの走行の安全弁頭打ち) / (3) スルースの被覆の穴 第一段 (冷間の飛ばしと記録・429 誤分類の撤去とペルソナ単位の小休止・読み戻し規則・採取ジョブ二本立て・手帳の二つの時刻)。発端 = 稟乃さんの夜通し 429 ループ (v0.2 記憶 DB、rate_diag で確定)。レビュー = ローカル LLM (qwopus) 1 巡 (採用 0) + Codex adversarial (Luna/high) 3 巡 (採用 8 / 縁として記録 2 / 棄却 — 明細は intent sluice_coverage_gaps の経緯)。一巡目の目玉はパンマーカー読み取り失敗の fail-closed 打ち消しと、束ね呼び直しループのレート制限貫通。検証 = 隔離テスト 12 本 (稟乃さんの形の縮尺再現) + 隔離環境の実サーバーで冷間スキップの実走 (130 通 13 万字 → 21 秒で応答・skipped cold 1 回・範囲記録 80 通・429 は一回で正直に停止) + まはーの実機 (会話・手帳・Chronicle 補修「5 回分が一発」) + フルスイート 5,884 緑。PR #286/#287 → develop、PR #288 → main (まはー確認)、タグ v0.3.11 (Actions が SAIVerse.zip 21.5MB 添付、ノートは gh release edit — agy の言葉づかい検査を通して硬い漢語と別名の混在を修正)。ノートに「psutil の無い環境は update.bat を一度だけ手で実行」を明記。残 = 稟乃さんの回復確認 (更新案内はまはー) / 次回更新時の UI 更新実機確認 / スルース第二段 (期間選択 UI、設計済み・未着手) の着手裁定。
