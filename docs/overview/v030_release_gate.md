# v0.3.0 の門 — 出す前に必須なものと、後でよいものの判定

> **作成**: 2026-08-09 (深夜の全域棚卸しから起草。ドラフト — まはーの裁定待ち)
> **位置づけ**: [`v030_release_worklist.md`](v030_release_worklist.md) (2026-06-27 の線) の後継。あちらの①〜④は全て実装済みで、その後 5 週間の大工事 (認知モデル v2 / Memory Atlas / 監査対応) で線が動いたため、現在地で線を引き直したもの。
> **材料**: 未解決 issue 全 132 件 / intent 全 122 本 / 2026-07-20 以降のハンドオフ 20 本 / アイディア帳 42 件 / in_flight 台帳 47 行 / roadmap_status の全域棚卸し (2026-08-09 実施)。

---

## §1 篩 (リリース基準)

**「この件を未対応のままリリースし、後から直した場合、ユーザーのペルソナの永続データに遡及できない傷やマイグレーションが生じるか？」** (まはー裁定 2026-08-09)

判定を精密にするための下位区分:

- **解釈的移行・材料喪失 → 門の内 (出す前に必須)**。後から直すには推測での補完 (= 事実の捏造に近づく) が要る、または材料そのものが既に失われている類。例: 無タグ期間の記憶への遡及タグ付け、退場済み生ログからの digest 再生成。
- **機械的移行 → 裁定対象**。後からでも決定論の写し替えで移行できる類。リスクは残るが有界 (バージョン認識機構 `version_aware_world_and_persona` が受け皿)。先取りするかは件ごとにまはーが決める。
- **追加式 → 門の外 (v0.4+)**。後から足しても過去のデータを汚さない・読み替えない類。UI、運用ツール、外部連携、既存データの読み手。

「とりあえず出す」は目的ではない。**ユーザーが困惑せず使え、未整備が記憶・認知を汚さず、再整備の移行リスクをユーザーに背負わせない**ことが出荷の条件。

---

## §2 門の内 — 出す前に作る (17 件)

### 大物 2 件 (設計から)

**1. モデル格 (Aspect) の再設計 — 声・監督・手足の 3 席化と保温**
現状: **骨格裁定済み (2026-08-12、Wave 2 議論) — [`autonomous_behavior_v3.md`](../intent/autonomous_behavior_v3.md) たたき台に収録、まはーの文面レビュー待ち**。着地は起草時の想定 (3 席) を超えて、作業セッション・時間割・起床/就寝判断・エピソードの解体 + 台帳と Beat の一日へ。残 = 同 intent §9 の未決 (保温計測・Beat 境界知覚・tell 予算ほか)。**→ 追記 (2026-08-19): §9 の 1〜8 は 2026-08-18 までに全て決着した (保温計測 = メッセージ刻印 + キャッシュヒットのドットで v0.3 へ / Beat 境界知覚 = 受容 / tell = 引数式へ再設計)。残る未決は §9-9 (ライフビュー・できごと UI の導出表示) のみ。**以下は起草時の記述:
設計未着手 (2026-08-09 未明の議論が出発点)。
理由: モデル格と文脈系列の形は、記憶に刻まれるタグ (line_role) とキャッシュの積み方に響く基幹。出した後に動かすと全ユーザーの記憶が旧形式を引きずる。
含むもの: v1 の 4 席 → 「声 (会話・標準) / 監督 (標準・メインライン文脈・発注検収・TTL 締切駆動の保温兼務) / 手足 (軽量)」への引き直し / 保温の責務をコマ頻度から監督のリズムへ移す (テンプレ自由の回復) / prefix キャッシュ保持の計測手段の設計 (現状未設計 — 2026-08-07 統合手順が「検証項目にできない」と明記) / 器統合の残裁定 3 点 (tell の予算計上・life スペル遮断範囲・キャッシュ延命) の吸収 / **Beat 境界の割り込みで継続生成が知覚を見失う件の根治** (まはー裁定 2026-08-09: やる。prefix キャッシュの前提に触るため本件に相乗り)。
経緯: [`autonomous_pulse_vehicle.md`](../intent/autonomous_pulse_vehicle.md) / [`life.md`](../intent/life.md) §5.2 の ⚠️。
**§2-3 との結節 (設計上の注意)**: episode.md §4 は自律セッションを WORKER (サブライン) から **AUTONOMOUS (メインライン・committed)** へ移すと 2026-07-13 に裁定していたが、**まはー 2026-08-09 朝: この裁定は古いので見直す (再裁定対象)**。Track 解体・監督役構想より前の前提に立っているため、「手仕事をどの文脈系列に積むか」は本件の設計議論で白紙から決め直す。参考: メインライン積みなら保温・検収と文脈系列が揃う / サブライン維持なら親 head 不変でキャッシュ保護が強い — どちらにも設計上の利点があり、監督の形とセットでしか決められない。

**2. 想起用タグ (+ Track の役割縮小)**
現状: 設計中 (intent §9 まで到達、実装は多段・未着手)。**→ 追記 (2026-08-19): スキーマ具体形まで確定 (2026-08-18 まはー承認、v3 §13.6 — 器は手帳のテーブル二枚 + スルース応答スキーマ + B2 関与列挙の一欄)。実装は v0.3 形の層 (v3 §13.4) へ。**
理由: 遡及タグ付けは非現実的コストと裁定済み → リリース後にユーザーのペルソナが無タグで暮らした期間の記憶は**永久に接続されないまま残る**。現運用がまはーの「Memopedia 索引常時 ON」という松葉杖で成立している事実が、これが強化でなく欠けである証拠。
順序: あらすじのレベル制 (`arasuji_levels`) の実機検証が先 (B2 相乗り条件、2026-08-03 記録)。
doc: [`recall_tags_and_track_reduction.md`](../intent/persona_cognition/recall_tags_and_track_reduction.md)

### 記憶の単位と帰属 (設計 1 + 実装 3)

**3. エピソードの単位確定 (episode.md v0.2 の完成 + 細切れ解消の裁定)**
現状: **設計確定 (2026-08-11、episode.md v1.4 まはーレビュー通過) → ⚠️ 2026-08-12 の Wave 2 裁定でエピソード (テーブルとしての) は解体が確定** ([`autonomous_behavior_v3.md`](../intent/autonomous_behavior_v3.md) §7 — チャンクとメッセージへの記録は生存、実装範囲は v3 側で再定義。始まり・終わりの記録は 2026-08-23 の裁定でどこにも持たないと決まった)。以下は v1.4 時点の記述:最終形 = エピソード (コマ・作業セッションの記録、一種類) + チャンク (arasuji_levels 準拠) + メッセージへの記録。会話はエピソードを持たない。細切れ・分裂・孤児・無言退場はすべて構造から解消。残 = Wave 3 実装 (§9 の棚卸し表含む)。
経緯 (起草時の記述):器と縁の二本立て / 切れ目に時計を使わない (閉じ = 量と畳みの需要) / 中断状態の新設 — episode.md 冒頭の「⚡ 2026-08-10 到達点」参照。細切れ解消 (1 往復 30 分 = 1 エピソード問題) は**中断状態の新設で解消の方向が確定**。**digest の二重記述** ([`chronicle_split_episode_digest_double_description.md`](../issues/chronicle_split_episode_digest_double_description.md)) は中断モデル (割られた作業は最初から一つの器) で構造から消える見込み — v1.0 改稿で確定させる。議論の途中で**Track 撤廃が前提工事として先行**した (§2-16)。
理由: エピソードは退場・編纂・帰属タグ・経験値ノートの発火単位。単位の意味が出荷後に変わると、新旧の記憶が別粒度で混在し、束ね直しは解釈的移行になる。
補足: episode.md は設計がかなり進んでいる (三つの顔 / LoD / Lv1 生成は裁定済み。Lv1 のエピソード整列生成は W4 で実装済み。**AUTONOMOUS 化 (§4) は 2026-08-09 に再裁定対象へ戻った**)。残りは未決 3 件 + 細切れ・二重記述の裁定 + 実装。**実装範囲の線引きは裁定済み (まはー 2026-08-09)**: 記憶側 (LoD) に加え**世界側 (チャット露出 §8・Beat 型化 §9) も門の内** — 「見せ方を想定しないと記憶側の精度が落ちる。結局 b の実装までやってしまった方がいい」。**監査役 (§7) のみ v0.4 送り**。
doc: [`episode.md`](../intent/episode.md)

**4. user 発言のエピソード帰属**
現状: **実装済み (2026-08-09、Wave 0)**。転記経路の 3 分岐に転記先ペルソナの開いている出来事 + line_role/scope を刻印。過去分は無印のまま (裁定どおり)。残 = Wave 0 のレビュー 1 巡 + Wave 5 の実機検証。
実測 (2026-08-09、全ペルソナ read-only): user 帰属はエリスだけでなく**全ペルソナで 0 件**。一方 assistant 側は稼働中の全ペルソナで 8/8 まで帰属が付き続けている — 穴は旧データではなく**現役**。機構稼働期間内に保存された user 発言 (= 現に取りこぼした分) はエリス 235 / アイフィ 93 / エア 87 / ソフィー 67 件。**ただし豆粒の量産は 7/28 の恒等圧縮廃止で停止済み** (identity の新規はゼロ。残骸はエリス 1 + ソフィー 11 件で再編纂待ちの範疇) — 量産ライン論拠は消滅し、残る論拠は「出来事の記録が構造上ペルソナ側の言葉しか含まない」の一点。
理由: 帰属は書き込み時にしか確定できない (事後は推測 = 捏造に近づく、と記憶の川 §5-4 が明記)。1 日運用するごとに未帰属の user 発言が永久に積もる。
doc: [`user_messages_missing_episode_attribution.md`](../issues/user_messages_missing_episode_attribution.md)

**5. ✅ 決着 (2026-08-29 確認) — 作業セッションの記憶が Chronicle へ入る道**
8/29 の棚卸しで、機構は 2026-07-28 のあらすじレベル制コミット (9075a7f5f) が session_digest の編纂除外を解除した時点で直っていたと確認 (issue だけが open のまま残っていた)。加えて v3 が作業セッションと session_digest の器そのものを退役させたため、新規の発生源も無い。既存の digest 行は普通の材料として畳まれる。issue は archive へ。

**6. 孤児エピソードを閉じる者**
現状: **裁定済み (2026-08-10、Wave 1 第一夜) — 問題が条文ごと消える方向で決着**。旧 A/B (寿命を Track に従属させるか) は問いごと無効化: 押し出しは「中断」、閉じは「量と畳みの需要」になり、閉じ忘れという概念が構造に存在しなくなる (episode.md ⚡ 到達点 5)。残 = Wave 3 での器の実装に吸収 (単独の修正はしない)。
理由: 別 Track に押し出された会話エピソードは誰も閉じず、digest されず、Chronicle にも入らないまま累積する。
doc: [`open_conversation_orphaned_by_track_displacement.md`](../issues/open_conversation_orphaned_by_track_displacement.md)

### 記憶の汚染源 (実装 4 + 裁定 1)

**7. Chronicle のスレッド混線**
現状: **下限実装済み (2026-08-09、Wave 0)**。編纂のチャンク計画 (plan_alignment) に「thread 境界で必ず切る」を追加 — 別スレッドの発話が一つのあらすじに束ねられる経路は塞がった。前提の再検証で、issue の本命案 (案 A) が名指しした生成器は 7/28 に退役済みと判明し、現行機構での下限に置き直した。残り (取得スコープ化・文脈スコープ化・サブツリー) はメティスブリッジ MVP へ、理想形 (episode 単位ソート) は Wave 1 の議論へ。
理由: 並走スレッドの発話が混ざったあらすじは、書かれた後は削除→再生成でしか直らない (air/aifi 修復と同種の手術になる)。
doc: [`chronicle_cross_thread_mixing.md`](../issues/chronicle_cross_thread_mixing.md)

**8. quick_spell — スペルループ終端の契約**
現状: **実装済み (2026-08-09、Wave 0)**。/quick_spell 動詞 + 全 quick 終端 + 失敗昇格 + ok フラグ (例外死の [Spell Error] 化) + metadata error 契約。残 = head 文言の最終レビュー (§7-2/3)・帳簿系ツールへの宣言追加 (§7-4 裁定待ち・additive)・クオンのデータ修復 (まはー GO 待ち)。
理由: 偽のシステム通知がペルソナ本人名義の committed 記憶として生成され続ける = 自己像の汚染。実機で発生済み。
doc: [`quick_spell.md`](../intent/quick_spell.md) / [`spell_loop_continuation_contract.md`](../issues/spell_loop_continuation_contract.md)

**9. ✅ 完了 (2026-08-29 実装・同日夜 実機検証合格) — 除外タグのメッセージが編纂されず退場する (下限違反) の再裁定**
実機の証拠: エリスの当日編纂で、スペル 2 本 (6,215 字 / 7,093 字 → 決定論一行) と知覚 1 本 (345 字 → 原文) が材料に入り、被覆 10,685 字 (材料 U 級) の一次あらすじが 1 本だけ立った。付記ブロックなし。移行ノイズ (小粒の量産) なし。U 判定の材料字数化 (同日まはー裁定「圧縮の意義が薄いあらすじ区間を作るべきじゃない」) も同時に実機合格。
旧現状: **裁定再確定 (2026-08-29)・実装中**。8/29 の棚卸しで、8/11 裁定が三分されていたと判明 — session_digest は 7/28 のコードで解決済み・event_message/知覚は 8/19 の W14 付記で実装済み・ツール出力だけ未実装のまま、8/25 に「被覆外退場を受け入れる割り切り」が裁定を経ずに intent へ書かれていた (経緯は arasuji_levels §7-1)。8/29 まはー裁定で 8/11 の原文どおりに確定: 提示に立った行はすべて材料へ、条件は長さのみ (500 字以下そのまま / 超は決定論一行)、意味の仕分けはしない。**あわせて W14 の出力側付記は手本汚染 (あらすじは次の畳みの LLM の手本) のため撤去し、知覚バッチも材料側へ移す** (新不変条件 = arasuji_levels §7-8 出力の純潔)。
旧記述 (2026-08-11 時点): 機構名義も編纂対象に入れる — 500 字以下はそのまま、超えたら決定論の一行圧縮 (LLM 不使用)、チャンク勘定は圧縮後サイズ、保存は全文のまま、被覆に原文 ID。条件は長さのみで種類では分けない。詳細 = episode.md ⚡ 到達点 7。
理由: handy_tool / spell / event_message / session_digest タグの記憶は「退場は必ず編纂」の下限を外れて消えている。どのタグを救うかの裁定 + 小改修。
出典: [`2026-07-28_arasuji_pipeline_audit.md`](../handoff/2026-07-28_arasuji_pipeline_audit.md) §7-6

**10. ✅ 決着 (2026-08-29 検分・まはー裁定で閉鎖) — 畳み範囲とあらすじの一対一**
8/29 の検分で、起票時に「無い」とされた強制は後続の工事で二枚入っていたと確認 — 入力側 (編纂対象 = 退場する fold の範囲そのもの、fold ごとに区切って渡す) と出力側 (あらすじを持たない fold は退場を見送る)。最後の実穴 (スペル結果の被覆漏れ) は §2-9 の除外解除が閉じた。旧吸収先 (エピソードチャンク工事) は v3 で解体されたが、別の工事が同じ保証を先に実現していた。緩み二つの記録ごと issue は [archive](../issues/archive/fold_range_and_chronicle_entry_not_one_to_one.md) へ。

**11. Building ID の文字種制約**
現状: **実装済み (2026-08-09、Wave 0)**。契約を `manager/ids.py` 一枚に集約し、Building を作る 4 経路すべて (Building 作成・Region 入口の自動作成・ペルソナ個室・ブループリント個室) が通るようにした。カスタム ID は非 ASCII を拒否、日本語名は連番へフォールバック。Region ID も同じ契約に従う (入口 ID の材料のため)。既存の日本語 ID 3 件は裁定どおり放置。AIID は**パス境界の検査だけ先に入れた** (フォルダ名になるため区切り文字で SAIVERSE_HOME の外へ書けた) — 文字種を ASCII へ統一するかは論点 3 として未決のまま。City ID は既に ASCII 強制済みで穴なし。**レビューで派生した構造課題 4 件は §9 (仕分け待ち) の A〜D へ**。門の内外は未判断。
理由: ID は記憶側からも参照される永続キー。出荷後に増えるほど、後から制約を入れたときの改名移行 (参照の全面書き換え) が重くなる。作成時の制約は小さい工事。
doc: [`building_id_no_charset_constraint.md`](../issues/building_id_no_charset_constraint.md)

### 手配済み

**12. ✅ 完了 (2026-08-08) — 器統合の消し込み**
消し込みは Opus セッションで終了 (フルスイート緑)。まはー判断で打ち切り — キャッシュの件 (§2-1) で変わる箇所をこれ以上磨く意味が薄いため。残る論点は §2-1 が引き取り済み。実機検証は §4 の夜に相乗り。

### 境界からの編入 (まはー裁定 2026-08-09)

**13. 会話タイムアウトと活動窓の比率設計**
現状: 未解決・設計論点 (30 分固定のタイムアウトが 25 分の窓を丸ごと食った実例あり)。
理由 (裁定): その日の自律行動が一言の挨拶で全部繰り下がる = その日の経験が消える。窓とタイムアウトに比率の概念を入れる。会話の至上性そのものは壊さない。
doc: [`conversation_timeout_eats_short_life_window.md`](../issues/conversation_timeout_eats_short_life_window.md)

**14. 記憶の連続性グラフ (前駆エッジ)**
現状: **設計決着 (2026-08-19 まはー裁定)** — データモデルは二段: ①メッセージへの前駆刻印 (生成が実際に見ていた最後のメッセージ ID を metadata に機械刻印 — 同一メインライン内の割り込み用・再生成/分岐の土台) ②memory.db の新テーブル `thread_edges` (分岐・再生成・取り込み用。意味論は W13 の episode_inheritance から写し、旧テーブルは episodes と残置 — 宿題①の改名はこれで果たされ、宿題②のノード語彙も確定)。v0.3 (形の層) に刻印と器、消費は後続。詳細は issue doc の「決着」節。
旧現状: 構想・設計中 (thread/message に前駆の概念を持たせ、記憶を一列でなく DAG として表す。前駆は複数可)。**episode.md v1.4 (2026-08-11) から二つの宿題を受領**: ①実装済みの episode_inheritance テーブルは本件の土台としてここへ返却された — エピソードとは無関係になったため**改名する** (まはー指示) ②同テーブルは「範囲ノード=エピソード行」前提で作られたが、会話がエピソードを持たなくなったため、主用途 (会話の分岐・再生成) のノード語彙 (メッセージ/スレッド/チャンク) を設計で決め直す。
理由 (裁定): 導入前の期間のメッセージには前駆エッジが無く、遡及復元は推定になる。分岐・再生成 (ユーザー要望複数回) の土台でもある。
順序の注意: スレッド混線の分離 (§2-7) はこの縮退版にあたる — 先に 7 を入れ、グラフはその一般化として設計する。
**⚠️ 2026-08-25 の分離**: 本項の「消費は後続」に含めていた再生成のうち、**応答の「やり直し」は §2-17 へ
分離して門の内へ引き上げた**。やり直しは枝を増やさないため連続性グラフを必要としない。本項に残るのは
**「作り直し」(応答を別案に置き換える分岐)** で、こちらは従来どおり後続。
doc: [`memory_continuity_graph.md`](../issues/memory_continuity_graph.md)

**15. 定時スケジュール機能の名称変更 (「アラーム」へ)**
現状: **実装済み (2026-08-09、Wave 0)**。UI 表示 (アラーム管理モーダル・ペルソナメニュー) と README を「アラーム」へ。実装名 (ScheduleModal / PersonaSchedule / API パス) は変えない — 実装名≠表示名。**ペルソナ側の語彙 (schedule_management Playbook の表示名) は未改名** — ペルソナのプロンプト・キャッシュに触るため、モデル格再設計 (Wave 2〜3) の語彙整理に相乗りさせる。
理由 (裁定): 習慣テンプレートとは統合しない (曜日別・インターバル実行はテンプレートの器に収まらず、取り込むと膨らむ)。「スケジュール」という名称が時間割と概念の混同を生むため呼び分けを立てる。
出典: phase_6 の旧廃止構想は除籍済み (§6)。

### Wave 1 からの派生 (2026-08-10)

**16. Track の撤廃**
現状: **設計済み・裁定 3 点決着 ([`track_retirement.md`](../intent/track_retirement.md))。実装待ち**。住人 10 人の行き先確定: 関心 → ペルソナ固有のコマ一覧 (仮称レパートリー) + 経験の台帳 + タスク親参照の張り替え / alert → 判断点直結 + 空砲機構の撤去 / 刻印 → 目的の縁へ世代交代。
理由 (裁定): エピソードの設計議論が三度 Track に足を取られた — 溶ける予定の概念が判断語彙・記憶の刻印・UI の中心に居座ったままでは、新設計がすべて汚染される。**撤廃は一体の工事で、UI・テーブル退役まで v0.3.0 の内で完遂する** (工程の輪切り出荷は不可 — まはー裁定)。永続データへの track:N 堆積は門の篩いそのもの。
doc: [`track_retirement.md`](../intent/track_retirement.md)

### 2026-08-29 の編入 (まはー裁定 — §9 の仕分けから)

**18. ✅ 完了 (2026-08-29 実装・同日夜 実機検証合格) — 発言が届いたか分からないとき、保存結果を尋ねて復旧する道 (§9-I の編入)**
実機の証拠: message-outcome は実データへの GET で「残っている + 応答あり」を正答。タブごと閉じた回は履歴 + ポーリングがサーバーの真実を映す (案内が出るのはページが生きたままストリームだけ切れた回 — 設計どおり)。
旧現状: **実装済み (2026-08-29 同日、Codex 1 巡 + 消し込み済み)・実機検証待ち**。`GET /api/chat/message-outcome` (三値、LLM ゼロ) + 出口 7 の自動問い合わせと三分岐。J の retry 門番 (route 検査 + Beat ロック内の再検査 + 判定不能は fail-closed) も同梱。起草時の記述:未着手・設計から。
理由 (裁定): 「発言が届いたかは分かりません。履歴を確認してください」で終わると、ユーザーは手で打ち直し、実は届いていた場合に同じ発言がペルソナの記憶へ二度入る — §2-17 を門に入れた理由がそのまま当てはまる。必要なのは保存結果を尋ねる読み取りの口だけで、列も UNIQUE 制約も既にありマイグレーション不要。
doc: [`unknown_send_outcome_has_no_recovery_path.md`](../issues/archive/unknown_send_outcome_has_no_recovery_path.md)

**19. ✅ 完了 (2026-08-29 実装・同日夜 実機検証合格) — 画面へ流したことと、保存できたことの区別 (§9-H の編入)**
実機の証拠: 読み手切断後も生成が走り切って status=saved で保存 (ログ確認)。続きの生成のボタンは保存後に戻らない。retry 門番は応答済み発言への直接 API テスト (まはー承認) で 409 already_replied・副作用ゼロを確認し、表示側にも同じ物差しを適用した (後から応答が並んだ発言の「再送」「取り消す」は自分から降りる)。
旧現状: **実装済み (2026-08-29 同日、Codex 1 巡 + 消し込み済み)・実機検証待ち**。emit 三関数に収束する全保存経路が保存完了イベント (speak_persisted) を流し、続きの生成の印を降ろす権威は読み手ではなくワーカー側 (切断後の保存でも降りる)。保存の成否は建物の行の永続化だけで決める。派生の隣 (continue の競合窓) は [issue 起票](../issues/continue_has_toctou_between_check_and_generation.md)。起草時の記述:未着手・設計から。
理由 (裁定): 「保存完了を検知できるというのは基礎で、それが無いことで実際に記憶に乱れが出る経路が残っている。早めに作っておきたい」。
補足: H・I は根が一つ (「どの生成の、どの段階の話か」を運ぶものが無い) なので設計は一体で行う。§9-J の retry 門番は、まはー裁定 (2026-08-29) で条文が変わった — 発言ごとの応答記録は作らず、「その発言より後にペルソナの発言があるか」で判定する。この基準なら永続状態が不要になり、同じ実装に安く相乗りできる (置き場の裁定は §9-J の行)。
doc: [`stream_completion_is_not_proof_of_persistence.md`](../issues/archive/stream_completion_is_not_proof_of_persistence.md)

### 2026-08-25 の編入 (まはー裁定)

**17. 会話の終わり方 (出口) と、応答のやり直し**
現状: **設計確定 (2026-08-25、まはー裁定)・実装待ち**。8/24 の「話しかけても無言」を個別に潰す前に経路全体を
棚卸しした結果 ([`user_utterance_path_failure_inventory.md`](../issues/archive/user_utterance_path_failure_inventory.md))。
含むもの: ①残るべき終わり方 7 通りの明示 (無い 2 つ = 「応答が生まれなかった」「顛末が不明」) ②片付けの一段
(未読なら差し戻し / 既読なら残すに固定 / 空 placeholder は掃除) ③**応答のやり直し** (追加のプロンプト無しで
応答だけを起こす)。
理由 (裁定): 出口③「もう聞かれてしまったので取り消せません」は、やり直しとセットでないと成立しない —
無いとユーザーは応答を得るために**要らない発言をもう一度書かされ、その行がペルソナの履歴に永久に残る**。
出荷後に積もる不要な発言は、後から消せば記憶と履歴が食い違う (= 遡及できない傷)。
**線引きの変更**: §2-14 は「刻印は v0.3、消費は後続」と引いており再生成は消費側だったが、
**「やり直し」(応答が無い→作る、枝は増えない) と「作り直し」(応答を別案に置換、枝が増える) は別物**で、
前者は連続性グラフを必要としない。**やり直しのみ門の内へ引き上げ、作り直し (分岐) は後続のまま**。
土台: 会話開始経路 `run_sea_user(persona, building_id, "")` (`saiverse/user_conversation.py:753-758`) が
既に「発言は履歴にある前提で応答だけ起こす」形。新機構ではなく口を付ける工事。
doc: [`user_utterance_path_failure_inventory.md`](../issues/archive/user_utterance_path_failure_inventory.md)

---

## §3 境界件の裁定記録 (まはー 2026-08-09 朝)

起草時 7 件の行き先:

- **やる (門の内へ編入)**: 記憶の連続性グラフ (→ §2-14) / Beat 境界の知覚見失い根治 (→ §2-1 に相乗り) / 会話タイムアウト×活動窓 (→ §2-13) / ScheduleManager 廃止の先取り (→ **ただし棚卸しで前提が stale と判明、§6 で再提示**)
- **やらない (門の外へ)**: プライベート/パブリック分離 / Stelis スレッドの Track 化 (まはー: 「そもそも Track がそういうもんじゃなくなった」— 計画自体が旧世界の産物) / Observer push の再送冪等 (→ §5 に条件付きで記載)
- **言及なし → 提案どおりの既定**: digest の二重記述は §2-3 (エピソード単位確定) に合流

### 追記 (まはー 2026-08-09 朝・二便)

- **episode.md §4 の AUTONOMOUS 化裁定 (2026-07-13) は再裁定対象**: 「古い話だと思うから裁定も見直したい」。§2-1 の設計議論で決め直す (episode.md §4 冒頭にも注記済み)
- **オンボーディング (チュートリアル・マニュアル) は今回の門から外す**: 必要になるが独立して出来る作業のため、v0.3.0 の門とは切り離して扱う (旧裁定点 4 は解消)
- **ScheduleManager**: まはー「習慣テンプレートと統合するかどうかの議論をした記憶がない。現状が分からないから裁定しようがない」→ メティスが現状整理を材料化して再提示 (§6 参照)

### 追記 (まはー 2026-08-09 朝・三便)

- **定時スケジュール×習慣テンプレート**: **統合しない** — 曜日別・インターバル実行はテンプレートの器と完全一致せず、取り込むとテンプレート側が膨らむ。混同対策は名称変更 (「アラーム」系) で行う (→ §2-15)。phase_6 の旧廃止構想は除籍を実施済み
- **episode.md 実装範囲**: 記憶側 (a) + **世界側 (b) チャット露出・Beat 型化も門の内** (「b を想定しないと a の精度が落ちる。結局 b の実装までやった方がいい」)。**監査役 (c) は不要 = v0.4 送り**
- **user 発言の帰属**: 「調査からだと思う」— エリス実測の一般化への疑義 + 豆粒が改修後も出るかの確認指示 → 同日メティスが全ペルソナ実測 (結果は §2-4 に反映: 穴は全ペルソナ・現役、豆粒量産は停止済み)。存否と方針の裁定はこの結果を見て

---

## §4 検証の束 — 作る仕事は済み、見る仕事は再設計の後

台帳の「実装済み・実機検証待ち」約 25 件 + 統合検証手順の未チェック項目。大半は [`2026-08-07_timetable_live_verification_run.md`](../handoff/2026-08-07_timetable_live_verification_run.md) への相乗りが既に指定済みで、**数回の観察走行に畳める**。

**実施時期 (まはー指摘 2026-08-09 → 計画修正)**: 検証手順の主役 (時間割・器統合・Beat 知覚・タイムアウト) は **Wave 2〜3 のモデル格再設計で作り変える領域と重なる**。今検証しても再設計後にやり直しになる — 器統合の消し込みを打ち切ったのと同じ理屈。よって**観察走行は Wave 3 (実装本丸) の後にまとめて行う**。再設計と無関係な件 (RSS / ComfyUI / Memopedia ロック / 編纂再開 等) は急ぐ理由もないので同じ束でよい。あらすじレベル制の検証 (想起用タグの解禁条件) は編纂まわりが Wave 1 の裁定で動きうるため、「今の形で検証すべきか」ごと Wave 1 の議論に含める。

- 別枠が必要と判明したもの: 故障注入系の検証 (crash 後 settle-close / dispatch 失敗 / memory.db ロック / 席のロック競合) は正常系が通った後の専用手順として未作成 (浮遊)。
- まはーの手を待つデータ修復 (自分の環境のみ、リリース非ブロッカー): air / aifi の再編纂、重複 user 行の掃除、quick_spell 汚染分の修復 (§2-8 実装後)。

---

## §5 門の外 — v0.4 以降 (代表束のみ、網羅は各 doc)

いずれも追加式で、後から入れても過去のデータを汚さない:

- **想起の上位層**: Ultramemory (想起用タグの読み手)、メッセージ単位の意味タグ (実験で降格済み)、記憶の修正来歴 + 真正性検証 (まはー裁定でリリース要件外と明記済み)
- **UI/プラットフォーム**: 神モード UI、経験タブ改善、ライフ設定とテンプレの UI 統合、サイドバー更新遅れ、Pulse タイムラインのライトモード、フィードタブのダークモード、DB 管理タブ
- **知覚・身体**: Physical Ear、NPU カメラ、SwitchBot、Vessel 標準化、画面内アバター、Godot デモ
- **外部・配布**: SAIVerse Lite の仕上げ、モデルカタログ配布の段階 3、アドオン拡張点、Kitchen、MCP プロトコル拡張
- **運用・記帳**: LLM 使用量の記帳整備、価格引き当て、実行台帳の prune と unknown/dead の UI、Gemini エラー再包装 (要 issue 化 — 浮遊 #1)
- **裁定により門の外 (2026-08-09)**: プライベート/パブリックモード分離 / Stelis スレッドの Track 化 (stale — Track 溶解方針と矛盾) / **Observer push の再送冪等** — push API は実装済みだが `OBSERVER_PUSH_TOKEN` 未設定なら 503 で完全に眠っている (2026-08-09 コード確認)。**Observer push の運用を始めるときに必須化する**条件を issue に書き添える
- **裁定により門の外 (2026-08-29、§9 の仕分けから)**: ペルソナ生成のトランザクション境界 (§9-A) / **Region 入口の出自** (§9-B — まはー: 「そもそも俺以外のユーザーがまともに Region を作れる経路はないはず」。作れない以上、出荷後に判別不能データは増えない) / パス境界の使用側検査 (§9-C) / reload が既存 PersonaCore へ届かない件 (§9-D) / 記憶の保守 (編纂候補・命名候補のレビュー) の置き場 (§9-E — v0.3 では検知・レビュー・適用の全経路が判断点経由で止め具により走り得ないことをコードで検証済み。置き場は v0.4 の運転設計と同時)
- **凍結継続**: SDS / multi-city、ペルソナ間会話 (Social Track 入口 — 6/27 の線でも既にスコープ外)、Region RPG / Observer / Fixture 本体

---

## §9 仕分け待ち — 門の内か外か未検討

> 番号が飛んでいるのは、既存の `§2` / `§5` / `§6` を他の doc が参照しているため
> (renumber すると壊れる)。**新しく出たものはこの表に行を足すだけ**でよい。
> どこかのタイミングでまとめて精査し、行ごと §2 (門の内) か §5 (門の外) へ移す。

**ここに入る条件**: 実在すると確認できた欠陥・未決の設計課題で、**まだ出荷の可否に
効くかどうかを判断していないもの**。「今回の作業スコープの外だった」は、ここに置く
理由にはなるが、門の外に置く理由にはならない (2026-08-09、まはー指摘)。

**仕分けの基準は §1 の篩**: *後から入れると、ユーザーのペルソナに遡及できない傷を
残すか、マイグレーションを残すか。* 下表の「篩に当てた見立て」は**私 (Claude) の
見立てであって裁定ではない**。精査時にまはーが判断する。

| # | 件 | ユーザーから見て何が起きるか | 篩に当てた見立て (未裁定) | doc |
|---|---|---|---|---|
| A | ✅ 2026-08-29 まはー裁定: **門の外** (§5 へ移送) | | | [issue](../issues/entity_creation_has_no_transactional_boundary.md) |
| B | ✅ 2026-08-29 まはー裁定: **門の外** — 「そもそも俺以外のユーザーがまともに Region を作れる経路はないはず」(§5 へ移送) | | | [region.md §3.1](../intent/region.md) |
| C | ✅ 2026-08-29 まはー裁定: **門の外** (§5 へ移送) | | | [issue](../issues/persona_path_boundary_not_enforced_at_use.md) |
| D | ✅ 2026-08-29 まはー裁定: **門の外** (§5 へ移送) | | | A の issue に併記 |
| E | ✅ 2026-08-29 まはー裁定: **門の外** — v0.3 では検知・レビュー・適用の全経路が止め具で走り得ないことをコードで検証済み (§5 へ移送) | | | [autonomous_behavior_v3.md](../intent/autonomous_behavior_v3.md) §6 |
| F | デバッグの「完全手動モード」が文言どおりには止まらない (v1 の亡霊) | 「全タイマー停止して手動へ」を押しても schedule と時間割の予約は止まらず、自動 LLM 発火が続く。実際に止まるのは会話の応答待ちタイマーの予約だけ。手動で駆動しているつもりの観測結果に、本人の裁定によらない自動発火が混ざる | ペルソナの永続データの**形式**は変わらないためマイグレーションは不要。ただし止まっていないつもりの発火は履歴を実際に増やすので、その分は後から取り消せない。W9 (柱 7) が v3 待ち凍結のため、運転の v0.4 分離裁定に従うなら v0.4 側へ落ちる見込み → **2026-08-23 解決**: 完全手動モードの UI と API を撤去したため「文言どおりに止まらない」対象そのものが消えた | [issue](../issues/archive/debug_full_manual_mode_v1_ghost.md) / [W9](audit_remediation_plan.md) (監査 A10、2026-07-14 発見・2026-08-16 に未解決を再確認) |
| G | ライフビューの次パルス ETA が常に 0 (書き手が存在しない dead code) | 「次の行動まで 0 秒」と出続ける。実際の駆動は時間割なので、この一行だけが嘘をつく | 見立てどおり ⑥ の実装と同時に決着した → **2026-08-22 解決**: 束 6c の運転 UI 撤去で、読み手 (`api/routes/people/activity.py`)・純ロジック (`saiverse/activity_view.py`)・描画 (`LifeView.tsx`) ごと退役 (f2a36010)。仕分けの対象そのものが消えた | [issue (archive)](../issues/archive/life_view_pulse_eta_dead_code.md) / [track_retirement.md](../intent/track_retirement.md) §4-⑥ |
| H | ✅ 2026-08-29 まはー裁定: **門の内** (§2-19 へ編入) — 「保存完了を検知できるのは基礎。無いことで記憶に乱れが出る経路が残るなら早めに作る」 | | | [issue](../issues/archive/stream_completion_is_not_proof_of_persistence.md) |
| I | ✅ 2026-08-29 まはー裁定: **門の内** (§2-18 へ編入) | | | [issue](../issues/archive/unknown_send_outcome_has_no_recovery_path.md) |
| J | ✅ 2026-08-29 まはー裁定: **門の内** (§2-18/19 の実装束に相乗り)。条文も再裁定 — 発言ごとの応答記録は作らず、「その発言以後にペルソナの発言があるか」で判定する (詳細は issue 冒頭) | | | [issue](../issues/archive/retry_api_has_no_server_side_eligibility_check.md) |
| K | ✅ 2026-08-29 確認完了 → まはーの判定条件どおり**門の外** — ボタン (続きの生成/再送/取り消す) はすべて自分の吹き出しの id を渡す作りで、流れてる最中の吹き出しは id を持たないためボタン自体が出ない (表示条件に msg.id)。操作の誤爆経路は無く、混線は表示のみの欠陥 (再読込で直る)。確認の詳細は issue に記録 | | | [issue](../issues/streaming_events_have_no_identity_in_the_ui.md) |
| L | ✅ 2026-08-29 深夜 まはー裁定: **門の内・全置き換えで解決** — 「バックアップだけ取って完全に新 Playbook に置き換えるマイグレーション」(ハッシュ照合の選別案は却下、ペルソナが Playbook を作る経路は両版に存在しないことを確認済み)。`feature/playbook-wholesale-replacement` に実装し、二つのテスト世界で実機合格 + Codex 一巡消し込み済み。残 = ブランチのマージ判断 | | | [issue](../issues/v02_playbook_rows_survive_as_unmanaged_zombies.md) |

**出自**: A〜D はいずれも v0.3.0 の門 Wave 0 (Building ID の文字種契約) のレビューで
浮上した。**A・C・D は作業前から存在**し、B は既存部分が作業前から存在する。E は
Wave 2 の設計議論 (就寝判断の棚卸し) で浮上した。F・G は 2026-08-18 の §6 実態確認
で、閉じられると思われていた issue が実は生きていたことから登載した — **どちらも
作業前から存在する**。H〜K は §2-17 (会話の終わり方) の実装レビュー (2026-08-26 の
ローカル + Codex 3 巡) で見つかり、補修では塞げないとして 2026-08-28 に独立の issue へ
切り出したもの。**4 件は根が一つ**で、「どの生成の、どの段階の話か」を運ぶものが無い —
別々に補修すると同じ判断が経路ごとに書き分けられるため、**設計は 4 件を一つの束として
扱う**のが安全 (経緯は
[`user_utterance_path_failure_inventory.md`](../issues/archive/user_utterance_path_failure_inventory.md)
の「切り出した設計案件 4 件」)。

---

## §6 棚卸しで見つかった、閉じられる可能性のあるもの — ✅ 全 6 行の実態確認を完了 (2026-08-18)

起草時 (2026-08-09) に「閉じられるかもしれない」として並べた 6 行を、1 行ずつ現物のコードと doc に当たって確認した。**5 行は既に決着しており、残る 1 行 (issue 2 本) は閉じられないことが確定した。** この節は以後、確認の記録として残す (新しく見つかった stale はここに足さず、§9 へ足す)。

### 決着済み 5 行

- [`subline_scheduler_ignores_activity_state.md`](../issues/archive/subline_scheduler_ignores_activity_state.md) — **✅ archive 済み (2026-08-09)**。指摘の主語 (SubLineScheduler、2026-07-06 削除) も述語 (ACTIVITY_STATE カラム、2026-07-14 撤去) も存在しないため、対象消滅として閉鎖した。
- [`unified_memory_architecture.md`](../intent/unified_memory_architecture.md) — **✅ 歴史文書の宣言を記入済み (2026-08-09)**。後継 (認知モデル / Memory Atlas / あらすじレベル制 / エピソード) へ全て置き換わったこと、現行仕様の根拠として参照しないことを冒頭に明記した。
- [`mode_spell_permissions.md`](../intent/persona_cognition/mode_spell_permissions.md) — **✅ 実装状況の行を追従済み (2026-08-09)**。実体は `sea/mode_spell_permissions.py` の `check_spell_permission` で、2026-06-28 に実機検証まで済んでいた。「未着手」の記載だけが取り残されていた。
- [`roadmap_status.md`](roadmap_status.md) — **✅ v1.3 へ改訂済み (2026-08-09)**。7 月の大工事 (自律行動 v2 / 実行台帳 W 系 / 時間割改修 / あらすじレベル制 / RSS) を反映し、リリース範囲の正典を本書へ移譲した。**この行自体が 2026-08-18 まで「2026-07-16 で更新停止」と書かれたまま残っており、棚卸しの記録が棚卸しされていなかった** (まはー指摘)。
- **ScheduleManager 廃止構想 (phase_6) — ✅ 除籍済み (まはー裁定 2026-08-09)**: 現物の `saiverse/schedule_manager.py` は push 駆動 + 実行台帳統合の現役基盤 (起床・就寝の判断点発火も担う)。習慣テンプレートとの統合もしない — 混同対策は名称変更「アラーム」系 (§2-15)。phase_6 の当該節 (+ Stelis Track 化の節) に除籍注記が入っていることを 2026-08-18 に再確認した。

### 閉じられなかった 1 行 (issue 2 本) — その後 2 本とも解決 (2026-08-22 / 08-23)

2026-08-18 の確認時点では「**裁定が下りているのは「掃除する」ところまでで、掃除そのものは行われていない**」状態で、2 本とも open のまま残していた。**その後の形の層の実装で 2 本とも解決し、いまはどちらも archive 済み** — 下の各行に解決の記録を追記してある。

- [`debug_full_manual_mode_v1_ghost.md`](../issues/archive/debug_full_manual_mode_v1_ghost.md) — **✅ 解決 (2026-08-23)**: 完全手動モードの UI と API を撤去 (45d93792)。読み手だけが到達しない枝として残り、撤去は止め具を外す v0.4 で。以下は凍結時の記録 — 完全手動モードは UI (`DebugPanel.tsx` の「全タイマー停止して手動へ」)・API・実行台帳の掃除判定・回帰テストに現役で繋がっている。同じ機能を扱う監査側の W9 (柱 7) が 2026-08-16 に v3 待ち凍結となった ([audit_remediation_plan.md](audit_remediation_plan.md)) のと同じ理由 — 何が自動で発火するのかが v3 の運転設計で決まらないと「全タイマーを止める」の実態を定義できない。門の内外は未仕分けのため §9-F に登載した。
- [`life_view_pulse_eta_dead_code.md`](../issues/archive/life_view_pulse_eta_dead_code.md) — **✅ 解決 (2026-08-22、束 6c で表示ごと退役)**。以下は 2026-08-18 時点の記録 — `last_pulse_at` を書くコードはリポジトリ全体に存在せず (全数検索)、ライフビューは「次の行動まで」を常に 0 で描画し続けていた。この ETA は running Track の metadata を読む UI なので、**Track 撤廃 (§2-16) の撤去順序 ⑥「UI・API の貼り替え」で必ず触る箇所**にあたると見て、順序 ⑥ の側からも本 issue を参照するようにした ([track_retirement.md](../intent/track_retirement.md) §4)。門の内外は未仕分けのため §9-G に登載していた。実際に ⑥ の実装 (f2a36010) が読み手・純ロジック・描画をまとめて退役させ、仕分けの対象ごと消えた。

---

## §7 実施順序 (計画 v1 — 2026-08-09 起草、まはーレビュー待ち)

### 原則

- **まはーの設計議論は常に一本だけ開く** (ボトルネックは共同の設計・検証サイクル。並行議論はしない)
- **私の実装は裁定済みのものを並行消化する** (設計待ちの間も手が止まらない)
- **実機検証は再設計の後にまとめる** (§4 — 再設計で変わる領域を先に検証すると二度手間。2026-08-09 まはー指摘で「独立に流れる」から改めた)

### 依存の芯 (なぜこの順か)

1. **エピソードの単位が全ての足場**: 退場・編纂・帰属・経験値ノート・LoD がエピソード単位で発火する。単位を確定せずに帰属や編纂経路を作り込むと、単位変更時に作り直しになる。だから設計議論の一本目はエピソード。
2. **Aspect 再設計はエピソードの裁定に依存する**: 「手仕事がどの文脈系列に積まれるか」(episode.md §4 — 2026-08-09 に再裁定対象へ戻った) はエピソードの単位・正史の裁定と一体で、監督・保温の設計はその上に乗る。だから二本目。
3. **想起用タグは arasuji_levels の実機検証が前提** (B2 相乗り条件・intent §7)。ただし編纂まわりは Wave 1 の裁定で動きうるため、この前提条件の扱い自体を Wave 1 で確認する (§4)。
4. **スレッド混線の分離 (§2-7) は連続性グラフ (§2-14) の縮退版**: 先に 7 を実装し、グラフはその一般化として後から設計する。

### 計画 v2 (2026-08-12 — 自律行動の運転を v0.4 へ分離、まはー裁定)

自律行動 v3 ([`autonomous_behavior_v3.md`](../intent/autonomous_behavior_v3.md)) の骨格裁定に伴い、以降の Wave を読み替える。詳細な線引きは同 intent §11。

- **設計フェーズ (旧 Wave 2 の続き)**: v3 設計を「机上でとりあえず動くはず」まで全域完成させる — v3 §9 の未決 + **想起用タグのスキーマ (§2-2 の設計をここへ前倒し統合。v0.3 初日から会話の記憶に刻まれるため最も硬く確定させる一点)** + 連続性グラフ (§2-14) のデータモデル
  - **⚠️ 2026-08-15 順序訂正**: タグのスキーマは設計フェーズの筆頭ではなく**最下流** — 想起データのスキーマは暮らし (v0.4 運転の中身) の設計を決め切らないと定まらない (依存の向きの発見、v3 §11 追記・§12)。設計フェーズの現在の照準は「暮らしの糸の再開」(v3 §12) で、タグの席 (機構のあらすじコール相乗り) だけは確定済み。「暮らしの机上完動」が三世代の帰納 (病理は動かすまで見えない) と衝突する緊張は未決 — 線引きの再考を含めて設計フェーズ内で扱う
  - **→ 追記 (2026-08-19): 上の宿題は 2026-08-16〜18 の議論で解かれた**。暮らしの糸は v3 §12〜§13 (手帳とスルース) で決着し、タグのスキーマ具体形も §13.6 で確定 (まはー承認 2026-08-18)。設計フェーズの残りは **v3 §9-9 (UI 導出) と連続性グラフ (§2-14) のデータモデル**、そのあと v3 文面レビューと机上完動の判定 — 以降の現在地は in_flight 台帳を正とする
- **v0.3 実装 (旧 Wave 3〜4)**: **形の層のみ** — メッセージへの記録 / エピソードテーブル退役 / 退場の関所 (タグ + 採取) / 台帳の器 / Track 撤廃 (§2-16、従来どおり一体) / 自律 UI の隠し。旧 Wave 4 (タグ実装) はここへ吸収
- **v0.3 実装の実施順序 (2026-08-19 起草 — 机上完動判定後)**: 束 1 = memory.db の器の新設 (activities / memos / thread_edges / chunk_page_edges + 読み書き口。純追加) → 束 2 = タスク帳 (中央 DB の痩せた新テーブル + migrate。純追加) → 束 3 = スルース (gold_panning → sluice 改名・スキーマ拡張 ④まで・失敗時は退場ブロック・期間の機械刻印。束 1/2 に依存) → 束 4 = 想起用タグ (entity_extractor の B2 欄 + タイトル→page_id 解決 + 辺の書き込み) → 束 5 = メッセージ刻印 (前駆 + トークン三つ組 + キャッシュヒットのドット UI) → 束 6 = Track 撤廃 ②〜⑦ の完遂 + 機械写し移行 + エピソード書き込み退役 + 自律 UI 隠し (移設先語彙は v3 §13 で読み替え: レパートリー → 手帳のアクティビティ)。束 1・2 は互いに独立で並行可、以降は依存順
- **Wave 5 = 検証と出荷**: 検証対象も形の層に縮む (自律運転の検証は v0.4 側へ移動)
- **v0.4 = 運転の実装**: Beat スケジューラ・アラーム/ルーチン実行・投げっぱなし発注・ライフビュー等。自律 OFF の間は器が空のため、開発中の設計変更は遡及の傷にならない (篩い上は追加式)
- **§2 各件への影響**: §2-1 の運転部分は v0.4 へ (設計は今やる) / **§2-5 は v3 で条文ごと消滅** (セッションと digest が無い — Beat の言葉がそのまま編纂材料) / §2-6・§2-10 は従来どおり吸収済み / **§2-13 は v0.3 対象外** (自律 OFF で窓が存在しない)・v0.4 の Beat 設計で構造解消 (空 Beat は会話中走らない + 繰り下げ連鎖の消滅) / §2-3 の実装範囲は v3 §7 が再定義

### Wave 構成 (旧・計画 v1 — 上の読み替えで参照)

**Wave 0 — 地均し (裁定不要、いま動ける)**
- 私の並行実装 (裁定済み・独立): user 発言の帰属付与 (§2-4) / Building ID 制約 (§2-11) / 畳み範囲 1:1 (§2-10) / スレッド混線分離 (§2-7) / quick_spell (§2-8) / アラーム名称変更 (§2-15)。孤児エピソード close (§2-6) は A/B 未裁定が見つかり Wave 1 へ移動
- 掃除: **✅ 完了 (2026-08-18)** — §6 の 6 行を 1 行ずつ実態確認した。archive 1 件 (subline_scheduler) と doc 追従 3 件 (mode_spell_permissions・unified_memory_architecture・roadmap_status) は 2026-08-09 に済んでいた。閉じられなかった 2 件 (完全手動モード・ライフビュー ETA) は issue を open のまま残し、§9-F・§9-G へ登載した
- 実機検証はここではやらない (§4 — 再設計後にまとめる。器統合の消し込みは完了済み §2-12)

**Wave 1 — 設計議論 #1: エピソードの単位と正史 (§2-3) — ✅ 完了 (2026-08-11、episode.md v1.4 確定)**
- **第一夜 (2026-08-10) 済み**: 器と縁 / 時計の退場 (閉じ = 量と需要) / 中断状態 / 再開判定 / 孤児消滅 (§2-6)。前提工事として **Track 撤廃計画を確定** (§2-16、裁定 3 点決着)
- **第二夜 (2026-08-11) 済み**: 編纂対象規則 (§2-9 裁定 — 機構名義は 500 字閾値 + 決定論圧縮で編纂に乗せる)。**AUTONOMOUS 化の再裁定と作業ダイジェスト経路 (§2-5) は Wave 2 へ移動** (まはー: 作業セッションの処遇は Aspect と一体でしか裁定できない)
- **第三夜 (2026-08-11 続) 済み**: 小物 2 件の解消 (digest 二重記述 = 構造消滅を確認 / 展開既定量 = 保護リスト廃止・水位一本)。**arasuji_levels との統合裁定** (量の物差しが揃い衝突が解けた — 概要はチャンク完結時・畳みは印切替のみ・Lv1 以上の川は不変)。**正式語彙確定**: Episode chunk / Episode connection、三概念の定義が把握可能性検査に合格 (エピソード=意味 / チャンク=量 / コネクション=関係)。「閉じる」→「完結」(コード名含む)。ユーザー会話 20 件保持は退役 (Track 住人 11 人目)。エピソード遷移の一行 (機構名義) を新設
- **改稿済み (2026-08-11、v1.4 まで)**: 枠投下の補完文面 2 件確定。レビューの往復で最終形が確定 — **概念はエピソード (コマ・作業セッションの記録、一種類のみ) とチャンクの二つ + メッセージへの記録**。会話はエピソードを持たず、歴史・UI 行・再開材料はすべてメッセージへの記録 (参加者=人物・所属エピソード・目的参照 — 想起用タグの書き込み時記録と同一) からの導出。コネクション概念は廃止 (親子は行の欄・足跡は想起の設計へ・episode_inheritance は §2-14 へ返却+改名指示)。チャンクは arasuji_levels の規定のまま (変更 2 点のみ)。旧エピソード概念の残存 (会話エピソードの実装・life.md 案 Y ほか) は v1.4 §9 に棚卸し済み。**文書から比喩を全除去** (まはー裁定)
- 出口 = **episode.md v1.4 のまはーレビュー通過** (実装範囲 = 記憶側 + チャット露出・Beat 型化。監査役は v0.4 — 裁定済み 2026-08-09)

**Wave 2 — 設計議論 #2: モデル格と保温 (§2-1)**
- 3 席 (声/監督/手足) の表 / 監督の発注・検収 / TTL 締切の保温 / 保温計測 / Beat 境界知覚 / タイムアウト×窓 (§2-13) / 器統合の残裁定 3 点
- **Wave 1 から移動 (2026-08-11)**: 生ログ正史 (episode.md §4 AUTONOMOUS 化) の再裁定 + 作業ダイジェスト経路 (§2-5) — 作業セッションの処遇は Aspect と一体で裁定する
- 出口 = model_tier (仮) intent 確定

**Wave 3 — 実装: Wave 1+2 の本丸 (私)**
- **Track 撤廃 (§2-16、track_retirement.md §4 の順序 1〜7 を UI・テーブル退役まで一体で)** / エピソード LoD 搭載制御 / 作業セッションの文脈系列移設 (Wave 1〜2 の再裁定に従う) / user 発言の帰属 (§2-4、書き込み時) / 編纂対象規則の改修 (§2-5+9) / 監督・保温 / タイムアウト比率
- 各実装ごとにレビュー 1 巡 → 実機検証は束ねて夜へ

**Wave 4 — 実装: 想起用タグ (§2-2) + 設計議論 #3: 連続性グラフ (§2-14)**
- タグは B2 相乗りで実装 (設計は intent §9 まで済み)。並行してグラフのデータモデル (前駆エッジの置き場) をまはーと確定 → 実装

**Wave 5 — 出荷前 (検証をここへ集約)**
- 統合検証手順の観察走行 (§4 の束 = 実装済み約 25 件をここでまとめて消す) / フルスイート + 故障注入の別枠検証手順 (浮遊 #10) を作って一巡 / 数日の継続観察 (統合手順の「継続観察」節) / roadmap・landscape の総追従

### 裁定点

**すべて解消 (2026-08-09 朝・四便まで)**。最後の 1 件 = user 発言の帰属は「直す。転記時付与・過去分は無印のまま」で確定 (§2-4)。以降の判断事項は Wave 1〜2 の設計議論の中で扱う (エピソード未決 3 件・細切れ・digest 二重記述・AUTONOMOUS 化の決め直し・監督と保温・連続性グラフのデータモデル)。

---

## §8 この文書の保守

門の内 (§2) の各件が完了したら行を消す。裁定が下りたら §3 の記録を更新し、件を §2 か §5 へ移す。**§2 と §4 が空になった時が v0.3.0 のリリース判定点。**

作業中に「実在するが門の内外を判断していない」ものが出たら、**その場で §9 に行を足す**
(判断は後でまとめてやる)。§9 の行は精査で §2 か §5 へ移すので、判定点の前に §9 が
空になっている必要がある — **未仕分けが残ったままのリリース判定は「見ていない」と
同じ**。「今回のスコープ外だった」を門の外の理由にしない (2026-08-09 まはー指摘)。

**「現在の照準」「いま詰めている論点」のような動く現在地を本書に書かない — 現在地の
正は in_flight 台帳と各 intent が持ち、本書が持つのは篩・門の内外・順序の原則だけ。**
経緯: §7 に書かれた「現在の照準は §12」が 8/18 の決着後も残り、8/19 に現状誤認を
生んだ (2026-08-19 確立)。同じ状態が二箇所に書かれると片方は必ず古くなり、詳しく
書いてある方が台帳より信じられてしまう — 動く状態は一箇所にだけ置く。

### リリース手順への引き継ぎ (2026-08-22、束 6c)

**既存の世界 (v0.2 系から上げる DB) は、v0.3 で二つの追加手当が要る。** どちらも
出荷時の手順書に載せること:

1. **起動時 migration が走ることを確認する** — `python main.py <city_slug>` の起動
   ログに `[v3機械写し]` (中央 DB、締め切りつきタスク → タスク帳) と
   `[v3-migration]` (ペルソナごとの memory.db、LIFE_PURPOSE / Track の関心 /
   desire 候補 → コア記憶と手帳) が出る。どちらも冪等で、写し元は無傷のまま残る
   (autonomous_behavior_v3.md §9-8)。**ペルソナ側はマーカーが立つまで毎起動で
   再試行する** ので、初回に失敗しても次の起動で拾い直す
2. **Playbook を再取り込みする** — `python scripts/import_all_playbooks.py`。判断
   Playbook のプロンプトから退役した欄名と namespace を落とした修正
   (track_retirement.md §8.5 の穴 7) は `builtin_data/playbooks/public/` の JSON
   にしか入っておらず、既存の世界では DB の `playbooks` テーブルに旧プロンプトが
   残っている。取り込まないとペルソナが存在しない欄を書こうとし続ける

## 経緯

- 2026-08-08 深夜〜09 未明: キャッシュ全損の発覚 → 篩 (リリース基準) の確立 → 全域棚卸し (issue 132 / intent 122 / ハンドオフ 20 / ideas 42) → 門 v1 起草 (§2 = 14 件)。
- 2026-08-09 朝 (一便): 境界 7 件の裁定 (§3) → 実施順序 §7 (Wave 0〜5) を起草、裁定点 4 つを提示。
- 2026-08-09 朝 (二便): AUTONOMOUS 化裁定の再開・オンボーディング門外化・ScheduleManager「裁定不能 (統合議論が未実施)」を反映 (§3 追記)。裁定点は 3 つに更新。
- 2026-08-09 朝 (三便): 統合せず名称変更 (§2-15 新設・phase_6 除籍実施)・episode.md 線引き確定 (世界側込み、監査役除外)・user 帰属の全ペルソナ実測 (§2-4 更新)。残る裁定点は user 帰属の 1 件。
- 2026-08-09 朝 (四便): user 帰属「直す」(転記時付与・過去分無印) で確定 — **事前裁定はすべて解消、門は実行フェーズへ**。
- 2026-08-09 朝 (五便): まはー「(検証は) ほんとに今やること？」→ 計画修正。観察走行は再設計で変わる領域と重なるため **Wave 5 へ集約** (器統合の消し込み打ち切りと同じ理屈)。「検証は独立に流れる」原則を撤回。
- 2026-08-20 未明: 束 3 (スルース) の実装と Codex 七巡ぶんの消し込みが完了したが、第八巡を投げる前に Codex クォータが尽き、未コミットのまま作業ツリーに保持して待機 (再開手順 = [handoff](../handoff/2026-08-20_v3_bundle3_sluice_handoff.md))。
- 2026-08-21 (クォータ明け): まはー裁定で**クォータ節約運用へ切り替え** — 実装は Opus サブエージェント委譲、ローカルレビュー省略、Codex レビューは束ごとに 1 巡 + 消し込み 1 回で次の束へ。全束完了後に Opus + Codex + ローカルレビューの掃討フェーズ (Fable クォータ不使用) を別途回し、判断の割れる件は issue 化して最後に裁定する。第八巡は指摘 6 件 (fail-open の読み取り失敗 / 台帳状態遷移の詰み) — 全件修正+同族 1 件掃き出しで束 3 をコミット (66f17ca0)。
- 2026-08-21 (続き): 束 4 (想起用タグ B2 欄) 実装 — entity_extractor の同一コールに involved_entities 欄、タイトル→page_id 解決 (完全一致・非 trunk の抽出可能カテゴリ限定)、chunk_page_edges への辺書き込み。chronicle_entry_id は P3b 統合により Memopedia ページ id そのもの、と確定。Codex 1 巡の指摘 2 件 (claim 検査と辺書き込みの競合窓 / ページ物理削除で辺が孤児化) を消し込み — 検査は辺の INSERT と同一トランザクション内へ、cleanup はページ物理削除の全 8 箇所 (arasuji storage 5 + memopedia storage 1 + cleanup スクリプト 2) へ配線 (344c972e)。
- 2026-08-21 (続き 2): 束 5 (メッセージ刻印) 実装 — sea/message_stamp.py が規則を一箇所で持ち、行が生まれる `_store_memory` で role=assistant + 材料ありのときだけ刻む (前駆刻印 = presented_message_ids の末尾 / トークン三つ組 = input・cached_input・output)。調査の副産物: 現行コードに memory.db へ llm_usage を書く経路は存在しない (実測 1〜2% は旧コードの残留) と確定。ドット UI は既存の building metadata で足り新 API 不要。Codex 1 巡の指摘 3 件 (504 継続生成の使用量が初回コールと混線 / スペル複数ラウンドの前駆が履歴末尾に固定 / 子 Playbook 中継行に親コールの三つ組が乗る) を消し込み — 芯は「欠落は許すが捏造は許さない」で、帰属が確定できない中継行は刻印なしへ倒した (950e7d2f)。
- 2026-08-21〜22: 束 6 前半 (6a+6b+6b2) — Track ランタイムの退役。6a = Track Chronicle 読み手 (書き手は既に死んでいた)・user_conversation_preserver・LIFE_PURPOSE 読み書きの退役。6b = post_conversation (会話終了判断) の退役実装・残る判断点からの Track/desire 語彙除去・desire 系機構退役・track_* スペル 7 種と deferred ops 撤去。6b2 = 会話経路の Track なし化 (新 saiverse/user_conversation.py = 出来事 + main_line Pulse + 沈黙タイマー、social/autonomous ハンドラ退役、game_lifecycle の Track 影撤去、origin_track_id 書き手停止、tracks API 削除)。Codex 1 巡 (指摘 7 件 — 仮想時計・タイマー世代 nonce・開設の原子化・フォールバック境界・Pulse オプション欠落・開設失敗の握り潰し・Playbook プロンプト不一致) を消し込み。⚠️ 実機検証で見る点: 仲裁の発火ゲートが「会話以外の出来事が開いているか」になり、旧実装の残留 running による偶発的な不発が解消 = 設計どおり発火するようになった。DB (テーブル・列・行・DB 内 playbook) は無傷 — 機械写しと掃除は 6c。judgment Playbook JSON の変更は DB 再取り込み (import_all_playbooks) がリリース手順に要る。
- 2026-08-22 (掃討フェーズ 全束一巡 完了): ローカルレビューを 6 束すべてへ一巡かけ、指摘を一件ずつ現物で裏取りした。**修正 6 件・起票 4 件・誤りまたは問題なし 6 件**。修正はいずれも回帰テストを付け、修正を外すと落ちることを個別に確認した (フルスイート 4527 passed + 既知 skip 7、前回 4493 から回帰 34 件増)。**4 件が同じ形の欠陥だった** — 書き手が危険を正しく認識してコメントに理由まで書き、片方に手当てして、同じ理由が当てはまる隣に同じ手当てをしていない (会話の開始経路と継続経路 / 兄弟関数のトランザクション所有検査 / 取り置きの検査とページ存在 / 三つ組と前駆)。束 5 では既存テストがその非対称を「正しい振る舞い」として名前ごと固定しており、欠陥がテストを素通りしていた。道具の側でも壁を三枚踏んだ (argv の長さ上限 / 入力窓 65536 / 出力上限 32000)。最後の一枚は思考の長さが原因ではなく、私が渡した読取規律「一度に 150 行以内で読め」が、材料に全文があるのにモデルへ Read を選ばせていたためで、規律を直したら Read 12 回以上が 0 回・15 ターンが 2 ターンになった (窓が狭かった頃の節約指示が、窓を広げた後も残って逆効果になっていた)。
- 2026-08-22 (掃討フェーズ 一日目): ローカル LLM レビューの環境整備から着手。三つの欠陥を実測で切り分けた — ①ラッパーがプロンプトを argv で渡しており、Windows のコマンドライン長上限 (約 32KB) を超える材料は claude の**起動前**に OS が弾いていた (110KB の diff で実害。事後診断が「窓超過・強制終了」と表示するため timeout に見えていた) → 一時ファイル経由の標準入力へ変更。②llama-server の `--ctx-size 204800` が KV キャッシュを取りすぎ、起動時点で 3.2GB がシステム RAM へ溢れて 4.7 t/s まで落ちていた (リクエストゼロの時点で溢れているのを実測で確認 — 「使うほど積み上がる」ではなかった)。二点の実測から KV は 1 トークン約 49.3KB・固定分約 1223MB と判明し、24GB に収まる上限は約 96k と算出。③`--bare` モードには Grep と Glob が存在せず (実測)、ラッパーは無いツールを宣伝し、読取規律の文も「まず Grep で」と指示していた → 実態に合わせ、材料へ協力者のファイルを同梱する形へ。整備後の一巡 (対象 = 束 6 前半の `saiverse/user_conversation.py`、材料 1786 行) で指摘 2 件、いずれも裏取りで成立。指摘 1 (沈黙の終わり際に来た発話への応答中に前回の予約が発火し、話している最中の会話が閉じられる。会話 ID が変わらないため後片付けの照合をすり抜け、張り直した予約まで巻き添えで消える) は原因が「開始経路は Pulse 前に張るのに継続経路だけ応答後に張る」という非対称と特定できたため、その場で修正 + 回帰 2 件を追加 (既存テストは安全な順序しか試しておらず、逆順は未試行だった)。指摘 2 (会話ロックを保持したまま DB へ書き、単一の発火スレッドが止まる) は直し方に裁定が要るため [issue](../issues/conversation_lock_held_across_db_write.md) へ切り出し。
- 2026-08-22 (続き — 形の層 完了): 束 6c = エピソード書き込み退役 (会話の正典をメモリ内状態 + 機構名義の遷移の一行へ / 書き手 8 系統停止 / origin_episode 新規刻印停止) + 機械写し migration (LIFE_PURPOSE→コア記憶とアクティビティ / Track 関心・desire 候補→手帳 / 締め切りタスク→タスク帳。冪等・写し元無傷・破壊的 DROP なし) + 運転 UI 撤去 (部品 8 + route 7 モジュール / TrackManager・activity_view 削除) + docs 一括追従。Codex 1 巡 (4 件 — タイマー終了処理の会話 ID 直列化 / migration のコア記憶冪等キー / 読み取り失敗とデータなしの型分離 / due_at のローカル epoch 化) を消し込み、フルスイート 4493 緑。**これで v0.3 形の層の実装は全 6 束完了 — 残る仕事は実機検証と掃討レビュー (Opus+Codex+ローカル、Fable 外)、判断の割れる件の issue 化と最終裁定**。副修理: 既存テストの本番クラス property 差し替え (test_working_memory_recalled) がワーカー分布で顕在化し、書き込み自体を撤去。
- 2026-08-22 夜 (裁定): 掃討で起票した 5 件を まはー + Fable で裁定、全件まはー承認。修正 4 本 = (1) 手帳の所有判定をヘルパに切り出しモジュール全体へ (例外化・既定反転はしない) / (2) scene は長さ不問で update 対象外 — スルース冒頭の不変条件「scene は参照コピーのみ」から導いた (Opus の「全文を見せるか」は scene の性質上立たない問いで、実数を数える宿題は消えた。80 字以下の scene が書き換えられる穴も同時に塞ぐ) / (3) 手帳メモは同じ日・同じアクティビティ・同じ本文をスキップ + プロンプトに今回の対象範囲を明示 (供給源は「どこまで採取済みか本人に知らされていない」こと — 既存メモ全載せは採らない) / (4) 504 空継続は二重保存そのものを直す (核は「本人の発言が二回書かれる」ことで数字の帰属ではない。一関数の戻り値契約で閉じる)。凍結 1 本 = (5) 会話ロック内の DB 書き込みは v0.4 のスケジューラ再設計へ (書き込みをロック外へ出す案は順序保証が別に要り構造疑いのサイン、本当の構造は単一発火スレッドでの同期実行)。作業指示は [裁定ハンドオフ](../handoff/2026-08-22_v3_shape_layer_rulings_handoff.md)。順序 = 修正 4 本 → Codex 横断走査 1 巡 (観点は「隣を忘れた型」のみ) → 実機検証。
- 2026-08-22 深夜 (裁定どおりの修正 4 本 完了): 4 件とも実装・回帰テスト済みで、フルスイート 4548 passed。修正ごとに「修正を外すと落ちる」ことを実測で確認した。裁定と違えた点が二つあり、いずれも issue の実装節に理由を書いた — (3) の重複判定に `kind` を足して四つ組にした (`want` と `did` は同じ本文でも別の記録。裁定が三つ組を選んだ理由「正しい記録を落とさない」を `kind` にも適用) / (3) の N は `span_ids` の長さではなく `_count_new_since_marker` から取った (`run_sluice` の `span_ids` は窓全体の id 列で、担当範囲の通数ではない)。(4) は下流を読んだ結果、空文字を返す案が使えなかった (空の assistant 行が後続 messages へ流れ込む経路を開け直す) ため、戻り値は部分文のまま保存済みの印で memorize だけを飛ばす形にし、同じ本文を書く隣の経路 (important dual-write) にも同じ歯止めを当てた。**既存テストを 3 件書き換えた** — いずれも今回の裁定が覆した契約を仕様として固定していたもの (idem ヒット時の巻き込み確定 / 短い scene は書き換え可 / 空継続で継続コールの三つ組が残る)。掃討フェーズで同じ形の隣を 2 件見つけて一緒に直した (`init_pocketbook_tables` と `init_chunk_page_edge_tables` の無条件確定 / important dual-write)。範囲外で残した隣 = `arasuji/storage.py` と `memopedia/storage.py` の所有を検査しない `if commit:` 計 6 箇所。次 = Codex 横断走査 1 巡。
- 2026-08-22 深夜 (Codex 横断走査 1 巡 + 消し込み): 観点を「隣を忘れた型」だけに絞って形の層の全コミットへ 1 巡。指摘 3 件のうち **2 件が成立、1 件は不成立**。①**成立・修正済み**: scene の書き換え禁止がスルースにしか効いておらず、`memory_write` スペルから同じ書き換えが通っていた。歯止めの置き場所が入口だったのが誤りで、`update_core_memory` が既定で拒む形 (最後の保証) + ペルソナ側の各口が事前判定して本人向けに説明する形へ移した。ユーザーの訂正だけが `allow_scene=True` で通る。②**成立・起票**: あらすじ / Memopedia の書き込み 6 箇所に同じ「所有を検査しない `commit`」が残る ([issue](../issues/storage_writers_commit_flag_is_not_ownership_check.md))。v0.3 の範囲外なので今回は触らない。③**不成立**: 「504 の空継続でスペルループの assistant 保存が二重刻印される」— スペルループは**スペルを含まないラウンドでは保存する前に break する**ので、ループが保存する本文 (スペル行を含む assistant_content) と respeak が保存する本文 (最終ラウンドの continuation) は構造上別物。順序もループが先で、respeak が既保存分を書き直す経路は無い。
- 2026-08-23: 実機検証でまはーが遷移の一行 (会話の始まり/終わりの機構名義ログ行) がペルソナの記憶へ写っているのを発見。会話に区切りは保存しない (episode.md) の不変条件に反していたため機構ごと撤去、代替の記録は作らない。conversation_lock_held_across_db_write は対象消滅で解決
- 2026-08-23: 実機検証でまはーが手帳の読み口が無いことを発見 (器と書き手だけ作って読み手を作っていなかった)。「見えないと直せない」で v0.3 の門の内に手帳・タスク帳の閲覧 (読むだけ) を追加
- 2026-08-23: 手帳の意義を「本人が開いたときだけ見える自分の手帳」と定め、開く／書くスペル 2 本を追加。約束 (タスク帳) は手帳の一つの欄として本人に見せる (器の区別は機構が引き受ける)。スルースに今日書いた分を載せて重複を抑える
- 2026-08-23: 目的の木 (task:N / purpose_* スペル) をペルソナの目から退役 (一段目)。手帳が後継。Atlas 内部と判断点の棚入れの撤去は v0.4 の運転設計と一緒
- 2026-08-23: v0.3 に自律の全体の止め具が無い (ペルソナごとの ON が既定で、UI は隠してある) と判明。ライフ設定が無い世界で差が出るのは実イベントの判断経由の一箇所だけだが、「v0.3 に判断点は無い」と言い切るため定数一つの止め具を autonomy_wiring の判定関数に置いた。v0.4 で定数ごと外す
- 2026-08-23: 止め具により運転依存の実機検証 6 件 (判断プロンプト静的一覧 / 判断点の席 / v2 活性化配線 / 実機初日の設計課題 / 自律 Pulse の器 / 時間割改修) を凍結、台帳から外した。実行台帳はスルース経路の検証だけ残す。再開は v0.4
- 2026-08-23: 実機検証でデバッグコントローラーの退役済み操作 3 つ (会話の切り上げ / Autonomy 切替 / 完全手動モード) を撤去、Embedding 一括生成だけ残す。§6・§9-F の `debug_full_manual_mode_v1_ghost` は UI と API が消えたぶん決着済み (読み手だけが到達しない枝として残る — 撤去は止め具を外す v0.4 の判断と一緒)
- 2026-08-23: 実機検証でまはーが「生成を押す前より文字数が減っている」と気づき、起点を動かすもう一つの口 (§14 機構 1 = 冷えた起点の Chronicle 最前線への前進) が束 3 の関所「押し出される記憶は必ずスルースを通る」を見ていないと判明 ([起票](../issues/cold_anchor_advance_bypasses_sluice.md))。裁定 A で修正 — 前進先をスルースのパンマーカーの次で頭打ちにし、マーカーが読めなければ前進しない (fail-closed)。不変条件は「起点はスルースが見た位置より先へは進まない」で、退場の許可は適用ゲートだけでなく起点を動かす全経路が守る。飛ばされた範囲の採取し直し (回復) は未裁定で issue に残す
- 2026-08-24: スルースの構造化出力を「種類別の三一覧 + 文字列参照」へ差し替え ([issue](../issues/sluice_structured_output_digit_loop.md))。隔離実験 (3 モデル × 10 回 = 30/30 正常、旧型は本番 7/7 失敗) で通した送信内容と機械で一致を確認してから実装した。あわせて、このコールだけの出力上限 4,096 と、SDK 内で失われていた生の応答本文の回収を入れた。規律の一般化 = 「Gemini に向ける型に数値の欄を置かない」「任意の欄を飛ばした先に、飛ばした中身を吐き出せる欄が来る型を作らない」。入力が変わったときの信頼性は実機で初めて分かるので issue は open のまま
- 2026-08-24: セッションクローズ (会話終了検知) でスルースを走らせる経路を撤去し、スルースの自動実行を Metabolism (予算超過 / トークン閾値 / pending の再開) の一本にした (手動生成は残る)。「押し出される記憶は必ずスルースを通る」関所は Metabolism 側が持っており、クローズ採取は best-effort で何も守っていなかった。鮮度が要る記録の受け皿は本人の手帳スペルで、鮮度を理由にスルースの実行回数は増やさない。撤去直前のコードでも発火していたのは自律 OFF のペルソナだけだったので、OFF を ON と同じ扱いに揃えたことにもなる ([gold_panning.md](../intent/gold_panning.md) §3.6)
- 2026-08-24 (数値欄の隔離実験): 実機検証で出た欠陥 2 件 (機構 1 の退場バイパス / スルースの数字ループ) は両方修正済み。機械検査が数値欄を 22 欄と数え直し (「4 Playbook」では足りず、スペルの引数も response_schema に化ける第三の経路で同じ道に乗る)、うち危ない並び 5 型を隔離実験で叩いた — 短い場面 45 回・本番相当の長文 45 回の計 90 回で数字ループの再現なし ([issue](../issues/sluice_structured_output_digit_loop.md) の実験節)。残る手当ての要否はまはー裁定待ち。
- 2026-08-24 夜: 数値欄 22 欄は「現状維持 + 機械検査の見張り」でまはー裁定 (隔離実験 90 回で再現なしを受けて)。スルースの実機初走行は採取まで成功 (新 schema の本番初成功、コア記憶 1 + 手帳メモ 1) したが、退場の関所が提示に載らない行 (discardable / sub_line volatile) を数えて永久保留になる欠陥と、手動経路が保留理由を「別の整理が処理中」へ誤写像して「予期しないエラー」に落ちる欠陥を発見 ([起票](../issues/archive/metabolism_deferral_mislabeled_as_window_claim.md))。裁定 =「経験」に提示に載らない裏方の行は含めない — 関所の照合から除外する。
- 2026-08-24 深夜 (診断の訂正): 上の「関所が提示に載らない行を数えて永久保留」という診断は**誤り**だった — 関所の窓は最初から提示と同じ規則で濾されており (実装エージェントが「規則は実装から導け」の指示で反証)、実際の機構は「整理の実行中に Chronicle が確定 → 冷えた起点の保守経路 (機構 1) が起点を前進 → 実行頭に撮った関所の窓とスルース入力がズレて退場だけ安全側で見送り」。既知の型でテストが仕様として固定済み、次回の整理で続きから進むので詰まない。まはーの裁定「提示に載らない行は経験に含めない」は現行コードが既に満たしていた。修正は表示の二段の嘘 (保留理由の誤写像 + 案内表の欠落) に絞った。issue は [metabolism_deferral_mislabeled_as_window_claim.md](../issues/archive/metabolism_deferral_mislabeled_as_window_claim.md) へ改名 (誤診の記録も収録)。
- 2026-08-29: §2 と §9 の棚卸し (リリース三日前)。§2-9 で裁定の食い違いを発見 — 8/25 に intent へ書かれた「ツール出力の被覆外退場を受け入れる割り切り」は、8/24 セッションの Claude の私見 (「いつか別途話そう」と先送りしたもの) が裁定を経ずに書き込まれたものだった (transcript で確定)。さらに 8/11 裁定の実装状況が三分と判明 (session_digest = 7/28 解決済み / event_message・知覚 = 8/19 W14 の出力付記 / ツール出力 = 未実装)。まはーが W14 の出力付記に手本汚染 (あらすじ本文は次の畳みの LLM の手本 — 機構ブロックを模倣して偽の機械記録を書き始める) を指摘し、実データ (eris 2 件、機構の採取判断があらすじ末尾に接がれていた) で確認。裁定 = 8/11 の原文どおり「提示に立った行はすべて材料へ、条件は長さのみ、意味の仕分けはしない」+ 出力側付記の撤去 (知覚バッチは材料へ移す)。途中案の「経験/帳簿」分類はまはーが却下 (「本人が見ている以上、帳簿も経験の一部」— 境界が実在しない)。新不変条件 = arasuji_levels §7-8 (出力の純潔)。§2-5 は 7/28 のコードで解決済みと確認して archive。実装はサブエージェント委譲で着手。
- 2026-08-29 夜 (実機検証): チェックリスト §1〜§3 を全消化し、§2-9・18・19 を完了に畳んだ。付随の発見と対処 — ① Fragment 抽出の空振り: 抽出も表示も生存と確認 (A/B 実験 6/6 で抽出成功、本番の空 1 回はモデルの揺らぎの尾が最有力)、揺らぎ空振りは検知不能という観察を [issue 起票](../issues/entity_extraction_empty_result_variance_is_undetectable.md)。② 再送・取り消しの印の鮮度: 後から応答が並んでも印が残る非対称をまはーが指摘し、門番と同じ物差しを表示側にも適用 (`fafd6274`)。③ ソフィーの空返答 3 件は NEBULA のタイムアウトが原因 (機構は設計どおり空行を隠して動作)。retry 門番はまはー承認の直接 API テストで 409 を確認。
- 2026-08-29 深夜 (アップグレード実機検証・段 1): まはーの提起「一番やらなきゃいけないのは現行リリースからのバージョンアップ」を受け、v0.2.29 (最終リリース、6/15) の世界を隔離環境に合成し v0.3 で起動する検証を実施。**起動不能の欠陥 1 件を発見・修正** (全書き換え移行の DB ファイル move が、import 時の addon ツール登録が残す接続プールの 1 本で Windows では必ず失敗 — migrate.py が move 前に共有エンジンを dispose する形で修理し、無垢からの一発跳びで合格)。移行・旧ログ取り込み・記憶の連続性 (ペルソナが跳びの前の会話を参照して応答) まで確認。派生発見 = §9-L (v0.2 Playbook 行のゾンビ残留)。同夜さらに**段 2 (遠距離跳び) も合格**: まはーが発掘した 3/13 の本番バックアップ (`~/.saiverse` の旧コピー、二都市・16 ペルソナ・エリス 594MB の記憶) を隔離 + ダミー API キーで v0.3 起動し、一発で完走 (会話は送らず、コピーのペルソナに認知は発生させていない)。建物履歴 0 件はバックアップ採取時のロック漏れ (コード欠陥ではない) と切り分け済み。残り = 段 3 (update.bat 経路)。詳細: [2026-08-29_v0229_upgrade_test.md](../handoff/2026-08-29_v0229_upgrade_test.md)
- 2026-08-30 未明 (§9-L の解決): Playbook 全置き換えマイグレーションを `feature/playbook-wholesale-replacement` に実装 (保険ブランチ — 重ければ元ブランチへ戻れる形、まはー指示)。UpgradeHandler dev5→dev6 で「全行を JSON 退避 (読み戻し検算つき fail-closed) → 全削除 → 退役名 permission 掃除 → 起動時同期が再取り込み」。削除とバージョン刻印は同一 commit で原子的。実機 = ゾンビ世界 62 行 / 3 月世界 64 行 + 許可 17→5 件の選別、二都市の二巡目 no-op も確認。Codex 一巡 5 件: 採用 2 (収集の部分失敗で中断する厳格モード / 空テーブルでも孤児 permission 掃除)、却下 2 (多 City 排他 — 並行に書かれ得る行はファイル由来で次回同期が復元・記録して受容 / 「同期失敗が再試行不能」— 同期は毎起動走るので自己回復する・事実誤認)、報告 1 (.env.bak の平文 — .gitignore に .env.bak* を追加、ファイルはまはーの物なので残置)。将来の宿題 = リリース版 0.3.0 を切るとき dev6→0.3.0 の空エッジ追加が必須 (機構の性質)。
- 2026-08-24: 記憶の整理 (Chronicle 生成) の確認窓が「残す量を超えていれば実行できる」と言っていたのを、実際に畳みが起きる条件へ揃えた。整理は残す量より古い側を U 文字 (一次あらすじの標準被覆、既定 10,000) ずつ刻んで畳むので、超過が U 未満の回は backend が noop を返し、押せたのに何も起きなかった (実機 104,980 / 残す量 100,000)。context-status に `fold_unit_chars` を足し、確認窓は超過が U 以上のときだけ実行を許して「あと何文字たまれば畳めるか」を出す
