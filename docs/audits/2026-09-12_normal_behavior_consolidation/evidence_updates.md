# 事実の更新 — 今回照合したこと、前の記述への訂正、確認の限界

照合した HEAD `b866d0b9` / 初稿の対象 `25ad75d6` (間に 72 コミット)

## この文書の約束

- **初稿・前回の台帳・二つの追加調査のうち、いま読むと違うものだけを挙げる。** 合っているものは挙げない。
- **「初稿はこう書いた → 実際はこう」の形で書く。** 引用できないものは訂正として扱わない。
- **確認の限界を各群の末尾に置く。** 何を確かめていないかが分かる形にする。

## 全体の傾向

**訂正は 58 件。前回と同じ傾向で、「無い」と書いた側の誤りがいちばん多い。**
存在するテスト・存在する設計文書・存在する告知を、初稿は数え落としていた。

| 群 | 訂正 | 初稿の後のコミットで既に直っていたもの |
|---|---|---|
| A 会話 | 13 | 6 |
| B 記憶 | 8 | 0 (中心のファイルに 1 コミットも入っていない) |
| C 世界 | 8 | 4 (うち 3 件はまはーの実機検証が未了) |
| D ペルソナと設定 | 8 | 3 |
| E 画面のない処理 | 7 | 0 (初稿が差として挙げていない箇所が 2 件直った) |
| F 導入・運用 | 14 | 5 |

**とくに重いもの 4 つ。**

1. **送る量の数字の契約が作り変わった** (`FLOW-04` `FLOW-09`)。初稿が表にした 4 つの水位のうち 3 つが機構ごと廃止され、
   組み込み既定は 120,000/40,000 から 60,000/20,000 へ下がり、画面から選ぶ 3 段のプリセットが入った。
   初稿の表はそのままでは使えない。
2. **記憶の中心のファイルには、初稿以後 1 コミットも入っていない** (`FLOW-07`〜`FLOW-13`)。
   動いたのは提示の組み立て方・送る量の既定・冷えた窓の見張りで、これらは初稿の**前提**を変えている。
3. **初稿が「別の経路でペルソナに届く」と書いた出来事の受信箱は、誰も開けていない** (`FLOW-16` `FLOW-24` `FLOW-25`)。
   書く処理と、起動時に読み込む処理はあるが、読み出す関数の呼び出し元がリポジトリ全体でゼロ。
4. **「無い」と書いたものが実在した 4 件** — スペル確認の設計文書 / 持ち主確認の検査 4 本 /
   Tailscale の前提を書いたランブックの節 / 3 層配置を定める契約。

---

## 群 A — 会話 (FLOW-01〜06)

### 今回新しく照合した事実

1. **知覚の二水位と保存時検査の余裕は機構ごと消えた。** `saiverse/model_configs.py:225-284` を読み、`BUILTIN_METABOLISM_TARGET_CHARS = 20_000` / `BUILTIN_METABOLISM_HIGH_CHARS = 60_000` と `METABOLISM_PRESETS` (多い 40,000/120,000・デフォルト 20,000/60,000・少ない 10,000/30,000) を確認した。`perception_high_chars` / `perception_target_chars` / `WATERMARK_HEADROOM_CHARS` をリポジトリ全体に grep したところ、本体コードでの現役の使用は無く、残るのは廃止を記す docstring・intent・archive 済み issue・旧版を保持する別 worktree だけだった。
2. **`tests/test_watermark_headroom_validation.py` は削除済み** (`791f3f5c`)。後継は `tests/test_model_watermark_validation.py` (16 本)。`tests/test_context_status.py` は現在 25 本、`tests/test_metabolism_global_defaults.py` は 41 本。
3. **`maybe_run_emergency_precompaction` の呼び出しは `sea/runtime.py:404` の一箇所だけ** (本体コードを grep)。プレビュー側に相当する処理は無い。
4. **プレビューのモデル解決は `sea/runtime_context.py:1502` の `getattr(persona, "model", None)`** で、実送信の `resolve_execution_context` とは別経路のまま。
5. **`saiverse/saiverse_manager.py:1423` の `set_model` は、非空の指定でメモリ上の全ペルソナへ適用する** (owner_decisions の記述を現物で確認)。
6. **`sea/pulse_context.py:99` の `aspect_from_pulse_type` は user と schedule をどちらも CONVERSATION にする** (同上)。
7. **`manager/runtime.py:479-483` の `_build_responding_personas` は派遣中 (`is_dispatched`) のペルソナを除外する。**
8. **`saiverse/saiverse_manager.py:1313-1345` の `cancel_active_generation` は現在建物の在室者全員へ `interrupted_by="user_stop"` を刻む。** `sea/runtime_llm.py:2194` の `_is_user_interruption` がその値を「ユーザーの操作により」の通告へ変える。
9. **スペル確認ダイアログには設計文書がある。** `docs/intent/x_integration.md:37-42` と `docs/intent/switchbot_integration.md:111-118`。
10. **`docs/user-guide/chat-options.md` と `docs/user-guide/items-and-files.md` はどちらも `d5c94a4a` が最終更新**で、今回の一系統化にもアイテム上限にも追随していない。
11. **`frontend/` にテスト基盤が無いことを再確認** (`*.test.*` が 0 本、`package.json` に `test` スクリプト無し)。
12. **`saiverse/uri_resolver.py` / `saiverse/media_utils.py` / `api/routes/uri.py` / `api/file_safety.py` は 25ad75d6 以降コミットが無い。**

### 初稿・前回台帳・追加調査の記述への訂正

1. 初稿 FLOW-04 §3-1 の表: 「`perception_high_chars` … 60,000 字」「`perception_target_chars` … 20,000 字」「`WATERMARK_HEADROOM_CHARS` … 10,000 字」の 3 行 → **この 3 つは廃止された。** 上限の組み込み既定も「120,000 字」→ **60,000 字**、残す量は「40,000 字」→ **20,000 字**。旧既定の組はプリセット「多い」へ降格した。
2. 初稿 FLOW-04 §5「既知の不一致 (2) — 台帳の矛盾リスト §3-6 は誤読」および §8 の「ブロックコメント『既定の 6万 / 4万』の古さは単なる記述の更新」→ **対象の定数とコメントごと書き換えられたので、訂正も指摘も残らない。** 現在の `saiverse/model_configs.py:225-244` は廃止の経緯を含めて現況を書いている。
3. 初稿 FLOW-04 §8「知覚の水位 2 つを画面に出すか。… `要追加確認`」→ **問いごと消えた。**
4. 初稿 FLOW-04 §3-2 / §8「モデル欄の効き先 … どちらが望ましい姿かは決まっていない」→ **2026-09-12 に決着** (その部屋で、かつユーザー入力への応答にのみ適用)。
5. 初稿 FLOW-04 §5「`tests/test_context_status.py` 27 本」「`tests/test_watermark_headroom_validation.py` 16 本」→ 前者は現在 **25 本**、後者は **ファイルごと削除** (後継は `test_model_watermark_validation.py`)。
6. 初稿 FLOW-05 §4「**スペル確認ダイアログ (CHAT-18) には設計文書が見つからない** — docstring に設計意図があるだけ」→ **誤り。** `docs/intent/x_integration.md` §3 が既定・オプトイン・削除の例外・自律パルス中の扱いを定め、`docs/intent/switchbot_integration.md` が汎用機構への昇格を設計している。設計が無いのは `no_channel` 分岐だけ。
7. 初稿 FLOW-05 §8 の三方向の表の読み方: 「三つとも同じ動機なのに結論が違う」は事実だが、**Playbook 権限とスペル確認については結論の側にも設計文書がある**。初稿は「スペル確認だけが実行する側に倒れており、かつ設計文書が見つからない」を判断材料に挙げていたが、後半は取り下げる。
8. 初稿 FLOW-01 §3「**発言は必ず一度だけ記録される。**」→ Astra の精密化を反映し、「**同じ送信 ID で二重に受理しない。受理された発言と未受理の発言を利用者が識別できる**」に改める。保存失敗による受付拒否の分岐が実在する。
9. 初稿 FLOW-01 §2-4「**その部屋の在室ペルソナ全員**が、リスト順に**逐次**返事をする」→ Astra の精密化を反映し、「**在室し派遣に出ていないペルソナへ順に応答を求める。全員の返答生成は保証しない**」に改める。
10. 初稿 FLOW-01 / FLOW-02 §4 の参照パス `docs/issues/user_utterance_path_failure_inventory.md` → 初稿の対象コミット時点で既に **`docs/issues/archive/`** 配下 (`e2efabfc`、2026-08-29 に archive へ移動)。`docs/issues/archive/retry_api_has_no_server_side_eligibility_check.md` も同様。
11. 初稿 FLOW-06 §8「添付が部屋に残り続けることを、利用者向け説明に書くか、機構として上限を設けるか。… 未合意ではなく設計待ち」→ **機構側は裁定・設計・実装まで完了** (`docs/intent/room_item_display_cap.md`、ステータス「検証待ち」)。残るのは利用者向け説明。
12. 初稿 FLOW-01 §3「話しかけた部屋と、発言が記録される部屋は必ず一致する」→ 利用者の発言については元から位置ガード付き INSERT で守られていたが、**ペルソナの発話については `b866d0b9` まで守られていなかった** (Pulse 途中の移動で移動後の発言が元の部屋に残る誤帰属、2026-09-12 実機観測)。現在は 1 Beat = 1 記録で解決済み。
13. 初稿 FLOW-01 §5「`quarantined` を画面が読んでいない。… 画面は『メッセージ 0 件の部屋』と同じ見た目になる」→ 事実だが、**`/api/system/quarantine` を読む別経路が Sidebar と QuarantineModal にあり、その部屋に印が付く**ことを今回確認した。「利用者が全く気づかない」とは書けない。

### 確認の限界

- 実行して確かめたものは無い。バックエンド・フロントエンド・LLM・Pulse をいずれも起動していない。すべてソースと文書の読みによる照合。
- `expansion_data/` 配下のアドオン (X / SwitchBot) は gitignore 対象のため、実際の確認呼び出しを現物で見ていない。`owner_decisions.md` に記録された限定確認に依拠している。
- 本番ログ (`~/.saiverse/`) を読んでいないので、停止の巻き添え・締切超過・プレビューと実送信の食い違いが実際にどれくらい起きるかは分からない。
- `sea/eviction_plan.py:plan_eviction` の内部、`saiverse/token_estimator.py` の推定精度、`presented_reduction.py` の縮みの実挙動は読んでいない。プリセットの数字と契約テストの存在までを確認した。
- ツールモードの「ツールを使わず話す」が撤去された裁定は、`git log -S` を掛けても見つけられなかった。無いことの証明ではない。

---

---


## 群 B — 記憶 (FLOW-07〜13)

### 今回新しく照合した事実

1. **群 B の中心のファイルは初稿以後 1 コミットも動いていない。**
   `git log --oneline 25ad75d6..HEAD -- sea/sluice.py sai_memory/arasuji api/routes/people/arasuji.py
   api/routes/people/memopedia.py api/routes/people/memory.py api/routes/people/core_memory.py
   sai_memory/core_memory.py sai_memory/memopedia scripts/arasuji` の出力が空。
2. **完了文の「まとめ N 件」は消費した予算 (試行を含む)。**
   `sea/session_lifecycle.py:6763-6767` が `consumed = max(attempts, created)` を累計し、
   `:7003` で `consolidated_folds` として渡し、`api/routes/people/arasuji.py:1357,1373` が
   「まとめ N 件」として表示する。確定数は別の変数 (`_band_created`) で、呼び直しの
   打ち切り判定にだけ使われている。
3. **実行直前の歯止めは件数だけ。** `api/routes/people/arasuji.py:1316-1324` の
   `estimate_stale` は `_count_repair_targets` の比較のみ。束ねの回数は
   `sea/session_lifecycle.py:6167` で実行時に計算し直される。
4. **レベルごとの上限超過を判定する関数は既にある。** `sai_memory/arasuji/bands.py:1137`
   の `_any_level_over_limit` が 1 クエリでレベル別の合計を出し、`BAND_CHAR_LIMIT` と比べる。
5. **Memopedia の編纂の翌朝報告は v0.3 では発火しない。**
   `sai_memory/curation_ops.py:1315` → `run_pending_plans` の呼び出し元は
   `builtin_data/tools/judgment_finalize.py:1186` だけ (リポジトリ全体の grep。他は
   docstring とテスト)。判断点は `saiverse/autonomy_wiring.py:91` の
   `AUTONOMOUS_DRIVING_SHIPPED = False` と `:217` の門で止まる。
6. **head の差分の読み取りはカテゴリで絞っていない。**
   `sea/head_pipeline/sections/memopedia_index.py:283-291` の SQL は
   `SELECT ... FROM memopedia_pages WHERE updated_at >= ? OR created_at >= ?`。
   コア記憶 (`sai_memory/core_memory.py:89`、題は「コア記憶 c:N」) とあらすじ
   (`sai_memory/arasuji/storage.py:503-507`、題は `chronicle:N`) も同じ表に入っている。
   コア記憶の訂正は `core_memory.py:374` が `updated_at` を進める。削除と復元
   (`:410` / `:502`) は `updated_at` を進めない。
7. **Memopedia の全削除はコア記憶を巻き込む。** `sai_memory/memopedia/core.py:1073` の
   除外は `page.id.startswith("root_")` と `page.category == "chronicle"` だけで、
   コア記憶のカテゴリ (`CATEGORY_CORE = "core"`) は除かれない。ページの id は
   `storage.create_page:557` の uuid なので `root_` では始まらない。
   入口は `api/routes/people/memopedia.py:292` と
   `frontend/src/components/memory/MemoryRecall.tsx:616,1161`。
8. **起動時の控えの失敗は起動を止めない。** `saiverse_memory/adapter.py:292` が
   daemon スレッドで起こし、`:1198-1201` が `BackupError` を WARNING に落とす。
9. **7 層ストレージに残る DELETE の口は退役した概念のもの。**
   `api/routes/people/storage_layers.py` の削除系は `meta-judgment` (`:426` / `:462`) と
   `track-logs` (`:494` / `:531`)。frontend に `storage-layers` の呼び出しは 0 件。
10. **送る量の既定とプリセット。** `saiverse/model_configs.py:245-246,270-284`。
    廃止の経緯は同ファイル `:239-244` のコメントと
    `docs/intent/presented_context_reduction.md` (ステータス: 完了、2026-09-11 実機検証)。
11. **未確認の件数の置き場所は台帳にまはー待ちとして立っている。**
    `docs/overview/in_flight.md:68`。
12. **利用者向け説明は初稿以後 1 行も直っていない。**
    `git log 25ad75d6..HEAD -- docs/user-guide` が空。`memory-migration.md:50` と
    `memory-view.md:12-13` と `memopedia.md:24` の記述はそのまま。

### 初稿・追加調査の記述への訂正

- **初稿の冒頭の表**: 「レベル0 の上限 20 万字 / 残す量 10 万字 (モデル設定 `high_chars` /
  `keep_chars`)」→ 実際には、その数字は `arasuji_levels.md §9` の記載であって出荷されている
  既定ではない。初稿の時点の組み込み既定は 120,000 / 40,000
  (`git show 25ad75d6:saiverse/model_configs.py:226-227`)、現在は 60,000 / 20,000 で、
  画面のプリセット 3 段から選ぶ。キー名も `metabolism_high_chars` / `metabolism_target_chars`。
- **初稿 FLOW-08 §5 の静的な疑い**: 「完了文に載るのが試行の側に**見える**。実行して
  確かめていないので疑いとして置く」→ 読んで確定した。試行を含む消費予算が載る。
  加えて `session_lifecycle.py:5771` の説明文が「確定した束ね」と書いており、
  実装が自分の説明と食い違っている。
- **初稿 FLOW-08 §5 の静的な疑い**: 「束ね回数がずれた場合の扱いは追えていない」→
  歯止め (`estimate_stale`) が未被覆メッセージの件数しか見ないことを確認した。
  束ねの回数のずれには歯止めが無い。
- **初稿 FLOW-09 §8 / FLOW-10 §8 の未合意**: 「意味検索の想起にシステム通知を含めるか」→
  稼働している経路は全部除外済みで、除外の根拠も明文がある。裁定を待つ問いではない
  (追加調査 a1 §2-c の実測)。
- **初稿 FLOW-11 §5-1**: 「編集・削除がペルソナに通知されるのはコア記憶だけ」→
  Memopedia については誤り。ただし**追加調査の「2 系統で届く」もそのままは引き継げない** —
  編纂の翌朝報告は v0.3 では発火しない (上の 5)。v0.3 で届くのは head の差分の 1 系統だけ。
  Astra の `astra_review.md` の留保 6 は正しかった。
- **初稿 FLOW-13 §5 の未追跡**: 「Memopedia の全削除が Fragment・クリップ・机の参照に
  何を残すか」→ あらすじには明示の関所があるが、コア記憶には無く、物理削除に巻き込まれる
  (上の 7)。
- **初稿 FLOW-13 §6 の未確認**: 「タイムアウトやロック待ちで取れなかった回に、起動が
  止まるのか進むのかは確かめていない」→ 進む。失敗はログの 1 行だけ (上の 8)。
- **初稿 FLOW-13 §5 の未追跡**: 「`storage_layers.py` の削除系エンドポイントの呼び出し元」→
  呼び出し元は無く、対象はどちらも退役した概念 (上の 9)。
- **初稿 FLOW-09 の流れ**: 応答の前後の段に、整理の瞬間の縮みが加わった。
  初稿は 10 分ごとの冷えた窓の見張りにも触れていない。

### 確認の限界

- **テストを一度も実行していない。** 「どの検査が存在し、何を通し何を差し替えているか」
  までが上限。
- **画面を操作していない。** 画面の話はすべてソースの読み。
- **本番のペルソナ・本番のデータに触れていない。** `~/.saiverse/` を読み書きしていない。
  LLM を呼んでいない。
- **head の差分がコア記憶とあらすじを拾うかを走らせて確かめていない。** 確かめ方は、
  隔離した `SAIVERSE_HOME` と合成の人格と fake LLM で、(1) あらすじを 1 本生成する、
  (2) コア記憶を 1 件訂正する、(3) 次の Pulse の頭で知覚の台帳に `world_state` の
  memopedia のラベルが積まれるかを見る。差分の基準時刻が整理の中のどの時点で進むかが
  この結果を決める。
- **Memopedia の全削除を実際に走らせていない。** コードの経路を読んだだけ。
- **あらすじ本文の書き換えを、まはーが実機の修復で使った実績があるかを確かめていない。**
  判断 B の推奨はこの点に依存する。

---

---


## 群 C — 世界 (FLOW-14〜18)

### 3-1. 今回新しく照合した事実

すべて HEAD `b866d0b9` のソースを開いて確かめたもので、**実行はしていない**。バックエンドも LLM も起動していない。`~/.saiverse/` は読み書きしていない。

1. **定員 0 は保存できる (初稿の「要追加確認」への回答)。** 更新の経路 `manager/admin.py:587` は受け取った定員をそのまま書き、数の検査が無い。ワールドエディタの更新は入力をそのまま送る (`WorldEditor.tsx:387`)。作成の経路だけは 0 を 1 に化けさせる (`:386`)。入室の判定は `saiverse/occupancy_manager.py:276,280` で、0 の部屋は在室 0 人でも常に断る。**さらに見つけた点**: 定員の欄は `value={formData.capacity || 1}` (`:593`) なので、0 を入力すると画面は 1 に戻って見えるのに、保存されるのは 0 になる。利用者は自分が何を保存したか画面から知れない。
2. **`saiverse://building/{id}/items` と `/history` は HEAD でも成立しない。** 呼ばれているメソッド `list_items_in_building` は `ItemService` に無く (実在は `get_open_items_in_building` / `get_all_items_in_building` / `get_bag_items_in_building`)、`manager.buildings` は `List[Building]` で `.get()` を持たず (`manager/initialization.py:180`)、`Building` クラスに `history` 属性は無い。参照の表 (`docs/reference/saiverse-uri.md`) は両方を約束したままで、`b866d0b9` はこの表に別の行 (`saiverse://building/{id}` = 画面の導線専用) を足しただけで、この 2 行には触れていない。
3. **アドオンが供給するトリガーは、この PC では X の 1 種だけ。** `expansion_data/` に入っている 7 つのアドオンを検索して、発火しているのは `saiverse-x-addon/integrations/polling.py:402` の `X_POLL_DETECTED` だけだと確かめた。残る 6 種は repo にもアドオンにも供給者が無い。**ただしこれは「この PC に入っているアドオンの範囲」であって、他の利用者の環境を数えたものではない。**
4. **物の記録と閾値の通知は、HEAD でも受け手を渡していない。** `saiverse/saiverse_manager.py:1025-1031` と `saiverse/observer_manager.py:707-728`。取り込み側の絞り込み (`builtin_data/tools/get_building_messages.py:382`) も、受け手の既定が空になること (`manager/history.py:94`) も変わっていない。
5. **`persona_event_log` は HEAD でも書き込み専用。** `fetch_pending_events` / `archive_events` を repo 全体で検索して、定義以外の一致はゼロだった。
6. **隔離の門の書き手は HEAD でも呼び出し元ゼロ** (`manager/initialization.py:435`)。消費側は 6 箇所以上ある。
7. **フィード施設は設置物の一覧に載る。** `GET /api/info/details` は建物の設置物を種類で絞らずに返す (`api/routes/info.py:202-228`) ので、右サイドバーの設置物欄にフィード施設が出る。
8. **世界の出来事から起きる Pulse に回数の歯止めは無いが、費用の記録は残る。** `phenomena/manager.py` に間隔・冷却の仕組みは見つからず、費用は `saiverse/usage_tracker.py` 経由で `llm_usage_log` に入る。
9. **右サイドバーからのアイテム作成は LLM を呼ばない。** `ItemCreateModal.tsx:81-93` が送るのは名前・種類・説明・置き場所だけで、ファイルの指定を含まないため、説明の自動生成の分岐に入らない。

### 3-2. 初稿・追加調査の記述への訂正

1. **初稿 FLOW-14 §8-1 / 申し送り 1**「建物ログの行の聴衆を誰が決めるか …『聴衆は世界が算出するものか、書き手が申告するものか』」→ **決着済みで、まはーへの質問にしない。** 追加調査 a2 が、受け手を渡す 8 箇所のうち 6 箇所は世界の在室者をそのまま渡していて、書き手が独自の名簿を作る箇所は一つも無いことを確認した。2026-08-27 に隣の関数 (発言の中断の通告) で同じ形が実機で見つかり、在室者を渡す形へ直して検査で固定した前例もある (`docs/issues/orphaned_streaming_placeholder_cleanup.md`)。残るのは配線であって価値判断ではない。
2. **初稿 FLOW-16 §2-5**「同室の他ペルソナへの `broadcast_item_event`」→ **その受信箱は誰も開けていない。** 書かれた行を読む側がリポジトリに存在しない (追加調査 a2 が確定、HEAD で再確認)。初稿はこれを生きている代替経路として数えていた。
3. **初稿 FLOW-14 §5**「0 が実際に保存されうるかは要追加確認」→ **保存されうる** (3-1 の 1)。
4. **初稿 FLOW-17 §5**「『Fixture は未実装』と書いている文書が 3 件」→ **4 箇所** (`landscape.md` に 2 箇所、`roadmap_status.md` に 1 箇所、`in_flight.md` に 1 箇所)。初稿は見出しで 3 件と書きながら 4 つ並べていた。
5. **初稿 FLOW-16 §2-1**「アイテムはワールドエディタのアイテムタブから作る」→ **作成の口は 2 つ** (`345c7910` で右サイドバーからの一般経路が入った)。
6. **初稿 FLOW-18 §5**「`SERVER_STOP` (`:1154`)」→ 現在は `saiverse/saiverse_manager.py:1159`。HEAD までの編集で行番号が動いた箇所は他にもある (物の記録の書き手が `:1021` → `:1025`、アイテムの削除が `:1117` → `:1165` など)。**指している場所と中身は同じ。**
7. **追加調査 a2 §5-5 の指摘を引き継ぐ**: `sea/head_pipeline/sections/building_occupants.py:22-25` の docstring が「移動が書いた行は取り込みを経てペルソナの記憶にも届く」と書いているが、取り込み側は移動の行を明示的に読み飛ばす。文書の直しの候補として記録する (初稿には無い)。
8. **初稿 FLOW-15 §3(a)** の「識別子は作った後は変わらない」は、**部屋の識別子については例外ができた** — フォルダ名や URL を壊す文字を含む古い識別子は、起動時に一度だけ付け替えられる (`195b2617`〜`0f15ff50`、v0.3.12)。まはーの GO 済みで、表示名は変わらない。

### 3-3. 確認の限界

- **実行していない。** 私が確かめたのは HEAD のソースと文書だけで、隔離環境での実測 (受け手を渡さない行がペルソナの記憶に入らないこと) は追加調査 a2 の結果をそのまま引き継いだもので、私が再実行したものではない。
- **定員 0 の保存を実際に試していない。** 経路をコードで追った読みで、画面から保存して DB を見たわけではない。
- **アドオンの数え方は、この PC に入っている 7 つに限る。** 他の利用者が入れているアドオンが別のトリガーを供給する可能性は否定できない。
- **2026-09-01 の観測値のスペルを隠す裁定の原文を見つけていない。** 見つけたのはコード内のコメントと、それを入れたコミットの本文だけ。コミット本文は「オブザーバー未出荷」と書いていて、まはーの 2026-09-09 の原文 (Fixture と Observer は v0.4.0 以降へ回す) と向きは揃っている。
- **部屋を消したときにファイルが残るか、街を消したときにフォルダが残るかは、削除する処理を見つけられなかったという形での確認**で、残ることを実地で見たわけではない。
- **ゲームの仕様はリポジトリ外**にあるので、FLOW-18 のゲーム部分の正常な姿は、リポジトリ内の根拠だけでは書き切れない。

---

---


## 群 D — ペルソナと設定 (FLOW-19〜23)

### 今回 HEAD で新しく照合した事実

| 見たもの | 場所 | 結果 |
|---|---|---|
| 開発者モードの一括更新 | `api/routes/config.py:513-550` | 残っている。DB の全ペルソナを False にして commit し、メモリ側も False にする。戻す処理は無い |
| 自律の判定を通す箇所 | `saiverse/autonomy_wiring.py:455, 1013, 1138, 1647` / `saiverse/saiverse_manager.py:1233, 1559` | 6 箇所。アラームの実行経路はどれも通らない |
| 設定を直読みする箇所 (`sea/` 全体) | `sea/runtime.py:1999` | 1 箇所だけ。キャッシュ維持の入口 |
| チャット画面のモデル指定の適用範囲 | `saiverse/saiverse_manager.py:1423-1453` | メモリ上の全ペルソナへ適用。部屋も発火元も見ていない |
| 予約に載らなかったことの扱い | `api/routes/people/schedule.py:164` と `ScheduleModal.tsx:288-294` | API は返し、画面は読んでいない |
| 一回きりのアラームの表示 | `ScheduleModal.tsx:337` | 「(完了)」を出す |
| 削除が触る範囲 | `manager/admin.py:1479-1530` | 3 つだけ。変更なし |
| `ai.AIID` を参照する列の数 | `database/models.py` | 21 本 (初稿と同じ) |
| 削除後に記憶へ辿り着けるか | `api/routes/people/utils.py:69` | 辿り着けない。存在検査で 404 |
| 表示名変更の巻き添え | `api/routes/user.py:229-243` | 全 City の自室名を条件なしで上書き |
| お知らせの停止トグルの読み手 | `api/routes/system.py:153-158` | バックエンドに読み手が無い |
| キャッシュ保管の通貨と書き込みトークン | `saiverse/usage_tracker.py:158-172, 205-218` | 通貨は入らず既定の USD になる。書き込みトークンは保存されない |
| チュートリアルのプリセット | `api/routes/tutorial.py:303-382` | 9 プロバイダ・18 キー。すべて `builtin_data/models/` に実在 (venv の Python で機械的に照合) |
| ライフ設定の API | `api/routes/people/` 一覧 | 設定の API は無い。`life.py` は会話バブルのクリップを読むだけ |
| 古い文書 | `persona-settings.md:23` / `world-editor.md:52` / `landscape.md:73` / `autonomous-mode.md:40` / `life.md` §9.2 | すべて HEAD でそのまま |

### 初稿・追加調査の記述への訂正

1. **初稿 FLOW-19「`tests/` に `tutorial` の語を含むファイルは 1 本も無い (grep の結果は空)」→ 実際はいま 3 本ある。** `tests/test_tutorial_preset_models_exist.py`、`tests/test_missing_model_startup_warnings.py`、`tests/test_update_default_model_admin_copy.py`。初回導入の全体を通す検査が無いことは変わらないが、「検査ゼロ」とは書けない。
2. **初稿 FLOW-19「`PROVIDER_PRESETS` が挙げる 19 個のモデル設定キー」→ いまは 18 キー・9 プロバイダ。** `50f9e0e6` が提供終了の組み込みモデル 22 件を外し、`2d08b289` `847afacf` がプリセットを役割をこなせるモデルへ付け替えた。手で照合した確認は機械検査に置き換わっている。
3. **初稿 FLOW-20「モデル・プロンプト・自律は保存時に即時」→ ペルソナ個別は今も即時だが、全体設定の標準モデルは違う。** `32284c5f` が即時反映を取り下げ、個別の標準モデルを持たないペルソナには再起動まで届かないことを警告で知らせる形にした (`manager/initialization.py:663`)。
4. **初稿 FLOW-23 §7「『消したペルソナの記憶を後から取り出す』ことは、現在の実装では可能である」→ 誤り。** ファイルは残るが、`ai` 行が消えた時点で記憶まわりの API はすべて 404 になる (`api/routes/people/utils.py:69`、削除調査 実験 4)。画面から辿り着ける経路は無い。
5. **初稿が引いた行番号のうち 2 つがずれた。** キャッシュ維持の入口は `sea/runtime.py:1994` ではなく `:1999` (`b866d0b9` の影響)。開発者モードの関数は `api/routes/config.py:519` ではなく `:513` から始まる。中身は変わっていない。
6. **削除調査 README の 3 つの言い回しは、同ディレクトリの `astra_review.md` が既に訂正している。** 「記憶が読める条件は ID 不変ただ一つ」は言い過ぎ、「無効化は完全に使える」は実測ではなく推論、「起動時にロードしない方式は禁止」は設計上の必然ではない。統合版はこの 3 つを前提に使っていない。とくに無効化の実装方式について、この統合版は特定の方式を排除していない。
7. **初稿 FLOW-21 の「起床・就寝のアラームの入口が無いのは、ライフ設定が削除されて前提が崩れた結果」→ 凍結裁定の側から読むと崩れではない。** v0.3 では判断点そのものが発火しないので、入口が無いことで利用者が失っている結果は無い。残っているのは、ライフ設定画面を実装完了と書いたままの文書。
8. **初稿 FLOW-20 の「ライフ設定 API とテストが削除されている」は HEAD でも正しい。** `api/routes/people/life.py` は名前が似ているが、会話バブルのクリップを読む別の API だった。

### 今回見つけた、初稿にも追加調査にも無い食い違い

- **テスト環境の注意書きと、アラームの実挙動が噛み合っていない。** `0427d254` が `docs/test_environment.md` を「立てて放置しただけでは LLM は動きません」と直し、止まっていない経路として会話と Metabolism を挙げた。しかし FLOW-21 で確定したとおり、利用者が作ったアラームは出荷停止定数でも自律設定でも止まらない。世界を複製するスクリプトは対象外ペルソナの自律設定を 0 にするだけなので (`scripts/clone_world_to_test_env.py:130`)、複製した世界に有効なアラームが残っていれば、放置でも発火して課金が出うる。**これは [ソース] の読みで、実機で確かめていない。** 群 F (FLOW-27 / FLOW-32) の担当範囲に寄るので、統合担当の判断で振り分けてほしい。

### 確認の限界

- 画面を操作していない。UI の記述はすべてソースの読み。
- バックエンドを起動していない。テストを実行していない。LLM を呼んでいない。
- 実験と書いたものは追加調査の報告の引用で、私は再実行していない。
- 本番の DB・ペルソナ・ログを読んでいない。実際の発生頻度は分からない。
- `META_JUDGMENT_CONFIG` のマージ失敗、アバターの保存の往復、`setup.bat` / `setup.sh` の分岐は追っていない。

---

---


## 群 E — 画面のない処理 (FLOW-24〜26)

### 今回新しく照合した事実

現在の HEAD (`b866d0b9`) でファイルを開いて確認した。

- キャッシュ維持の連鎖が止まる条件は、初稿が挙げた 4 つ (呼び出し失敗、anchor の失効、
  非 explicit への変更、自律 OFF) に加えて **ライフの谷** がある。`sea/runtime.py:2012-2019` が
  `saiverse.day_plan.is_keepalive_allowed` を呼び、その日にライフ区間が宣言されていて現在時刻が
  どの区間にも入らないときは温め直さない。判定に失敗したときは温める側へ倒す。
- 組み込みモデル定義は 125 個で、キャッシュ方式が `explicit` なのは 11 個、すべて Claude 系
  だった (`builtin_data/models/`)。初稿は 142 個中 11 個と書いており、分母が棚卸し
  (`50f9e0e6` ほか) で減っている。explicit の 11 個という数は変わっていない。
- 使用状況の表示名辞書はいまも 2 件だけである (`api/routes/usage.py:290-292`)。
- アラームの発火経路に自律ゲートの呼び出しは 1 件も無い (`saiverse/schedule_manager.py` を
  grep して確認)。owner_decisions の「自律 OFF でもアラームは動く」と実装が一致する。
- 実行台帳の掃除は各手順を個別に `try` で包み、その理由 (周期処理は例外で止まる契約である)
  を docstring に書いている (`saiverse/execution_ledger_wiring.py:239-299`)。findings a3 が
  「確認していない」と残した点で、確認した結果は初稿の記述どおりだった。
- 出来事の受信箱を読む関数の呼び出し元は、定義を除いてゼロだった
  (`persona/core.py:214`, `:224` を `.py` / `.ts` / `.tsx` で検索)。起動時の読み込みには
  件数の上限が無い (`manager/persona_events.py:24-32`)。
- 都市を「オンラインで起動する」設定が有効な世界では、起動時に SDS への登録と心拍の予約が
  走る (`saiverse/saiverse_manager.py:396-409`)。設定は画面から変更できる
  (`frontend/src/components/settings/WorldEditor.tsx:351`)。既定は無効である。
- 冷えたウィンドウの見張りの周期は 600 秒のままで、発火条件も初稿の 4 つのままである
  (`sea/session_lifecycle.py:152`, `:4664-4719`)。新しく、試し終えた回の状態を記録して
  変わっていなければ試さない素通しが入った (`:4508-4515`、`9242eb68`)。
- 返事の来なかった発言への導線は、`b866d0b9` で画面のコードが大きく動いた後も findings a3
  項目 7 のとおりだった。再送の印を立てるのは走行中のストリームの中だけである
  (`frontend/src/app/page.tsx:1688-1692`)。サーバー側の門番は再起動後も同じ判定をする
  (`api/routes/chat.py:1212-1257`)。
- `main.py:244` の `_sync_builtin_playbook_flags` は、いまも呼び出し元がゼロである
  (`.py` 全体を検索)。
- 終了処理の入口と順序は初稿のままで、走行中の発言を最初に締める理由がコメントに残っている
  (`main.py:561-609`、入口は `:618-619`)。凍結中の都市間 API の子プロセスは、起動前に
  そのポートを使っているプロセスを PID で終了させてから立ち上がる (`main.py:206-220`)。

### 初稿・前回台帳・追加調査の記述への訂正 (7 件)

1. 初稿は「`builtin_data/models/` の 142 個のモデル定義のうち `explicit` は Anthropic の
   Claude 系 11 個だけだった」と書いた。実際は現在 125 個中 11 個である。
2. 初稿は保温の連鎖が止まる条件を「呼び出し失敗・anchor 失効・非 explicit への変更・自律 OFF
   のいずれか」と書いた。実際はライフの谷も止める条件である (`sea/runtime.py:2012-2019`)。
3. 初稿は見張りを「600 秒ごとに全ペルソナを巡回し、条件が成立したペルソナだけを daemon
   スレッドへ逃がす」と書いた。実際はいま、条件が成立しても前回の試行から状態が動いていない
   ペルソナは素通しする (`9242eb68`)。
4. 初稿 (FLOW-25 §5-1) は legacy `user_data/` の移送について「移送しきれなかった項目は
   WARNING に名前が並び」と書いた。実際は飛ばした回の記録は DEBUG で、WARNING が出るのは
   コピーにも失敗した回だけである (`saiverse/data_paths.py:403`, `:433`)。
5. 初稿 (FLOW-26 §5-2) は「カーソルが失われると、次に開いたときにペルソナがどこまで読んだかを
   見失う」と書いた。実際は古い値へ巻き戻るだけで、取り込み済みの印があるため記憶に二重には
   入らない。失われるのは `log.json` の巻き戻りに絞られる (findings a3 項目 6)。
6. 初稿 (FLOW-26 §2) は「確定しなかった下書き行は content が空のまま DB に残る」を終了時の
   一般的な形として書いた。`b866d0b9` 以降、一言も生まれなかった行は取り下げられるので、
   本文の無い行が残るのは締切を超えた回に絞られる (`sea/runtime_llm.py:143-199`)。
7. 前回台帳 (`inventory.md:1526`) と `flows/C_world.md` は、アイテム操作には別の経路
   (`broadcast_item_event` から `record_persona_event` へ) があると書いた。実際はその受信箱を
   誰も開けていない (findings a2)。この流れでは「起動のたびに未処理の行が全部メモリへ
   読み直される」という形で現れる。

初稿が挙げた差のうち、コードの変更によって解消したものは 0 件だった。`9242eb68` は初稿が
差として挙げていなかった箇所 (見張りが同じ失敗を繰り返す件) を直している。

### 確認の限界

- バックエンドを起動しておらず、LLM も呼んでいない。画面を操作しておらず、UI はソースで
  読んだ。本番のペルソナとデータには触れていない。
- 8 秒の締切を実際に超える回、Windows でウィンドウを閉じたときの終了処理、SDS を有効にした
  世界での実際の登録要求、Unity を無効にした側の分岐で起動が最後まで通ることは踏んでいない。
- 12 本のデータ補填の中身 (`database/migrate.py`) は読んでいない。
- 先回りの畳みが途中で切られたときに何が残るかは追っていない (FLOW-09 の担当)。

---

---


## 群 F — 導入・運用・拡張 (FLOW-27〜32)

### 今回新しく照合した事実

すべて HEAD (`b866d0b9`) のソースを読んで確かめた。バックエンドは起動していない。

| 何を | どこで | 結果 |
|---|---|---|
| 更新の入口の集約 | `update.bat:8` / `update.sh:8` | 今も `scripts/update_engine.py --manual` の一本 |
| スナップショットの制限時間 | `scripts/update_engine.py:812` | `SNAPSHOT_TIMEOUT_SECONDS = 3600` の固定値のまま |
| 更新の鎖の辺と移行の担い手 | `saiverse/upgrade_handlers.py` 末尾 / `database/migrate.py:119` / `manager/initialization.py:156` | v0.3.11・v0.3.12 とも辺は空。列削除は migrate、部屋 ID の付け替えは起動時修復 |
| 復元手順の掲載 | `README.md` / `docs/user-guide/` / `docs/getting-started/` を検索 | `snapshot.bat restore` も `backup.py restore` も 0 件 |
| 標準モデルの反映 | `manager/initialization.py:571-716` / `api/routes/config.py:898` | 即時反映は取り下げ。警告を読むたびに作る形で確定 |
| モデル管理画面の告知 | `frontend/src/components/settings/ModelManagementPanel.tsx` を検索 | 「再起動」「反映」「notice」いずれも 0 件。プロバイダ側にだけ文面がある |
| 壊れない書き込み | `api/routes/config.py:1520,1550` / `api/routes/admin.py:104` | モデル JSON は直接上書き、`.env` は切り詰めてから書く形のまま |
| 3 層解決の形 | `saiverse/data_paths.py:67,130` / `saiverse/model_configs.py:589` | 読み込みはアドオンごと、モデル名の引き直しは平らな配置を見る |
| 3 層の契約 | `docs/intent/model_provider_management.md:47,528` | `expansion_data/<addon>/providers/` と明記されている |
| 起動時警告の取得 | `frontend/src/app/page.tsx:1141` | 画面が読むのは最初の描画の一回だけ |
| アドオンの失敗表示の導線 | `AddonManagerModal.tsx:1035,1197` / `MCPSection.tsx:156,243-251` | 折り畳みが既定、早期 return とも HEAD で同じ |
| Unity の待ち受け | `main.py:501,511` / `unity_gateway/server.py:85` | 既定 `"true"`、`ws://0.0.0.0:8765` のまま |
| 都市間の子プロセス | `main.py:542` / `main.py:153` | 毎回起動し、その前にポートを持つプロセスを強制終了する |
| SDS の登録 | `saiverse/saiverse_manager.py:396-409` / `database/models.py:286` | `START_IN_ONLINE_MODE` (DB 既定 False) が条件。切り替えはワールドエディタに残る |
| 中継先の環境変数 | `frontend/next.config.ts:34` / `frontend/src/app/api/**/route.ts` | 名前が 2 つに割れたまま、どちらも文書に無い |
| 隔離の検出器 | `manager/initialization.py:435` を全体検索 | 定義 1 件、呼び出し元 0 件 |
| バックアップ失敗の扱い | `database/backup.py:200-205` | ログ 1 行で先へ進む。起動時アラートには載らない |
| 起動時アラートの生産者 | `manager/initialization.py:175,288,349,394,528` | 5 箇所。うち 528 は呼ばれない。DB 破損・バックアップ失敗・アドオンは 1 件も無い |
| CI の実体 | `.github/` を列挙 | `FUNDING.yml` と workflows 2 本だけ |
| 版と履歴 | `VERSION` / `CHANGELOG.md` 冒頭 | `0.3.12` に対し CHANGELOG は `v0.3.0 (development)` のまま |
| 未解決 issue の所在 | `docs/issues/` と `archive/` を照合 | 群 F に関わる 9 本は未解決フォルダ、更新拒否の 1 本は archive |

### 初稿・前回台帳・追加調査の記述への訂正 (14 件)

1. **初稿 §FLOW-30-5「`OwnerAuthMiddleware` を検査するテストは 0 本である」→ 誤り。** `tests/test_owner_auth.py` に 4 本ある (未認証の拒否 / Bearer での状態変更 / ログインの HttpOnly クッキー / OAuth コールバックの例外パス)。このファイルは `cc2712e8` で入っており、初稿の対象コミットの時点でも存在していた。
2. **初稿 §FLOW-29-7「この例外パス (`/api/oauth/callback/`) を検査するテストは無い」→ 誤り。** 上の 4 本目 `test_oauth_callback_remains_available_for_state_validation` がそれを検査している。
3. **初稿 §FLOW-28-8「3 層解決の『正しい形』がどれかを決める文書が見つからない」→ 誤り。** `docs/intent/model_provider_management.md:528` が「`expansion_data/<addon>/providers/` も対象に含める」と書き、`CLAUDE.md` も同じ配置を規則として書いている。決めるのではなく、逸脱している側 (`get_data_paths` の平らな配置) を契約へ揃える案件である。
4. **初稿 §FLOW-30-5「SDS 登録と heartbeat のスケジュール登録は無条件に走る」→ 不正確。** 走る条件は `START_IN_ONLINE_MODE` で、DB の既定は False である。初稿も後段で発火条件としてこの列を挙げており、前段の「無条件」が過剰だった。凍結の対象になっていないという趣旨は正しい。
5. **初稿 §FLOW-30-5「『Tailnet に他人の端末を入れない』という前提は現在どこにも書かれていない」→ 誤り** (追加調査 b45)。`docs/getting-started/tailscale-runbook.md:176-181` に ACL の設定と不要な端末の削除がある。ただし推奨事項の箇条書きであって、SAIVerse の保護がそこに依存しているという説明ではない。
6. **初稿 §FLOW-30-5「欠けているのは機構ではなく案内」→ 成立しない** (追加調査 b45)。中継先が `127.0.0.1:8000` に固定されているため `--listen-host` の値が実質 `0.0.0.0` に限られること、中継先を差し替える環境変数の名前が 2 つに割れていてどちらも文書に無いこと、`/api/addon/events` だけが Cookie を落とすことの 3 点は、案内を直しただけでは残る。
7. **初稿 §FLOW-29-3/5「有効化の登録が失敗しても画面に現れない」→ MCP については誤り** (追加調査 b45)。分類つきの日本語の説明と再試行ボタンが既にある。問題は導線で、折り畳みが既定のため開くまで取得もバッジも走らず、アドオンのカードの中の同じ表示は現状描かれない。残る 3 種の登録については初稿の読みどおりである。
8. **初稿 §FLOW-31-5「DB (`building_messages`) が壊れたときの検出・隔離・復旧があるかを私は確かめていない」→ 無いことが確定した** (追加調査 b12)。あわせて追加調査の「`PRAGMA integrity_check` はリポジトリ全体で `database/backup.py` にしか無い」は微修正が要る。HEAD では `scripts/snapshot.py:382` にもあるが、どちらも控えやコピーに対して走るもので、動いている DB には走らないという結論は変わらない。
9. **初稿 §FLOW-27-5・§FLOW-32-5「`git archive` と `.gitattributes` の改行変換を確かめていない。README の約束が更新拒否に直行しうる」→ 壊れていなかった** (追加調査 b3)。公開中の本物の ZIP と照合して 1913 件が一致し、展開後の更新前検査も 3 通りすべてできれいだった。ただし「あらゆる版の配布・更新の健全性まで証明した」ことにはならない。
10. **初稿 §FLOW-28-6「`provider_change_does_not_reach_live_personas.md` の内容は要追加確認」→ 決着した** (追加調査 b6)。生成済みのクライアントが古い接続先を持ち続ける実体は残るが、issue が挙げた二案のうち「反映を保証しない設計にして明示する」方は、起票と同じコミット `9d405112` で UI とリファレンス文書に実装済みである。issue 本文の「UI には何も出ていない」を根拠に再起票しない。
11. **初稿 §FLOW-28-3「選んだモデルが次の発話から使われる」→ グローバル設定の標準モデルについては現行仕様と食い違う。** `32284c5f` で即時反映が取り下げられ、「個別の標準モデルを持たないペルソナには再起動するまで反映されない」を警告で知らせる形が採用された。期待の側を現行の確定に合わせる。
12. **初稿 §FLOW-32-8「開発者モード OFF の一括副作用は、まはー待ちの案件である」→ 決着した。** 2026-09-12 に、開発者モードは表示の切り替えとし、個別の機能設定を上書きしない方向で合意し、上書き先が現役の設定であることの確認も済んだ。判断待ちから外す。
13. **初稿 §FLOW-30-8「Unity ゲートウェイは依頼元が別途先行対処する」→ HEAD では未了。** 既定は `"true"` のまま、待ち受けは `0.0.0.0` のままである。`release_history.md` の「次の版の範囲」に「Unity 側はまはーが実装中」と記録があるので、別ブランチで進んでいる可能性がある (私はブランチを切り替えて確認していない)。
14. **初稿・追加調査の「起動時アラートに追記する箇所は 4 つ」→ HEAD では 5 つ。** 部屋 ID の付け替え (`manager/initialization.py:175`) が加わった。行番号も移動している (旧 250/308/353/487 → 現 288/349/394/528)。DB 破損・バックアップ失敗・アドオンの失敗が一件も載らないという結論は変わらない。

あわせて、初稿が触れていなかった変更を 2 件記録する。`50f9e0e6` ほかで組み込みモデルの棚卸しが行われ (提供元で終了した 22 件の削除、後継の追加、料金の修正、チュートリアルのプリセットの差し替え)、「設定ファイルが見つかりません」の警告が軽量・Memory Weave・グローバル設定の各役割へ対称化された。また `docs/overview/release_history.md` に v0.3.11 と v0.3.12 の発行が記帳され、フルスイートの手による記録は 5,884 緑と 6,043 緑まで伸びている。

### 確認の限界

- バックエンドを起動していないので、画面に実際に何が描かれるかはすべてソースの読みである。
- 別のマシンから物理 LAN 経由で実際に届くかは測っていない。追加調査の実測も同一ホストからの接続で、本番の UI と API を別端末から開けた証拠ではない。
- 凍結対象が起動しないことの確認も、実装がまだなので観測していない。
- Unity の対処が別ブランチで進んでいるかは、共有の作業ツリーを切り替えないため確かめていない。`release_history.md` の記述を引いただけである。
- テストは一本も実行していない。テストファイルの存在と関数名を読んだだけである。
- 初稿が「未追跡」とした境界のうち、接続テストの実エンドポイント判定、Codex のトークン保管、Discord の中継ボット側、`release.yml` の失敗時の挙動は今回も追っていない。

---

---
## 統合担当が自分で確かめたこと

各群の報告のうち、**新しい欠陥候補として成果物に載るもの**は、統合担当が現物を開き直した。
いずれも HEAD `b866d0b9` のソースを読んだだけで、実行していない。

1. **手帳の全削除は、コア記憶を一緒に物理削除する。** 除外されるのは `root_` で始まるページと
   あらすじの種別だけで (`sai_memory/memopedia/core.py:1068-1076`)、コア記憶の種別
   (`sai_memory/core_memory.py:93` の `CATEGORY_CORE = "core"`) は除かれない。
   あらすじには「2026-08-19 Codex 第五巡 #1」という日付つきの関所がコメントごと入っているのに、
   同じ表に同居しているコア記憶には入っていない。**同じ理由が当てはまる隣を見落とした形。**
2. **手帳の更新をペルソナへ知らせる読み取りは、種別で絞っていない。**
   `sea/head_pipeline/sections/memopedia_index.py:284-287` の問い合わせは
   `SELECT ... FROM memopedia_pages WHERE updated_at >= ? OR created_at >= ?` だけで、種別の条件が無い。
   コア記憶もあらすじも同じ表にいるので、「手帳『コア記憶 c:3』が更新されました」という形の知らせが
   ペルソナへ届きうる。**実際に届くかは走らせて確かめていない** (確かめ方は群 B の限界の節にある)。
3. **テスト環境の注意書きと、アラームの実挙動が噛み合っていない。**
   `docs/test_environment.md:139` は「**立てて放置しただけでは LLM は動きません**」と書き、
   止まっていない経路として会話と記憶の整理だけを挙げている。しかし利用者が作ったアラームは
   出荷停止の定数でも自律の設定でも止まらない (`FLOW-21` で確定)。
   世界を複製するスクリプトは対象外のペルソナの自律設定を 0 にするだけで、
   予定の表には一切触れない (`scripts/clone_world_to_test_env.py` に `schedule` の語が 0 件)。
   **複製した世界に有効なアラームが残っていれば、放置しても発火して課金が出る。**
   これは初稿にも二つの追加調査にも無かった食い違いで、群 D が見つけ、統合担当が現物で確かめた。
   `0427d254` (2026-09-12) がこの注意書きを v0.3 の実態へ更新した直後なので、**新しく生まれた食い違い**。

4. **引用したテストと文書のパスを、全部実在するか機械で当てた。**
   統合版が「[既存] の検査」として名前を挙げたテスト 56 本のうち、**1 本は実在しない名前だった** —
   `tests/test_admin_env_default_model.py` と書かれていたが、その中身に当たるのは
   `tests/test_update_default_model_admin_copy.py` (グローバル設定の標準モデルが、起動時に写された側まで揃うか)
   だった。**無いものを「既にある検査」として渡しかけた**ので、渡す前に直した。
   残る 55 本と、引用した文書 79 本はすべて実在した。


5. **初稿が、まはーの原則を適用場面の違う採点表として流用していた (2026-09-13、まはーの指摘で判明)。**
   `decisions.md` 議題 6 は「止めるのではなく、記録し、明示し、選択してもらう」を三つの軸に分解して
   削除の操作を採点し、「アドオンのデータ削除と DB の初期化は、『明示』と『選択』は満たしていて
   『記録』だけが無い (控えが残らない)」と整理していた。**この原則はエラーやイレギュラーで機構が
   止めたくなるときの規範**で、利用者が自分の意思で操作して正常に完遂される処理の規範ではない。
   まはーの指摘 — 「この話のどこに『止める』要素があるの？」。
   統合担当はこの採点表を検算せずに引き継ぎ、まはーへの判断として出した。

   **利用者自身の不可逆な操作を測る規範は、同日まはーが示した** —
   「ユーザー操作の結果はその影響範囲をユーザーの期待と合わせる。不可逆な操作であるほどこの規範は
   厳しく守られるべきで、削除はその筆頭」。期待がはっきりしないなら訊く。
   この規範を [normal_behavior.md](normal_behavior.md) の冒頭に独立した節として置き、削除の 7 つの流れへ当てた。

   **同じ原則を引いている箇所を全部見直した。** `FLOW-31` (壊れたときに復旧する) は、壊れたときの対処と
   利用者自身の初期化の操作が同じ節に混ざっていたので、二つを分けて両方の規範を書いた。
   `FLOW-19` (はじめて導入する) の根拠からは外した (飛ばした手順を先に知らせることは、同じ製品の中の前例で足りる)。
   `FLOW-02` の材料の中でこの原則を「ここでは答えを出さない (停止は利用者自身が選んだ操作だから)」と
   除外していた記述は、**まはーの指摘と同じ線を引いていたので正しかった。**
   統合担当は、その正しい除外と誤った流用を、矛盾に気づかずに同じ文書へ並べていた。

   **指摘を受けた直後、統合担当は問いそのものを取り下げ、まはーに戻された** —
   「今のはまだ指摘であって、どう直せとは言ってないんだけど……。今回メティスが 5 について聞いてくれたこと自体は正しい」。
   壊れていたのは根拠で、問いではなかった。経緯は [remaining_decisions.md](remaining_decisions.md) の決着 5 にある。

## この文書全体の確認の限界

- **バックエンドを起動していない。テストを一度も実行していない。** 「検査が存在する」と「合格する」を分けてある。
- **画面を操作していない。** 画面の記述はすべてソースの読み。
- **本番のペルソナ・本番のデータに触れていない。** `~/.saiverse/` を読み書きしていない。LLM を呼んでいない。
- **[実験] と書いたものの大半は、二つの追加調査が隔離環境で観測した結果の引き継ぎ**で、今回再実行していない。
- **別のマシンからの実到達、締切を超える生成の通し、凍結対象が起動しないことの確認は、いずれも未測定。**
- **ゲームの仕様はリポジトリの外**にあるので、`FLOW-18` のゲームの部分はリポジトリ内の根拠だけでは書き切れない。
- 各群ごとの限界は、上の群別の節の末尾にある。
