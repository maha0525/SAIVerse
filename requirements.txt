discord.py>=2.4.0
websockets>=12.0
httpx>=0.27.0
fastapi==0.116.1
google-genai>=1.75.0,<2.0
langgraph==1.0.3
openai==1.97.0
anthropic>=0.42.0
pydantic==2.11.7
pydantic-settings==2.5.2
python-dotenv==1.1.1
requests==2.32.4
SQLAlchemy==2.0.41
uvicorn==0.35.0
python-multipart>=0.0.9
cryptography>=45.0.0
tzdata

# SAIMemory embeddings
fastembed>=0.7.3
numpy>=1.26.0

# Image processing
Pillow>=11.0.0
markdownify>=0.12.0

# Audio/Video processing (bundles ffmpeg binary per-platform via pip wheels)
imageio-ffmpeg>=0.6.0

# PDF processing
pypdf>=4.0.0

# RSS/Atom feed intake (saiverse/feed_fetch.py)
feedparser>=6.0
# HTML autodiscovery (<link rel="alternate"> の解析、discover_feed)。markdownify の
# 従属でも入るが、直接 import する依存は直接宣言する
beautifulsoup4>=4.12

# xAI (Grok) native SDK
xai-sdk>=1.6.0

# MCP (Model Context Protocol) client
mcp>=1.10.0

# Codex 系クライアント (llm_clients/openai_codex.py が利用)
curl-cffi>=0.10.0

# Linting
ruff>=0.9.0
