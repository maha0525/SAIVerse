# Development dependencies for SAIVerse
# Install with: pip install -r requirements-dev.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Linting (also in requirements.txt for convenience, but listed here for CI)
ruff>=0.9.0