Playbook placeholders

- meta_user.json: Router(構造化出力)→exec→speak。routerはJSON schemaでplaybook/args/reasonを返します。
- meta_auto.json: Router(構造化出力)→exec→think。autonomous用途向け。
- building/personal/public スコープの Playbook は DB 連携後にここから移行予定。
